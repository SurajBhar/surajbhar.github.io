<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://surajbhar.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://surajbhar.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-01-12T17:15:37+00:00</updated><id>https://surajbhar.github.io/feed.xml</id><title type="html">blank</title><subtitle>Portfolio of Suraj Bhardwaj. </subtitle><entry><title type="html">Generative Modeling- From Datasets to GANs</title><link href="https://surajbhar.github.io/blog/2026/01/generative-modeling/" rel="alternate" type="text/html" title="Generative Modeling- From Datasets to GANs"/><published>2026-01-12T00:00:00+00:00</published><updated>2026-01-12T00:00:00+00:00</updated><id>https://surajbhar.github.io/blog/2026/01/generative-modeling</id><content type="html" xml:base="https://surajbhar.github.io/blog/2026/01/generative-modeling/"><![CDATA[<p>A one-screen map of the journey:</p> <pre><code class="language-mermaid">%%{init: {
  "theme": "default",
  "flowchart": { "nodeSpacing": 60, "rankSpacing": 60 },
  "themeVariables": { "fontSize": "20px" }
}}%%
flowchart TB
  A["Dataset → empirical distribution"] --&gt; B["Likelihood &amp; density estimation"]
  B --&gt; C["Latent variables &amp; EM"]
  C --&gt; D["Deep explicit models\n(autoregressive, flows, VAE)"]
  D --&gt; E["Distribution mismatch\n(divergences)"]
  E --&gt; F["Convex duality → variational bound"]
  F --&gt; G["Min-max game → GANs"]
</code></pre> <p>Generative modeling is the art of <strong>learning to sample</strong>. Given a dataset (images, text, audio, tabular records, or multimodal pairs), we want a model that can generate new samples that look like they came from the same underlying <em>data-generating process</em> — formally, an unknown “true” distribution $ (P_{\mathrm{data}}) $ on an appropriate data space $ (\mathcal{X}) $. This “true distribution” is not something we ever observe directly; it is a <strong>modeling abstraction</strong> that says: there exists some probability law that could, in principle, produce the kind of examples we see. What changes across modalities is the space $ (\mathcal{X}) $ and what “probability” means.</p> <p>For <strong>text</strong>, a datapoint is usually a token sequence $ (x_{1:T}) $ over a vocabulary, so $ (P_{\mathrm{data}}) $ is a <em>discrete</em> distribution (a PMF) over all possible sequences. In other words, it assigns probability mass to every possible sentence, paragraph, or document, including ones you never saw in the dataset. For <strong>audio</strong>, a datapoint might be a waveform (continuous amplitudes) or quantized samples (discrete PCM values), so $ (P_{\mathrm{data}}) $ can be modeled as a continuous density or a discrete mass function depending on representation. For <strong>multimodal data</strong> (e.g., text–image, vision–audio, or text–audio), the most faithful object is a <strong>joint distribution</strong> $ (P_{\mathrm{data}}(x^{(a)},x^{(b)})) $ over paired modalities (caption and image, video and sound), and many tasks are naturally expressed as <strong>conditional</strong> generation, like $ (P_{\mathrm{data}}(\text{image}\mid \text{text})) $ or $ (P_{\mathrm{data}}(\text{audio}\mid \text{video})) $. So “learning the distribution” can mean learning the joint, the conditional, or both.</p> <p>In large language models like GPT based models, $ (P_{\mathrm{data}}) $ should be understood as the distribution induced by the <strong>data collection and filtering pipeline</strong> (a huge but still incomplete slice of the world’s text: web pages, books, code, and other sources), not literally “the entire internet.” The generative goal is the same across all modalities: not copying training examples, but learning the statistical regularities — syntax, semantics, style, structure, and cross-modal alignment — that make the data what it is, so that new samples are plausible under the same underlying process.</p> <p>This post is a timeline, but it is also a logic chain.</p> <ol> <li>We start from the most basic question — <em>what is a dataset, mathematically?</em> — and build up the probabilistic objects we will need (distributions, densities, expectations).</li> <li>Then we move through classical density estimation (MLE, KDE, Bayesian thinking), into latent-variable models (mixtures, EM), and finally into deep generative models (autoregressive models, flows, VAEs).</li> <li>At the end, we show why GANs appear naturally when you try to minimize distribution mismatch without a tractable likelihood.</li> </ol> <hr/> <h2 id="0-from-a-dataset-to-a-distribution">0. From a dataset to a distribution</h2> <h3 id="01-what-a-dataset-is-mathematically">0.1 What a dataset is (mathematically)</h3> <p>A dataset is a finite collection of observations. To do probability with it, we treat each observation as a point in a space $ (\mathcal{X}) $, and we treat the dataset as samples from an unknown distribution.</p> \[\mathcal{D}=\{x^{(1)},x^{(2)},\ldots,x^{(n)}\}, \qquad x^{(i)} \stackrel{\text{i.i.d.}}{\sim} P_{\mathrm{data}}.\] <p>This single line is the “probabilistic starting point” of almost all generative modeling. It says that there exists an unknown distribution $ (P_{\mathrm{data}}) $ over the data space $ (\mathcal{X}) $, and our dataset $ (\mathcal{D}) $ is generated by drawing $ (n) $ samples from that distribution. The symbol “i.i.d.” means independent and identically distributed: each example is drawn from the same distribution, and the draws are independent of each other. This does not claim that the <em>components</em> of an example are independent; it only models different examples (different images, different sentences) as separate draws. The i.i.d. assumption is the bridge that lets us replace expectations with averages, which is what makes mini-batch training meaningful. When i.i.d. is wrong (time series, trajectories, dialogue), we will still use the same distributional thinking, but the model will be conditional or sequential. So before learning any model, we commit to this idea: data is not just a list of files, it is a finite window into a much bigger distribution.</p> <h3 id="02-the-empirical-distribution">0.2 The empirical distribution</h3> <p>A dataset gives you samples, not the true distribution. or A dataset gives you <strong>examples</strong>, not the full “rule” that created them. One useful “distribution built from the dataset” is the empirical distribution, which places equal mass on each observed point. Or in other words, A simple way to turn the dataset into a probability distribution is the <strong>empirical distribution</strong>: imagine a bag that contains exactly your (n) training examples, each appearing once. If you pick one item uniformly at random from that bag, every training example has probability (1/n). That “uniform-over-the-dataset” rule is $ (\widehat{P}_n) $.</p> \[\widehat{P}_n=\frac{1}{n}\sum_{i=1}^{n}\delta_{x^{(i)}}, \qquad \widehat{P}_n(A)=\frac{1}{n}\sum_{i=1}^{n}\mathbf{1}\{x^{(i)}\in A\}.\] <p>Here $ (\delta_{x^{(i)}}) $ is a Dirac measure concentrated at the point $ (x^{(i)}) $, and $ (\mathbf{1}{\cdot}) $ is an indicator function that equals $ (1) $ when its condition is true and $ (0) $ otherwise. The symbol $ (\delta_{x^{(i)}}) $ means “<strong>all probability mass is placed at the single point</strong> $ (x^{(i)}) $” (a mathematical way to say “choose exactly that sample”).</p> <p>The indicator $ (\mathbf{1}{x^{(i)}\in A}) $ is just a yes/no counter: it equals (1) if the sample $ (x^{(i)}) $ falls inside the set (A), otherwise (0). So $ (\widehat{P}_n(A)) $ simply counts <strong>what fraction of your dataset lies in (A)</strong>.</p> <p>Why this matters: sampling from $ (\widehat{P}_n) $ can only return <strong>copies of training examples</strong>, because it assigns probability only to the points you already observed.</p> <p>That’s pure memorization. A generative model tries to learn a smoother, more general distribution $ (P_\theta) $ that explains the dataset but also assigns probability to <strong>new, plausible points</strong> not explicitly present in $ (\mathcal{D}) $.</p> <p>Many learning objectives can be read as: make \((P_\theta)\) close to \((\widehat{P}_n)\) and therefore close to the unknown \((P_{\mathrm{data}})\) in a principled way.</p> <p>So $ (\widehat{P}_n) $ is not the final goal, but it is the clean “starting distribution” that makes it clear what we have (finite samples) and what we want (a true generative distribution that generalizes).</p> <h3 id="03-random-variables-and-data-spaces">0.3 Random variables and data spaces</h3> <p>A random variable is a measurable function from an abstract probability space into a value space. You can think of this as a “random sample generator” that outputs one data example each time you run it. In practice, you can think of it as “a draw from a distribution,” but it helps to be explicit about the space.</p> \[X:\Omega\to\mathcal{X}, \qquad X\sim P_{\mathrm{data}}, \qquad x^{(i)}=X(\omega_i).\] <p>The notation $ (X:\Omega\to\mathcal{X}) $ means the random variable $ (X) $ takes an underlying random outcome $ (\omega\in\Omega) $ and maps it to a concrete datapoint $ (x\in\mathcal{X}) $. You can think of this as: “there is a hidden random seed $ (\omega) $, and when you plug it into the real-world generator $ (X) $, you get one sample $ (x) $.”</p> <p>The statement $ (X\sim P_{\mathrm{data}}) $ means the outputs of $ (X) $ follow the (unknown) data distribution $ (P_{\mathrm{data}}) $.<br/> Think of this as: “the real world has a rule for how likely different kinds of samples are, and $ (P_{\mathrm{data}}) $ is that rule.”</p> <p>Each dataset example $ (x^{(i)}) $ is one realized value of $ (X) $ for some outcome $ (\omega_i\in\Omega) $, written $ (x^{(i)}=X(\omega_i)) $. This implies: “your dataset is what you got after pressing the ‘generate real data’ button $ (n) $ times with $ (n) $ different hidden seeds.”</p> <aside><p> <strong>Why is \(\Omega\) abstract?</strong><br/> Mathematically, \(\Omega\) is the set of all possible “random outcomes,” but we almost never need to describe it explicitly. You can think of \(\Omega\) as a bookkeeping device for randomness: it represents everything that could vary in the world (lighting, pose, background, word choice, microphone noise, camera motion), without forcing us to list those factors one by one. </p></aside> <h4 id="what-is-the-data-space--mathcalx-">What is the data space $ (\mathcal{X}) $?</h4> <p>The space $ (\mathcal{X}) $ is the set of all valid objects your dataset lives in (all possible images, all possible videos, all possible sequences, etc.). You can think of $ (\mathcal{X}) $ as the “format” of your data: whatever can be represented as a valid example counts as a point in $(\mathcal{X})$.</p> <p><strong>Example: RGB images.</strong> An RGB image of height $ (H) $ and width $ (W) $ has $ (H\times W\times 3) $ pixel values. After flattening one image tensor, it becomes a very large vector made of numbers. In literature, this large vector is represented by $ (d) $ and is also known as the dimensionality of one sample in our dataset.</p> <p>For example, If the resolution of an image in the image dataset is 400x400, then the value of $ d = (H\times W\times 3) = (400\times 400\times 3) = 480,000 $.</p> <p>If pixels are stored as integers, a natural data space is: \(\mathcal{X}=\{0,1,\ldots,255\}^{H\times W\times 3}.\) This means: “each pixel channel is one of 256 discrete values, so the whole image is a huge grid of discrete numbers.”</p> <p>If pixels are normalized to real values (common in deep learning pipelines), a natural data space is: \(\mathcal{X}=[0,1]^{H\times W\times 3}.\) This means: “each pixel channel is a real number between 0 and 1, so the image lives in a continuous space.”</p> <p><strong>Example: videos.</strong> A video clip with $ (T) $ frames is a sequence of images, so its tensor shape is $ (T\times H\times W\times 3) $.<br/> A video can be interpreted as “an image, repeated over time,” which makes the data object even higher-dimensional.</p> <p>A corresponding discrete space is: \(\mathcal{X}=\{0,1,\ldots,255\}^{T\times H\times W\times 3},\) and a corresponding continuous space is: \(\mathcal{X}=[0,1]^{T\times H\times W\times 3}.\) In other words, you can think of these as: “all possible valid video clips of that length and resolution, either stored as integers or as normalized real values.”</p> <p>For text, $ (\mathcal{X}) $ is often a space of token sequences; for tabular data, it is a product space mixing continuous and categorical coordinates.</p> <h4 id="why-does-choosing--mathcalx--matter-pmf-vs-pdf">Why does choosing $ (\mathcal{X}) $ matter (PMF vs PDF)?</h4> <p>Stating $ (\mathcal{X}) $ forces you to decide whether your model should treat outcomes as discrete or continuous, which determines whether probabilities are computed by sums (PMF) or by integrals (PDF).<br/> In other words, you can think of this as: “if your data values are countable choices, you add probabilities; if your data values are real-valued, you integrate densities.”</p> <p>This is where vector-valued random variables naturally appear: an image (or video) is not a single number but a high-dimensional vector/tensor random variable. In other words, you can think of generative modeling as learning a probability law over <em>entire structured objects</em>, not just over scalars.</p> <h4 id="mental-model-two-machines">Mental model: “two machines”</h4> <p>There is a real (unknown) machine that generates data according to $ (P_{\mathrm{data}}) $, and your dataset is a finite set of its outputs.<br/> In other words, you can think of the world as a black-box generator you can sample from, but you cannot inspect directly.</p> <p>A generative model builds a new machine $ (P_\theta) $ (or a sampler $ (x=g_\theta(z)) $) that tries to produce outputs with the same statistical patterns as the real machine.<br/> In other words, you can think of training as: “tune the knobs $ (\theta) $ until the fake samples become indistinguishable (in distribution) from the real ones.”</p> <p>Once you internalize this dataset $ \rightarrow\ $ random variable $ \rightarrow\ $ distribution view, most generative models become variations on one theme: pick a model family on $ (\mathcal{X}) $, and fit it from samples.<br/> In other words, you can think of the rest of this post as exploring different “machines” (MLE, EM, VAEs, GANs) that all aim to learn the same kind of object: a good approximation to $ (P_{\mathrm{data}}) $.</p> <hr/> <h2 id="1-probability-building-blocks-you-will-actually-use">1. Probability building blocks you will actually use</h2> <p>This section is short on purpose, but it is extremely important. Almost every generative model is just a clever way to compute (or avoid computing) these three things: (1) probabilities of events, (2) how to relate joint/marginal/conditional distributions, and (3) how to replace integrals with averages over data.</p> <hr/> <h3 id="11-pmf-vs-pdf-vs-cdf-and-why-it-matters">1.1 PMF vs PDF vs CDF (and why it matters)</h3> <p>A key idea: <strong>probability is about a region or a set of outcomes</strong>, not about a single exact point (when things are continuous).</p> <p>If $ (X) $ is <strong>continuous</strong> (like a real-valued measurement), we use a <strong>probability density function</strong> (PDF) $ (p(x)) $ and compute event probabilities by <em>integrating</em>:</p> \[P(X\in A)=\int_A p(x)\,dx \quad \text{(continuous PDF)}.\] <p>If $ (X) $ is <strong>discrete</strong> (like a token ID or an integer-valued pixel), we use a <strong>probability mass function</strong> (PMF) $ (p(x)=P(X=x)) $ and compute event probabilities by <em>summing</em>:</p> \[P(X\in A)=\sum_{x\in A} p(x) \quad \text{(discrete PMF)}.\] <p>In other words, you can think of it like this:</p> <ul> <li>A <strong>PMF</strong> is like a table of probabilities for each possible outcome (token 42, pixel value 128, class “cat”, etc.).</li> <li>A <strong>PDF</strong> is like a “height map” over a continuous line/space; probability comes from the <strong>area under the curve</strong> over a region.</li> </ul> <p>A very common confusion is: “why isn’t $ (p(x)) $ itself a probability in the continuous case?” Because for a continuous variable, the probability of hitting exactly one point is essentially zero. So a PDF value $ (p(x)) $ is more like “how concentrated the distribution is near $ (x) $,” and the real probability comes from integrating over an interval/region around $ (x) $.</p> <pre><code class="language-vega_lite">{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "hconcat": [
    {
      "width": 260,
      "height": 180,
      "title": "PMF (discrete)",
      "data": { "values": [
        {"x": 0, "p": 0.10}, {"x": 1, "p": 0.25}, {"x": 2, "p": 0.40}, {"x": 3, "p": 0.20}, {"x": 4, "p": 0.05}
      ]},
      "mark": "bar",
      "encoding": {
        "x": {"field": "x", "type": "ordinal", "title": "outcome x"},
        "y": {"field": "p", "type": "quantitative", "title": "p(x)"}
      }
    },
    {
      "width": 260,
      "height": 180,
      "title": "PDF (continuous): probability = area",
      "data": { "values": [
        {"x": -3, "y": 0.004}, {"x": -2, "y": 0.054}, {"x": -1, "y": 0.242},
        {"x": 0, "y": 0.399}, {"x": 1, "y": 0.242}, {"x": 2, "y": 0.054}, {"x": 3, "y": 0.004}
      ]},
      "layer": [
        {
          "mark": {"type": "line"},
          "encoding": {
            "x": {"field": "x", "type": "quantitative", "title": "value x"},
            "y": {"field": "y", "type": "quantitative", "title": "p(x)"}
          }
        },
        {
          "transform": [{"filter": "datum.x &gt;= -1 &amp;&amp; datum.x &lt;= 1"}],
          "mark": {"type": "area", "opacity": 0.25},
          "encoding": {
            "x": {"field": "x", "type": "quantitative"},
            "y": {"field": "y", "type": "quantitative"}
          }
        }
      ]
    }
  ]
}
</code></pre> <p><strong>Why it matters in ML (a real example).</strong><br/> In image modeling, you must decide how you treat pixel values:</p> <ul> <li>If pixels are treated as integers in $ ({0,\ldots,255}) $, then your model outputs a <strong>PMF</strong> (often categorical probabilities).</li> <li>If pixels are treated as real numbers in $ ([0,1]) $, then your model outputs a <strong>PDF</strong> (often Gaussian or logistic-like).</li> </ul> <p>In other words, you can think of this as choosing the “measurement type”: are pixel values “countable categories” (discrete) or “real measurements” (continuous)? This choice changes the likelihood $ (p_\theta(x)) $ and therefore changes the training signal.</p> <p><strong>What about the CDF?</strong><br/> The cumulative distribution function (CDF) is $ (F(t)=P(X\le t)) $. It is very useful in 1D because “$ (\le) $” makes sense on a line. But in high-dimensional data (images, audio spectrograms, embeddings), there is no natural single “$ (\le) $” ordering for vectors. That is why deep generative modeling usually uses PDFs/PMFs (or samples) rather than CDFs.</p> <pre><code class="language-vega_lite">{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "width": 260,
  "height": 180,
  "title": "CDF (continuous): F(t)",
  "data": {
    "values": [
      {"x": -3, "F": 0.001},
      {"x": -2, "F": 0.023},
      {"x": -1, "F": 0.159},
      {"x": 0,  "F": 0.500},
      {"x": 1,  "F": 0.841},
      {"x": 2,  "F": 0.977},
      {"x": 3,  "F": 0.999}
    ]
  },
  "layer": [
    {
      "mark": {"type": "line"},
      "encoding": {
        "x": {"field": "x", "type": "quantitative", "title": "threshold t"},
        "y": {"field": "F", "type": "quantitative", "title": "F(t)", "scale": {"domain": [0, 1]}}
      }
    },
    {
      "mark": {"type": "point", "filled": true, "size": 40},
      "encoding": {
        "x": {"field": "x", "type": "quantitative"},
        "y": {"field": "F", "type": "quantitative"},
        "tooltip": [
          {"field": "x", "type": "quantitative", "title": "t"},
          {"field": "F", "type": "quantitative", "title": "F(t)", "format": ".3f"}
        ]
      }
    },
    {
      "data": {"values": [{"x0": -1, "x1": -1, "y0": 0, "y1": 0.159}]},
      "mark": {"type": "rule", "strokeDash": [4, 4]},
      "encoding": {
        "x": {"field": "x0", "type": "quantitative"},
        "y": {"field": "y0", "type": "quantitative"},
        "y2": {"field": "y1"}
      }
    },
    {
      "data": {"values": [{"x0": -3, "x1": -1, "y0": 0.159, "y1": 0.159}]},
      "mark": {"type": "rule", "strokeDash": [4, 4]},
      "encoding": {
        "x": {"field": "x0", "type": "quantitative"},
        "x2": {"field": "x1"},
        "y": {"field": "y0", "type": "quantitative"}
      }
    }
  ]
}
</code></pre> <hr/> <h3 id="12-joint-marginal-conditional">1.2 Joint, marginal, conditional</h3> <p>Real data often has multiple parts. For example:</p> <ul> <li>An image $ (X) $ and its label $ (Y) $</li> <li>A video frame $ (X_t) $ and the previous frame $ (X_{t-1}) $</li> <li>Text $ (Y) $ describing an image $ (X) $</li> </ul> <p>Probability gives us a consistent way to relate “together” vs “separate” vs “given.”</p> \[p(x,y)=p(x\mid y)\,p(y), \qquad p(x)=\int p(x,y)\,dy \ \text{(continuous)}, \qquad p(x)=\sum_y p(x,y) \ \text{(discrete)}.\] <p>In other words, you can think of these as three basic operations:</p> <p>1) <strong>Joint</strong> $ (p(x,y)) $: “how likely is it to see $ (x) $ and $ (y) $ together?”<br/> Example: “this image AND this caption.”</p> <p>2) <strong>Conditional</strong> $ (p(x\mid y)) $: “how likely is $ (x) $ if I already know $ (y) $?”<br/> Example: “generate an image GIVEN the caption.”</p> <p>3) <strong>Marginal</strong> $ (p(x)) $: “how likely is $ (x) $ overall, if I don’t care about $ (y) $?”<br/> To get this, you <em>remove</em> (marginalize) $ (y) $ by summing or integrating it out.</p> <p>This is exactly where generative modeling becomes hard: marginalization is often expensive or intractable when $ (y) $ is high-dimensional or unobserved. For latent-variable models, $ (y) $ is the latent variable (hidden cause) that you do not see, but you still need to account for it. So “learning a distribution” often becomes: learn a joint model $ (p(x,y)) $ and also learn a practical way to marginalize $ (y) $.</p> <p>This is why later methods appear:</p> <ul> <li>EM is a clever way to deal with hidden discrete variables.</li> <li>VAEs use a variational approximation to handle hidden continuous variables.</li> <li>GANs avoid explicit marginal likelihoods and instead match distributions using a critic.</li> </ul> <hr/> <h3 id="13-expectations-monte-carlo-and-why-mini-batches-work">1.3 Expectations, Monte Carlo, and why mini-batches work</h3> <p>Most learning objectives in ML are written as an <strong>expectation</strong>. An expectation is just the “average value” of something under a distribution.</p> \[\mathbb{E}_{X\sim P}[h(X)] = \int h(x)\,p(x)\,dx \approx \frac{1}{n}\sum_{i=1}^{n} h\!\left(x^{(i)}\right), \quad x^{(i)}\sim P.\] <p>In other words, you can think of this as:</p> <ul> <li>The left side is the “true average” we would like to compute (over all possible images/text/audio that could exist under $ (P) $).</li> <li>The right side is what we can actually compute: an average over a finite dataset (samples we have).</li> </ul> <p>This approximation is called <strong>Monte Carlo estimation</strong>: you replace an impossible integral with a sample average. This is the reason modern ML works: we cannot “sum over all possible images,” but we can average over the images in our dataset.</p> <p><strong>Why mini-batches work.</strong><br/> Mini-batch training is just doing the same sample-average idea, but using a smaller random subset per step. You get a noisy estimate of the expectation, but it is fast and still points in the right direction on average.</p> <p>The i.i.d. assumption matters here because it is what justifies the idea that “averaging over samples approximates averaging over the distribution.” So whenever you see a loss written as $ (\mathbb{E}[\cdot]) $, a good mental translation is: “this is something we will estimate by averaging over the dataset (or a mini-batch).”</p> <p>Once you understand this expectation viewpoint, many generative methods become easier to compare: they differ mainly in (1) which distribution the expectation uses and (2) what function $ (h) $ is being averaged.</p> <hr/> <h2 id="2-what-it-means-to-learn-a-generative-model">2. What it means to learn a generative model</h2> <p>This section answers one question: <em>what are we actually learning when we train a generative model?</em> In other words, you can think of this as: we want to build a new machine that produces data-like samples, and we need a principled way to tune it.</p> <hr/> <h3 id="21-the-modeling-contract">2.1 The modeling contract</h3> <p>A generative model is a <strong>family of probability distributions</strong> $ ({P_\theta}) $ controlled by parameters $ (\theta) $ (for neural networks, these are the weights). This simply means that the architecture stays the same, but different settings of $ (\theta) $ produce different “styles” of generated samples.</p> <p>We assume the dataset comes from an unknown “true” distribution $ (P_{\mathrm{data}}) $, and training means choosing $ (\theta) $ so that the model distribution $ (P_\theta) $ becomes close to $ (P_{\mathrm{data}}) $. Here the dataset is a finite set of clues about a hidden rule, and we are trying to learn a good approximation of that rule.</p> <p>A compact way to write the goal is:</p> \[\theta^\star \in \arg\min_{\theta}\; D\!\left(P_{\mathrm{data}} \,\|\, P_\theta\right).\] <p>Here $ (D(\cdot|\cdot)) $ is a discrepancy (a distance-like measure) between two distributions.<br/> We can think of $ (D) $ as a score that says “how different the outputs of these two machines are, statistically.”</p> <p>This equation is abstract on purpose because it captures the shared structure behind many methods.</p> <p>The entire history of generative modeling can be seen as different choices of:</p> <p>1) the model family $ (P_\theta) $ (what kind of machine we build), and<br/> 2) the discrepancy $ (D) $ (how we decide what “close” means).</p> <p><strong>Practical example (images).</strong><br/> Suppose your dataset contains thousands of cat images. Then $ (P_{\mathrm{data}}) $ is the unknown distribution that produces cat-like images with the same variety of poses, lighting, backgrounds, and styles. Here for simplicity, you can think of $ (P_{\mathrm{data}}) $ as “the cat-image world” that your dataset sampled from.</p> <p>Your model chooses some $ (\theta) $, which defines $ (P_\theta) $, and training tries to make samples from $ (P_\theta) $ look statistically like samples from $ (P_{\mathrm{data}}) $. Training is repeatedly adjusting the knobs until the generated examples match the dataset’s patterns.</p> <blockquote> <p>Important nuance: we never directly observe $ (P_{\mathrm{data}}) $.<br/> We only see samples. So in practice we estimate $ (D) $ from mini-batches of real and generated data.<br/> We compare the machines by comparing many examples from each, not by inspecting their full distributions.</p> </blockquote> <hr/> <h3 id="22-explicit-vs-implicit-models">2.2 Explicit vs implicit models</h3> <p>Generative models differ in a crucial capability: whether they let you <strong>evaluate the likelihood</strong> (the probability mass or density) of a given observation. In other words, you can think of this as: can the model not only generate an image, but also assign a reliable “probability score” to a specific image?</p> <p>A useful classification is:</p> \[\textbf{Explicit model: can evaluate } p_\theta(x)\] \[\qquad \textbf{vs} \qquad\] \[\textbf{Implicit model: can sample } x\sim P_\theta \textbf{ but cannot evaluate } p_\theta(x).\] <p>In an <strong>explicit</strong> model, the likelihood $ (p_\theta(x)) $ is tractable (a PMF for discrete data or a PDF for continuous data). Explicit models are “models with a visible scoring rule” for any sample.</p> <p>This makes training straightforward in principle: you can do maximum likelihood by maximizing $ (\log p_\theta(x)) $ on the dataset. You can think of it as: “push the model to assign higher probability to real data.”</p> <p>Examples include:</p> <ul> <li><strong>Autoregressive models</strong>, which build the joint distribution from many conditional probabilities.</li> <li><strong>Normalizing flows</strong>, which use invertible transformations to compute likelihood exactly.<br/> In other words, you can think of explicit models as great for likelihood-based evaluation, but they may require architectural constraints for tractability.</li> </ul> <p>In an <strong>implicit</strong> model, you can sample from the model but you cannot compute a tractable likelihood $ (p_\theta(x)) $.<br/> Simply, you can think of implicit models as “sample generators without an explicit probability score.”</p> <p>GANs are the classic example: sample noise $ (z\sim P_Z) $, generate $ (x=g_\theta(z)) $, but do not compute $ (p_\theta(x)) $.<br/> A GAN generator is just like a painter: it can create images, but it doesn’t output a probability for a given image.</p> <p>This difference explains two practical consequences:</p> <ul> <li><strong>Evaluation:</strong> explicit models can be compared using log-likelihood; implicit models usually need sample-based metrics.</li> <li><strong>Training:</strong> if you cannot compute $ (p_\theta(x)) $, you cannot do MLE directly, so you need objectives that depend only on samples (like adversarial training).<br/> Implicit models are “trained by comparing sets of samples,” not by scoring individual datapoints with likelihood.</li> </ul> <hr/> <h3 id="23-pushforward-distributions-a-key-modern-idea">2.3 Pushforward distributions (a key modern idea)</h3> <p>A very common modern assumption is: complex data can be generated from a simpler random variable $ (Z) $ through a deterministic mapping. This means we can start with simple noise, then transform it into a structured object like an image.</p> <p>We write:</p> \[Z \sim P_Z, \qquad \widehat{X} = g_\theta(Z), \qquad P_\theta = (g_\theta)_{\#} P_Z.\] <p>Here $ (P_Z) $ is a simple base distribution (often $ (\mathcal{N}(0,I)) $), and $ (g_\theta) $ is a generator network.<br/> In other words, you can think of $ (Z) $ as a random seed, and $ (g_\theta) $ as a learned program that converts that seed into data.</p> <p>The notation \((g_\theta)_{\#}P_Z\) means the <strong>pushforward distribution</strong>: the distribution of $ (\widehat{X}=g_\theta(Z)) $.</p> <p><strong>“Whatever distribution you get after pushing noise through the generator.”</strong></p> <p>This idea is powerful because it keeps sampling easy (sample $ (Z) $) while allowing complex outputs (through $ (g_\theta) $).<br/> In other words, you can think of it as: we avoid writing down a complicated distribution directly, and instead learn a complicated transformation of a simple one.</p> <aside><p> <strong>Side note: what does the “\(\#\)” symbol mean in \( (g_\theta)_{\#}P_Z \)?</strong><br/> The symbol “\(\#\)” here is <em>not</em> a comment marker. It is standard shorthand for the <strong>pushforward distribution</strong> (also called the “image measure”). In other words, you can think of \( (g_\theta)_{\#}P_Z \) as: “take samples \(z\) from \(P_Z\), run them through \(g_\theta\), and look at the distribution of the outputs.” Formally, if we define \( \widehat{X}=g_\theta(Z) \) with \( Z\sim P_Z \), then the induced distribution \(P_\theta\) over outputs satisfies: $$ P_\theta(A)=P(\widehat{X}\in A)=P_Z\!\left(g_\theta^{-1}(A)\right) \quad \text{for any measurable set } A\subseteq\mathcal{X}. $$ In other words, you can think of this as: the probability that the generated sample lands in region \(A\) equals the probability that the latent noise lands in the set of points that map into \(A\). If you prefer more familiar notation, you can avoid “\(\#\)” entirely and write the same idea as: $$ Z\sim P_Z,\quad \widehat{X}=g_\theta(Z),\quad \widehat{X}\sim P_\theta. $$ In other words, you can think of this as: “\(P_\theta\) is simply the distribution of the random output \(\widehat{X}\).” </p></aside> <p><strong>Sanity-check example (1D).</strong><br/> If $ (Z\sim\mathcal{N}(0,1)) $ and $ (g_\theta(z)=a z+b) $, then $ (\widehat{X}\sim\mathcal{N}(b,a^2)) $.<br/> Scaling and shifting Gaussian noise gives another Gaussian, and deep generators do the same thing but in a much richer, nonlinear way.</p> <p>The pushforward view is the backbone behind many families (VAEs, GANs, flows, diffusion). We can think of many “different” generative models as different ways to design and train the mapping from simple randomness to complex data.</p> <hr/> <h2 id="3-classical-density-estimation">3. Classical density estimation</h2> <p>Classical generative modeling starts with a straightforward goal: <strong>write down a probability model for the data</strong> and fit its parameters from samples. This approach is attractive because it gives a clear training signal: if the model assigns high probability to the observed dataset, it is doing something sensible. The main limitation is also clear: if the model family is too simple, the fitted model will be a “best effort” inside that family, even if reality is more complex. This section explains the core ideas with small, concrete examples before we move to richer model families.</p> <hr/> <h3 id="31-maximum-likelihood-estimation-mle">3.1 Maximum likelihood estimation (MLE)</h3> <p>In MLE, you pick a parametric family $ ({p_\theta(x)}) $ and choose parameters $ (\theta) $ that make the observed dataset most likely under the model. Here $ (p_\theta(x)) $ is a <strong>PMF</strong> if $ (x) $ is discrete, and a <strong>PDF</strong> if $ (x) $ is continuous, but the training rule looks the same because we always combine independent samples.</p> \[\widehat{\theta}_{\mathrm{MLE}} = \arg\max_{\theta}\prod_{i=1}^{n} p_\theta\!\left(x^{(i)}\right) = \arg\max_{\theta}\sum_{i=1}^{n}\log p_\theta\!\left(x^{(i)}\right).\] <p>The left form multiplies probabilities/densities across samples because the dataset is modeled as i.i.d. draws. The right form uses logs to turn the product into a sum, which is numerically stable and matches how loss functions are implemented. This objective has two assumptions built in: the true data distribution can be approximated by the chosen family, and “best” means maximizing the probability assigned to the observed data. A practical way to read MLE is: it pushes the model to place high probability mass (discrete) or high density (continuous) in the regions where the dataset lies. If the model family cannot represent the dataset’s structure (for example, fitting one Gaussian to data with multiple clusters), MLE still converges, but to the best approximation <em>within the wrong family</em>. That is why the history of generative modeling keeps revisiting the same pattern: keep the likelihood idea, but make the model family richer.</p> <hr/> <h3 id="32-likelihood-vs-probability">3.2 Likelihood vs probability</h3> <p>A common confusion is to read likelihood as “the probability of parameters.” In frequentist statistics, $ (\theta) $ is treated as a fixed but unknown quantity, and likelihood is a function that scores different values of $ (\theta) $ based on the observed data.</p> \[\mathcal{L}(\theta;\mathcal{D}) = p_\theta(\mathcal{D}) = \prod_{i=1}^{n} p_\theta\!\left(x^{(i)}\right), \qquad \text{but } \mathcal{L}(\theta;\mathcal{D}) \neq p(\theta\mid \mathcal{D}).\] <p>“$ (\mathcal{L}(\theta;\mathcal{D})) $ = the likelihood of parameters $ \theta $ evaluated on the dataset $ \mathcal{D} $ ”.</p> <p>The likelihood function $ (\mathcal{L}(\theta;\mathcal{D})) $ ranks parameter settings: higher likelihood means “this $ (\theta) $ explains the data better under the model assumptions.” It is not a probability distribution over $ (\theta) $ because it does not need to integrate (or sum) to $ (1) $ over all possible $ (\theta) $. A probability like $ (p(\theta\mid \mathcal{D})) $ is a <strong>posterior</strong> from Bayesian inference, which requires a prior and Bayes’ rule. Keeping this distinction clear matters in practice: MLE returns a single best parameter value, while Bayesian inference returns a whole distribution over parameters that represents uncertainty. In deep learning, people often say “likelihood of $ (\theta) $,” but the correct meaning is “likelihood evaluated at $ (\theta) $.” This clarity becomes essential later when you see objectives involving KL divergences and distributions over latent variables.</p> <hr/> <h3 id="33-worked-micro-example-1-1d-gaussian-mle">3.3 Worked micro-example 1: 1D Gaussian MLE</h3> <p>Assume $ (x^{(1)},\ldots,x^{(n)}\in\mathbb{R}) $ were generated from a normal distribution with mean $ (\mu) $ and variance $ (\sigma^2&gt;0) $. The Gaussian PDF is:</p> \[p(x\mid \mu,\sigma^2) = \frac{1}{\sqrt{2\pi\sigma^2}} \exp\!\left(-\frac{(x-\mu)^2}{2\sigma^2}\right).\] <p>This single formula already encodes strong assumptions: the distribution is unimodal, symmetric around $ (\mu) $, and fully described by just two parameters. The normalization constant $ (1/\sqrt{2\pi\sigma^2}) $ ensures the density integrates to $ (1) $, and it depends on $ (\sigma^2) $ because a wider distribution must have a lower peak. The exponent term gives higher density to values near $ (\mu) $ and decreases as $ (x) $ moves away, with the speed controlled by $ (\sigma^2) $. This model is also generative in the most direct sense: draw noise $ (\varepsilon\sim\mathcal{N}(0,1)) $ and output $ (x=\mu+\sigma\varepsilon) $. Gaussian models were historically popular because they allow clean math and easy sampling. They are also limited: a single Gaussian cannot represent multiple clusters, heavy tails, or complex constraints.</p> <p>The log-likelihood of the dataset under this model is:</p> \[\ell(\mu,\sigma^2) = \sum_{i=1}^{n}\log p(x^{(i)}\mid\mu,\sigma^2) = -\frac{n}{2}\log(2\pi\sigma^2) -\frac{1}{2\sigma^2}\sum_{i=1}^{n}\left(x^{(i)}-\mu\right)^2.\] <p>This equation makes MLE feel concrete: maximizing $ (\ell(\mu,\sigma^2)) $ is the same as fitting the center and spread that best explain the samples. The second term is a scaled sum of squared deviations, so choosing $ (\mu) $ is tightly linked to “least squares” thinking. The first term prevents the model from cheating by shrinking $ (\sigma^2) $ toward zero to create an infinitely sharp spike on the data. Most importantly, $ (\ell) $ is not a probability of parameters; it is a score that compares different $ ((\mu,\sigma^2)) $ values. This is the basic template for explicit generative modeling: define $ (p_\theta(x)) $, compute $ (\log p_\theta(x)) $, sum over the dataset, and maximize. Later, when we cannot compute a likelihood exactly, we will replace this step with EM, variational bounds, or adversarial training.</p> <p>Solving $ (\nabla \ell = 0) $ yields the closed-form MLE:</p> \[\widehat{\mu}=\frac{1}{n}\sum_{i=1}^{n}x^{(i)}, \qquad \widehat{\sigma}^2=\frac{1}{n}\sum_{i=1}^{n}\left(x^{(i)}-\widehat{\mu}\right)^2.\] <p>The estimate $ (\widehat{\mu}) $ is the sample mean, matching the intuition that the best Gaussian “center” is the average of observed values. The variance estimate uses denominator $ (n) $ (not $ (n-1) $) because this is MLE, not the classical unbiased estimator. This highlights an important lesson: MLE optimizes likelihood under the assumed model, not unbiasedness under the true unknown distribution. Closed-form solutions like this made early probabilistic modeling elegant and practical. As models become more expressive, analytic solutions disappear, and iterative optimization becomes the norm.</p> <hr/> <h3 id="34-kde-in-one-equation-and-why-it-breaks-in-high-dimensions">3.4 KDE in one equation (and why it breaks in high dimensions)</h3> <p>If you do not want to commit to a specific parametric family, you can estimate a density directly from samples using kernel density estimation (KDE). Given data in $ (\mathbb{R}^d) $, KDE is:</p> \[\widehat{p}_h(x) = \frac{1}{n\,h^d} \sum_{i=1}^{n} K\!\left(\frac{x-x^{(i)}}{h}\right).\] <p>Symbol glossary (KDE).</p> <p>$n$: number of samples in the dataset.</p> <p>$d$: data dimension (e.g., $d=1$ for scalars, $d=H\times W\times 3$ for flattened RGB images).</p> <p>$x\in\mathbb{R}^d$: the point where we want to estimate density.</p> <p>$x^{(i)}$: the $i$-th observed datapoint.</p> <p>$K(\cdot)$: the kernel (a smooth “bump,” often Gaussian), centered at each sample.</p> <p>$h&gt;0$: the bandwidth (smoothing scale).</p> <p>$h^d$: a volume-scaling term that grows/shrinks with dimension.</p> <p>$ \widehat{p}_h(x)$: the KDE estimate of the true (unknown) density $p(x)$.</p> <p>Intuition. KDE places a smooth bump $K(\cdot)$ around every datapoint $x^{(i)}$ and averages those bumps.</p> <p>The bandwidth $h$ controls the tradeoff:</p> <ul> <li> <p>small $h$ → sharp, spiky estimate (can overfit / memorize),</p> </li> <li> <p>large $h$ → very smooth estimate (can wash out structure).</p> </li> </ul> <p>KDE places a smooth “bump” $ (K(\cdot)) $ around each datapoint and averages them. The bandwidth $ (h) $ controls smoothness: small $ (h) $ makes narrow bumps (spiky estimate), large $ (h) $ makes wide bumps (over-smoothed estimate). In one dimension, KDE behaves like a smoothed histogram and is easy to visualize: with enough samples and a sensible $ (h) $, it approximates the true curve well. The problem appears in the factor $ (h^d) $: as dimension $ (d) $ grows, volume grows explosively, and “local neighborhoods” become mostly empty unless the dataset size is enormous. For images, $ (d) $ can be hundreds of thousands, so KDE in raw pixel space is not practical. This failure is useful to remember because it explains why modern methods rely on structure: factorization (autoregressive models), latent spaces (VAEs/GANs), or learned representations. KDE shows the core difficulty in the simplest possible form: density estimation is easy in low dimensions, and extremely hard in high dimensions without additional assumptions.</p> <pre><code class="language-vega_lite">{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "width": 600,
  "height": 260,
  "title": {
    "text": "KDE bandwidth intuition (toy 1D dataset)",
    "anchor": "start",
    "fontSize": 16
  },
  "data": {
    "values": [
      {"x": -2.3}, {"x": -1.8}, {"x": -1.4}, {"x": -0.6}, {"x": -0.2},
      {"x": 0.05}, {"x": 0.25}, {"x": 0.9}, {"x": 1.3}, {"x": 1.8}, {"x": 2.25}
    ]
  },
  "layer": [
    {
      "transform": [
        {
          "density": "x",
          "bandwidth": 0.15,
          "extent": [-3, 3],
          "steps": 200,
          "as": ["x", "density"]
        }
      ],
      "mark": {"type": "line", "tooltip": true},
      "encoding": {
        "x": {"field": "x", "type": "quantitative", "title": "x"},
        "y": {"field": "density", "type": "quantitative", "title": "estimated density  p̂_h(x)"},
        "color": {"datum": "h = 0.15 (spiky)", "type": "nominal", "title": "bandwidth"},
        "tooltip": [
          {"field": "x", "type": "quantitative", "format": ".2f"},
          {"field": "density", "type": "quantitative", "format": ".4f"}
        ]
      }
    },
    {
      "transform": [
        {
          "density": "x",
          "bandwidth": 0.60,
          "extent": [-3, 3],
          "steps": 200,
          "as": ["x", "density"]
        }
      ],
      "mark": {"type": "line", "tooltip": true},
      "encoding": {
        "x": {"field": "x", "type": "quantitative"},
        "y": {"field": "density", "type": "quantitative"},
        "color": {"datum": "h = 0.60 (smooth)", "type": "nominal", "title": "bandwidth"},
        "tooltip": [
          {"field": "x", "type": "quantitative", "format": ".2f"},
          {"field": "density", "type": "quantitative", "format": ".4f"}
        ]
      }
    },
    {
      "mark": {"type": "tick", "thickness": 2, "size": 18},
      "encoding": {
        "x": {"field": "x", "type": "quantitative"},
        "y": {"value": 0}
      }
    }
  ],
  "resolve": {"scale": {"color": "shared"}}
}
</code></pre> <p>In 1D, KDE is easy to visualize and behaves like a smoothed histogram. But in high dimensions, KDE becomes sample-inefficient: the “local neighborhood” around any point occupies a vanishing fraction of space, so you’d need an enormous number of samples to get a stable estimate.</p> <p>The core reason is the factor $h^d$: as $d$ increases, “volume” grows explosively, and local coverage collapses unless $n$ grows exponentially.</p> <pre><code class="language-vega_lite">{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "width": 600,
  "height": 260,
  "title": {
    "text": "Curse of dimensionality: local volume shrinks exponentially",
    "anchor": "start",
    "fontSize": 16
  },
  "data": {
    "sequence": {"start": 1, "stop": 101, "step": 1, "as": "d"}
  },
  "transform": [
    {
      "calculate": "datum.d * log(0.2) / log(10)",
      "as": "log10_fraction"
    }
  ],
  "mark": {"type": "line", "tooltip": true},
  "encoding": {
    "x": {"field": "d", "type": "quantitative", "title": "dimension  d"},
    "y": {"field": "log10_fraction", "type": "quantitative", "title": "log10((0.2)^d)"},
    "tooltip": [
      {"field": "d", "type": "quantitative"},
      {"field": "log10_fraction", "type": "quantitative", "format": ".2f"}
    ]
  }
}

</code></pre> <hr/> <h3 id="35-bayesian-view-in-one-page">3.5 Bayesian view in one page</h3> <p>Bayesian inference treats parameters as random variables and updates beliefs using Bayes’ rule:</p> \[p(\theta\mid \mathcal{D}) \propto p(\mathcal{D}\mid \theta)\,p(\theta).\] <p>This equation combines two ingredients: the likelihood $ (p(\mathcal{D}\mid\theta)) $, which rewards parameters that explain the data, and the prior $ (p(\theta)) $, which encodes preferences or inductive bias before seeing data. The result is the posterior $ (p(\theta\mid\mathcal{D})) $, which expresses uncertainty over parameters after observing the dataset. This perspective connects directly to regularization: many common penalties correspond to log-priors, and maximum a posteriori (MAP) estimation is like MLE with a prior-induced penalty term. In small models with conjugate priors, the posterior can be computed in closed form. In deep networks, exact posteriors are usually intractable, which motivates approximations such as variational inference or sampling-based methods. These Bayesian ideas reappear later in deep generative modeling: VAEs, for example, can be viewed as variational inference applied to latent-variable models with neural networks. Even when full Bayesian inference is not used in practice, the framework is valuable because it forces you to separate what comes from data (likelihood) and what comes from assumptions (prior).</p> <aside><p> <strong>What does “intractable” mean?</strong><br/> In this post, “intractable” means a quantity is mathematically well-defined, but cannot be computed exactly in a reasonable amount of time or memory. This often happens when the computation requires summing or integrating over an enormous space. For example, in latent-variable generative models the marginal likelihood can be written as $$ p_\theta(x)=\int p_\theta(x\mid z)\,p(z)\,dz, $$ but this integral is typically intractable when $ (z) $ is high-dimensional and $ (p_\theta(x\mid z)) $ is a neural network. In practice we replace such intractable computations with approximations such as Monte Carlo estimates, variational bounds (e.g., the ELBO), or objectives that avoid explicit likelihood evaluation. </p></aside> <hr/> <h2 id="4-latent-variable-models-and-em-intuition">4. Latent variable models and EM intuition</h2> <p>Many datasets are not well described by a single “average pattern.” A histogram might show multiple peaks, and a scatter plot might show multiple clusters. In this section, <strong>multi-modal</strong> means <em>multi-peaked / multi-cluster in the statistical sense</em> (one distribution with multiple high-probability regions). This is different from <strong>multimodal data</strong> (multiple sensor modalities like RGB–IR–Depth); here we are talking about the <em>shape</em> of one distribution.</p> <p>A natural way to model multiple clusters is to introduce a hidden variable that tells us “which cluster generated this datapoint.” This hidden variable is called a <strong>latent variable</strong> because it is not directly observed in the dataset. Once a latent variable is part of the model, training needs both: fitting parameters and inferring these hidden assignments.</p> <hr/> <h3 id="41-mixture-models-as-hidden-causes">4.1 Mixture models as hidden causes</h3> <p>To represent multi-cluster data, we assume each observation is generated by a hidden discrete cause $ (Z\in{1,\ldots,K}) $. A <strong>mixture model</strong> defines the marginal distribution of $ (x) $ as:</p> \[p(x)=\sum_{k=1}^{K}\pi_k\,p(x\mid Z=k), \qquad \pi_k\ge 0, \qquad \sum_{k=1}^{K}\pi_k=1.\] <p>This equation describes a simple generative process: pick a component index, then sample from that component. The sum means the overall distribution is a weighted blend of simpler component distributions. It is exactly how you would describe data that comes from several sub-populations mixed together. For example, if a dataset contains two clusters of points on a line, one component can model the left cluster and another can model the right cluster. Because the model blends components, it can represent multiple peaks even when each component is unimodal.</p> <p><strong>Symbol glossary (mixture model).</strong></p> <ul> <li>$ (x) $: one observed datapoint (e.g., one number, one feature vector). Practical meaning: the thing you measured.</li> <li>$ (K) $: number of mixture components. Practical meaning: how many clusters or sub-populations you allow the model to use.</li> <li>$ (Z) $: a discrete latent variable in $ ({1,\ldots,K}) $. Practical meaning: the hidden “cluster label” that says which component generated $ (x) $.</li> <li>$ (\pi_k) $: mixing weight for component $ (k) $, with $ (\sum_k\pi_k=1) $. Practical meaning: how common component $ (k) $ is in the dataset.</li> <li>$ (p(x\mid Z=k)) $: component distribution for cluster $ (k) $. Practical meaning: the shape of cluster $ (k) $ (often a Gaussian in continuous data).</li> </ul> <p>A key point is that $ (\pi_k) $ is a PMF over the discrete variable $ (Z) $ (it sums to 1), while $ (p(x\mid Z=k)) $ is a PDF or PMF depending on whether $ (x) $ is continuous or discrete. This matters because mixing discrete and continuous quantities incorrectly leads to wrong likelihoods and wrong training objectives.</p> <p>The main learning challenge is that $ (Z) $ is hidden. We do not know which component generated which datapoint, so the dataset likelihood contains a sum inside a log:</p> \[\log p(\mathcal{D}) = \sum_{i=1}^{n}\log\left(\sum_{k=1}^{K}\pi_k\,p\!\left(x^{(i)}\mid Z=k\right)\right).\] <p>This expression is the reason mixture learning is harder than single-Gaussian MLE. For each datapoint $ (x^{(i)}) $, the model must consider all components that could have produced it, which creates the inner sum. The outer log makes optimization coupled: you cannot update one component independently without affecting the others. This is where the EM algorithm becomes useful, because it introduces a clean way to handle the hidden assignments.</p> <hr/> <h3 id="42-the-em-idea-via-responsibilities">4.2 The EM idea via responsibilities</h3> <p>EM works by introducing soft assignments of datapoints to components. For each datapoint $ (x^{(i)}) $ and component $ (k) $, the <strong>responsibility</strong> is:</p> \[r_{ik} = p\!\left(Z=k\mid x^{(i)}\right) = \frac{\pi_k\,p\!\left(x^{(i)}\mid Z=k\right)} {\sum_{j=1}^{K}\pi_j\,p\!\left(x^{(i)}\mid Z=j\right)}.\] <p>The responsibility $ (r_{ik}) $ is a probability, so for each fixed $ (i) $ we have $ (r_{ik}\ge 0) $ and $ (\sum_{k=1}^K r_{ik}=1) $. Practically, $ (r_{ik}) $ answers: “given this datapoint, how likely is it that component $ (k) $ generated it?” The numerator combines two intuitive factors: $ (\pi_k) $ (how common component $k$ is overall) and $ (p(x^{(i)}\mid Z=k)) $ (how well component $k$ explains this datapoint). The denominator normalizes across all components so that the result is a valid probability distribution over $ (k) $.</p> <p>EM alternates between two steps:</p> <ul> <li><strong>E-step (Expectation):</strong> compute responsibilities $ (r_{ik}) $ using the current parameters.<br/> Practical meaning: estimate soft cluster membership for each datapoint.</li> <li><strong>M-step (Maximization):</strong> update parameters using responsibilities as fractional counts.<br/> Practical meaning: re-fit each component using weighted data points.</li> </ul> <p>This turns the hard problem “we do not know cluster assignments” into a manageable loop: first estimate assignments probabilistically, then re-fit parameters, and repeat until stable. This “infer latents, then fit parameters” pattern will reappear later in VAEs, where we infer continuous latents with an encoder network.</p> <hr/> <h3 id="43-worked-micro-example-2-one-em-responsibility-update">4.3 Worked micro-example 2: one EM responsibility update</h3> <p>Consider a 1D two-component Gaussian mixture with $ (K=2) $ and equal variances $ (\sigma^2=1) $. Let the current parameters be:</p> <ul> <li>$ (\pi_1=0.6), (\pi_2=0.4) $</li> <li>$ (\mu_1=-1), (\mu_2=2) $</li> </ul> <p>For a datapoint $ (x^{(i)}=0) $, the responsibility of component 1 is:</p> \[r_{i1}= \frac{\pi_1\,\mathcal{N}\!\left(0\mid \mu_1,1\right)} {\pi_1\,\mathcal{N}\!\left(0\mid \mu_1,1\right)+\pi_2\,\mathcal{N}\!\left(0\mid \mu_2,1\right)}, \qquad r_{i2}=1-r_{i1}.\] <p>To compute this, evaluate how much probability density each Gaussian assigns at $ (x=0) $, weight by the mixture proportions, then normalize. Since $ (\mu_1=-1) $ is closer to $ (0) $ than $ (\mu_2=2) $, the first Gaussian assigns higher density at $ (0) $. Numerically, $ (\mathcal{N}(0\mid-1,1)\approx 0.242) $ and $ (\mathcal{N}(0\mid2,1)\approx 0.054) $. After weighting by $ (\pi_1) $ and $ (\pi_2) $, we get $ (0.6\times 0.242 \approx 0.145) $ and $ (0.4\times 0.054 \approx 0.022) $. Normalizing yields $ (r_{i1}\approx 0.87) $ and $ (r_{i2}\approx 0.13) $.</p> <p>So EM treats this datapoint as roughly “87% from component 1 and 13% from component 2.” That is the core intuition: EM does not force a hard yes/no assignment when the datapoint could plausibly belong to multiple clusters.</p> <hr/> <h3 id="44-factor-analysis-intuition-a-vae-ancestor">4.4 Factor analysis intuition (a VAE ancestor)</h3> <p>Mixture models explain data using a discrete hidden label. Factor analysis explains data using a continuous, low-dimensional hidden vector that acts like a compact “cause” behind the observation.</p> <p>A standard factor analysis model is:</p> \[x=\mu+\Lambda z+\varepsilon, \qquad z\sim \mathcal{N}(0,I), \qquad \varepsilon\sim \mathcal{N}(0,\Psi).\] <p><strong>Symbol glossary (factor analysis).</strong></p> <ul> <li>$ (x\in\mathbb{R}^{d}) $: observed datapoint. Practical meaning: a high-dimensional measurement vector.</li> <li>$ (z\in\mathbb{R}^{k}) $ with $ (k\ll d) $: latent factor vector. Practical meaning: a low-dimensional “code” that captures the main reasons samples differ.</li> <li>$ (\Lambda\in\mathbb{R}^{d\times k}) $: loading matrix. Practical meaning: a linear decoder that maps latent factors into the data space.</li> <li>$ (\mu\in\mathbb{R}^{d}) $: mean vector. Practical meaning: baseline average sample.</li> <li>$ (\varepsilon) $: noise term. Practical meaning: leftover variation not explained by the low-dimensional factors.</li> <li>$ (\Psi) $: noise covariance (often diagonal). Practical meaning: per-dimension noise strength, assuming dimensions have independent noise.</li> </ul> <p>The generative story is: sample $ (z\sim\mathcal{N}(0,I)) $, map it to data space via $ (\Lambda z) $, add the mean $ (\mu) $, and then add noise $ (\varepsilon) $. The main assumption is that high-dimensional data varies mostly along a small number of directions (the latent factors), while the rest is treated as noise. This connects to PCA intuitions, but factor analysis is fully probabilistic: it defines a distribution for $ (x) $ rather than just a subspace.</p> <p>Factor analysis is important for the deep generative timeline because it has the same core structure used in VAEs: latent variable $ (\to) $ decoder $ (\to) $ observation. VAEs keep the latent-variable idea but replace the linear decoder $ (\Lambda z) $ with a nonlinear neural decoder $ (g_\theta(z)) $ and replace exact inference with learned approximate inference.</p> <hr/> <h2 id="5-deep-explicit-generative-models">5. Deep explicit generative models</h2> <p>Deep generative modeling became practical once neural networks were used to parameterize probability distributions. In this section we focus on <strong>explicit</strong> generative models: models where you can compute a likelihood $ (p_\theta(x)) $ (a PMF for discrete data or a PDF for continuous data). Having an explicit likelihood gives two major benefits: you can train by maximum likelihood and you can evaluate models using log-likelihood (when the likelihood is meaningful and comparable). The tradeoff is that the model must be designed so that probability calculations remain tractable.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blogs/AI_Image_Timeline_1-480.webp 480w,/assets/img/blogs/AI_Image_Timeline_1-800.webp 800w,/assets/img/blogs/AI_Image_Timeline_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blogs/AI_Image_Timeline_1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Timeline of generative models" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Timeline of generative model families (<a href="https://clay.global/blog/ai-guide" target="_blank" rel="noopener noreferrer">Source</a>) </div> <h3 id="51-autoregressive-modeling-exact-likelihood-slow-sampling">5.1 Autoregressive modeling (exact likelihood, slow sampling)</h3> <p>Autoregressive models use the probability chain rule to write a joint distribution as a product of conditional distributions. This is especially natural for <strong>sequences</strong> such as text (tokens), audio (time samples), and even images (pixels in a chosen scan order).</p> \[p_\theta(x_{1:T}) = \prod_{t=1}^{T} p_\theta(x_t\mid x_{1:t-1}).\] <p><strong>Symbol glossary (autoregressive factorization).</strong></p> <ul> <li>$ (x_{1:T}) $: a sequence of length $ (T) $ (the full object). Example: a sentence as tokens or an audio clip as samples.</li> <li>$ (x_t) $: the element at position $ (t) $. Example: the $t$-th token or the $t$-th audio sample.</li> <li>$ (x_{1:t-1}) $: the prefix (all earlier elements). Example: the already-generated tokens.</li> <li>$ (p_\theta(\cdot)) $: the model distribution parameterized by $ (\theta) $ (neural network weights).</li> <li>$ (T) $: the sequence length.</li> </ul> <p>This equation means: the probability of the whole sequence is obtained by predicting one element at a time, always conditioned on what came before. The modeling choice is the form of each conditional distribution $ (p_\theta(x_t\mid x_{1:t-1})) $. For text, $ (x_t) $ is discrete, so each conditional is typically a categorical PMF over the vocabulary. For audio, $ (x_t) $ can be treated as continuous, so each conditional might be a Gaussian, logistic mixture, or another continuous density.</p> <p>A key advantage is <strong>exact log-likelihood</strong>. Taking logs converts the product into a sum:</p> \[\log p_\theta(x_{1:T}) = \sum_{t=1}^{T}\log p_\theta(x_t\mid x_{1:t-1}).\] <p><strong>Symbol glossary (log-likelihood).</strong></p> <ul> <li>$ (\log p_\theta(x_{1:T})) $: log-likelihood of the entire sequence under the model.</li> <li>$ (\log p_\theta(x_t\mid x_{1:t-1})) $: log-probability (or log-density) of the next element given the prefix.</li> </ul> <p>This decomposition makes training straightforward: maximize the sum of next-step log-probabilities over the dataset. The downside is sampling: to generate one sequence you must sample $ (x_1) $, then sample $ (x_2\mid x_1) $, and so on until $ (x_T) $. If $ (T) $ is very large (long text, high-resolution images, long audio), generation can be slow because it is inherently sequential. This tradeoff is a recurring theme: exact likelihood is convenient for training and evaluation, but it can make sampling expensive.</p> <hr/> <h3 id="52-normalizing-flows-exact-likelihood-invertible-maps">5.2 Normalizing flows (exact likelihood, invertible maps)</h3> <p>Normalizing flows build a complex distribution by transforming a simple base random variable through an <strong>invertible</strong> function. The key benefit of invertibility is that it allows exact density evaluation via the change-of-variables formula.</p> <p>Assume:</p> <ul> <li>$ (z \sim p_Z(z)) $ from a simple base distribution (often standard normal), and</li> <li>$ (x = f_\theta(z)) $ where $ (f_\theta) $ is invertible.</li> </ul> <p>Then the model density is:</p> \[p_\theta(x) = p_Z\!\left(f_\theta^{-1}(x)\right)\, \left|\det\!\left(\frac{\partial f_\theta^{-1}}{\partial x}\right)\right|.\] <p><strong>Symbol glossary (flows and change of variables).</strong></p> <ul> <li>$ (z) $: latent (base) variable. Example: Gaussian noise.</li> <li>$ (p_Z(z)) $: base density over $ (z) $. Common choice: $ (p_Z(z)=\mathcal{N}(0,I)) $.</li> <li>$ (x) $: observed datapoint in data space. Example: an image represented as a vector.</li> <li>$ (f_\theta) $: invertible transformation (a neural network designed to be bijective).</li> <li>$ (f_\theta^{-1}) $: inverse mapping, used to map data back to latent space.</li> <li>$ (\frac{\partial f_\theta^{-1}}{\partial x}) $: Jacobian matrix of the inverse map.</li> <li>$ (\det(\cdot)) $: determinant, measuring how volumes scale under the transformation.</li> </ul> <p>Absolute volume change factor: \(\left|\det\!\left(\frac{\partial f_\theta^{-1}}{\partial x}\right)\right|.\)</p> <p>This equation says: to compute $ (p_\theta(x)) $, map $ (x) $ back to $ (z) $, evaluate the base density there, and correct for how the transformation stretches or compresses space. The determinant term is the price you pay for changing coordinates: if the mapping expands volume in some region, density must decrease accordingly, and vice versa.</p> <p>Flows sit at an interesting point in the timeline. Like GANs, they often start with $ (z\sim\mathcal{N}(0,I)) $ and transform it to data space. Unlike GANs, flows preserve invertibility so that $ (p_\theta(x)) $ remains computable and training can use exact likelihood. The restriction is architectural: $ (f_\theta) $ must be designed so that both $ (f_\theta^{-1}) $ and the Jacobian determinant are tractable.</p> <hr/> <h3 id="53-variational-autoencoders-vaes">5.3 Variational autoencoders (VAEs)</h3> <p>VAEs are deep latent-variable models that keep a probabilistic generative story while using neural networks for the conditional distribution. They combine:</p> <ul> <li>a simple prior $ (p(z)) $ over latent variables, and</li> <li>a neural decoder $ (p_\theta(x\mid z)) $.</li> </ul> <p>The marginal likelihood of a datapoint is:</p> \[p_\theta(x)=\int p_\theta(x\mid z)\,p(z)\,dz, \qquad p(z)=\mathcal{N}(0,I).\] <p><strong>Symbol glossary (VAE generative model).</strong></p> <ul> <li>$ (x) $: observed datapoint. Example: an image, a feature vector, or a short sequence.</li> <li>$ (z) $: continuous latent variable (often low-dimensional). Example: a compact code describing “style” or “content.”</li> <li>$ (p(z)) $: prior distribution over latents (commonly $ (\mathcal{N}(0,I)) $).</li> <li>$ (p_\theta(x\mid z)) $: decoder likelihood (a distribution whose parameters come from a neural network).</li> <li>$ (p_\theta(x)) $: marginal likelihood of data after integrating out $ (z) $.</li> <li>$ (\theta) $: parameters of the decoder network.</li> </ul> <p>The generative story is: sample $ (z\sim p(z)) $, then sample $ (x\sim p_\theta(x\mid z)) $. The integral marginalizes out $ (z) $, which turns the latent-variable story into a distribution over $ (x) $ alone. This makes the model expressive: the decoder can be highly nonlinear, so a simple prior in latent space can produce complex structure in data space.</p> <p>The difficulty is computational: the integral over $ (z) $ is usually intractable for neural decoders in high dimensions. VAEs solve this by introducing an approximate inference model (an encoder) and optimizing a lower bound instead of the exact log-likelihood.</p> <hr/> <h3 id="54-the-elbo-the-vae-training-objective">5.4 The ELBO (the VAE training objective)</h3> <p>VAEs introduce an approximate posterior $ (q_\phi(z\mid x)) $ (an encoder network) to make training feasible. The key result is the evidence lower bound (ELBO):</p> \[\log p_\theta(x) \ge \mathbb{E}_{z\sim q_\phi(z\mid x)}\!\left[\log p_\theta(x\mid z)\right] - \mathrm{KL}\!\left(q_\phi(z\mid x)\,\|\,p(z)\right).\] <p><strong>Symbol glossary (ELBO).</strong></p> <ul> <li>$ (q_\phi(z\mid x)) $: encoder (approximate posterior) distribution over $ (z) $ given $ (x) $.</li> <li>$ (\phi) $: encoder parameters.</li> <li>$ (\log p_\theta(x\mid z)) $: log-likelihood of reconstructing $ (x) $ from $ (z) $ under the decoder.</li> <li>$ (\mathrm{KL}(q|p)) $: Kullback–Leibler divergence, a measure of how different two distributions are.</li> </ul> <p>Where, \(\mathbb{E}_{z\sim q_\phi(z\mid x)}[\cdot]\) is the expectation when $ (z) $ is sampled from the encoder distribution.</p> <p>The left-hand side $ (\log p_\theta(x)) $ is the ideal objective: maximize the log-likelihood of the data. The right-hand side is a computable surrogate that we maximize instead. The first term encourages the decoder to assign high probability to the observed $ (x) $ when $ (z) $ is sampled from the encoder. The second term regularizes the encoder: it discourages $ (q_\phi(z\mid x)) $ from drifting too far from the prior $ (p(z)) $, which makes it meaningful to generate by sampling $ (z\sim p(z)) $ at test time. The direction of the KL matters here: $ (\mathrm{KL}(q_\phi(z\mid x)|p(z))) $ specifically pulls encoder posteriors toward the prior.</p> <p>A helpful way to interpret the ELBO is: it balances two goals—reconstruct data well, while keeping the latent codes structured and sampleable from a simple prior.</p> <hr/> <h3 id="55-reparameterization-trick-making-gradients-work-through-sampling">5.5 Reparameterization trick (making gradients work through sampling)</h3> <p>To optimize the ELBO with gradient descent, we need to backpropagate through samples $ (z\sim q_\phi(z\mid x)) $. For a Gaussian encoder, the reparameterization trick rewrites sampling as a deterministic transformation of noise:</p> \[z=\mu_\phi(x)+\sigma_\phi(x)\odot \varepsilon, \qquad \varepsilon\sim \mathcal{N}(0,I).\] <p><strong>Symbol glossary (reparameterization).</strong></p> <ul> <li>$ (\mu_\phi(x)) $: encoder output for the latent mean.</li> <li>$ (\sigma_\phi(x)) $: encoder output for the latent standard deviation (often elementwise).</li> <li>$ (\varepsilon) $: noise sampled from $ (\mathcal{N}(0,I)) $ (independent of $ (\phi) $).</li> <li>$ (\odot) $: elementwise multiplication.</li> <li>$ (z) $: sampled latent variable used by the decoder.</li> </ul> <p>This identity separates randomness from parameters. All randomness comes from $ (\varepsilon) $, which does not depend on $ (\phi) $. The encoder parameters $ (\phi) $ only control how that noise is shifted and scaled to create $ (z) $. Because $ (z) $ is now a differentiable function of $ (\mu_\phi(x)) $ and $ (\sigma_\phi(x)) $, gradients can flow through the sampling step. This is what makes VAE training practical with standard backpropagation. For non-Gaussian or discrete latent variables, a clean reparameterization is harder, which is why those settings often require alternative gradient estimators.</p> <hr/> <h2 id="6-measuring-distribution-mismatch">6. Measuring distribution mismatch</h2> <p>A generative model is judged by a simple question: <strong>do its samples look like they could have come from the same source as the training data?</strong> Mathematically, that question becomes: <strong>how close is the model distribution $(P_\theta)$ to the data distribution $(P_{\mathrm{data}})$?</strong></p> <p>For explicit models (autoregressive models, flows, many VAEs), we can often compute a likelihood $(p_\theta(x))$ and train by maximum likelihood. For implicit models (GAN-style generators), we can sample $(x\sim P_\theta)$ but we cannot evaluate $(p_\theta(x))$, so we need another way to compare distributions. This is where divergences and discrepancies enter: they translate “distribution matching” into a training objective.</p> <hr/> <h3 id="61-why-we-need-divergences">6.1 Why we need divergences</h3> <p>A useful way to summarize learning is: choose parameters that make the model distribution close to the data distribution under some discrepancy measure.</p> \[\theta^\star \in \arg\min_{\theta}\; D\!\left(P_{\mathrm{data}}\,\|\,P_\theta\right), \qquad \text{where } D \text{ is a divergence or discrepancy.}\] <p><strong>Symbol glossary (distribution mismatch objective).</strong></p> <ul> <li>$ (P_{\mathrm{data}}) $: the true (unknown) data distribution that generated the dataset.</li> <li>$ (P_\theta) $: the model distribution determined by parameters $ (\theta) $.</li> <li>$ (\theta) $: model parameters (e.g., neural network weights).</li> <li>$ (D(\cdot|\cdot)) $: a function that measures how different two distributions are (a divergence or discrepancy).</li> <li>$ (\theta^\star) $: an optimal (or best-found) parameter setting that minimizes the chosen discrepancy.</li> </ul> <p>The main requirement for $ (D) $ in modern ML is <strong>computability</strong>. We want a quantity we can estimate using samples $ (x\sim P_{\mathrm{data}}) $ (real data) and $ (\widehat{x}\sim P_\theta) $ (generated data), because we rarely know $ (P_{\mathrm{data}}) $ explicitly. Different choices of $ (D) $ lead to different learning behavior: some give stable gradients and smooth optimization, while others can fail when the data and model distributions barely overlap (a common situation in high-dimensional spaces). Thinking in terms of divergences also unifies model families: MLE corresponds to minimizing cross-entropy (and hence KL divergence up to constants), VAEs use KL divergence inside the ELBO, and GANs can be viewed as minimizing a discrepancy defined by a learned critic. So divergences act as the bridge between probability theory (“match distributions”) and deep learning practice (“minimize a loss”).</p> <hr/> <h3 id="62-f-divergences-one-definition-many-behaviors">6.2 f-divergences (one definition, many behaviors)</h3> <p>A broad and important family of divergences is the family of <strong>$f$-divergences</strong>. Assume two distributions $ (P) $ and $ (Q) $ have densities (or PMFs) $ (p) $ and $ (q) $ with respect to the same reference measure, and let $ (f) $ be a convex function with $ (f(1)=0). Then the f-divergence is defined as:</p> \[D_f(P\|Q) = \int q(x)\,f\!\left(\frac{p(x)}{q(x)}\right)\,dx.\] <p><strong>Symbol glossary ($f$-divergence).</strong></p> <ul> <li>$ (P, Q) $: two probability distributions we want to compare.</li> <li>$ (p(x), q(x)) $: densities/PMFs of $ (P) $ and $ (Q) $ (PDFs for continuous, PMFs for discrete).</li> <li>$ (f(\cdot)) $: a convex function that sets the “shape” of the divergence.</li> <li>$ (\frac{p(x)}{q(x)}) $: density (or mass) ratio, comparing how likely $ (x) $ is under $ (P) $ vs $ (Q) $.</li> </ul> <p>This definition is powerful because many familiar divergences are special cases for different choices of $ (f) $. The ratio $ (p(x)/q(x)) $ is the core signal: it tells you whether $ (P) $ assigns more mass/density to $ (x) $ than $ (Q) $ does. The choice of $ (f) $ determines how strongly you punish large ratios (places where $ (P) $ is much larger) or tiny ratios (places where $ (Q) $ is much larger).</p> <p>Below are three important examples (written in the standard $f$-divergence form).</p> <p><strong>Example 1: KL divergence (forward KL).</strong><br/> Choosing \(f(u)=u\log u\) gives \(D_f(P\|Q)=D_{\mathrm{KL}}(P\|Q)=\int p(x)\log\frac{p(x)}{q(x)}\,dx.\) This choice heavily penalizes cases where $ (q(x)) $ is tiny in regions where $ (p(x)) $ is large, which often encourages “covering” the data support.</p> <p><strong>Example 2: Jensen–Shannon (JS) divergence.</strong><br/> Let $ (M=\tfrac{1}{2}(P+Q)) $ be the mixture distribution with density $ (m(x)=\tfrac{1}{2}(p(x)+q(x))) $.</p> <p>The JS divergence is: \(D_{\mathrm{JS}}(P\|Q)=\frac{1}{2}D_{\mathrm{KL}}(P\|M)+\frac{1}{2}D_{\mathrm{KL}}(Q\|M).\)</p> <p>JS is also an $f$-divergence; one valid generator function is: \(f(u)=\frac{1}{2}(u\log u-(u+1)\log\!\left(\frac{u+1}{2}\right)).\)</p> <p>JS is symmetric and bounded, and it appears naturally in the original GAN analysis.</p> <p><strong>Example 3: Total variation (TV) distance.</strong><br/> Total variation can be written as an $f$-divergence using: \(f(u)=\frac{1}{2}\lvert u-1\rvert.\)</p> <p>Then \(D_f(P\|Q)=\mathrm{TV}(P,Q)=\frac{1}{2}\int \lvert p(x)-q(x)\rvert\,dx.\)</p> <p>TV measures the largest possible difference in event probabilities between $ (P) $ and $ (Q) $, which makes it very interpretable, but it can be hard to optimize directly in high-dimensional settings.</p> <p>In high-dimensional generative modeling, the behavior of a divergence matters a lot. If a model completely misses a region where the data often appear, some divergences penalize that extremely, while others may give a weak or unstable learning signal. This is one reason different objectives produce different “styles” of failure modes, such as missing modes (low diversity) or overly spread-out samples (blurriness).</p> <p>There is also a practical caveat: the formula above uses densities $ (p) $ and $ (q) $ explicitly, which is not directly usable for implicit generators where we cannot evaluate $ (p_\theta(x)) $. A major idea behind adversarial training is to rewrite such divergences into forms that depend only on expectations over samples, so that learning remains possible without explicit likelihood evaluation.</p> <hr/> <h3 id="63-mode-covering-vs-mode-seeking-why-direction-matters">6.3 Mode covering vs mode seeking (why direction matters)</h3> <p>A divergence does not just tell you “how far” two distributions are; it also influences <em>how</em> the model tries to match the data. A classic example is the KL divergence:</p> \[D_{\mathrm{KL}}(P\|Q)=\int p(x)\log\frac{p(x)}{q(x)}\,dx.\] <p><strong>Symbol glossary (KL divergence).</strong></p> <ul> <li>$ (D_{\mathrm{KL}}(P|Q)) $: KL divergence from $ (P) $ to $ (Q) $ (note the order matters).</li> <li>$ (p(x)) $: density/PMF of $ (P) $.</li> <li>$ (q(x)) $: density/PMF of $ (Q) $.</li> <li>$ (\log\frac{p(x)}{q(x)}) $: log density ratio, showing where the two distributions disagree.</li> </ul> <p>KL is asymmetric: $ (D_{\mathrm{KL}}(P|Q)\neq D_{\mathrm{KL}}(Q|P)) $ in general, and the direction changes the learning pressure. If you minimize $ (D_{\mathrm{KL}}(P_{\mathrm{data}}|P_\theta)) $, the integral is weighted by $ (p_{\mathrm{data}}(x)) $. That means the model is strongly punished when it assigns very small $ (p_\theta(x)) $ in regions where real data frequently occur. This tends to encourage <strong>mode covering</strong>: the model tries to put probability mass wherever the data have mass, even if it becomes somewhat spread out.</p> <p>If you minimize $ (D_{\mathrm{KL}}(P_\theta|P_{\mathrm{data}})) $, the integral is weighted by $ (p_\theta(x)) $. Now the model “cares” mostly about regions where it already puts mass. A model can avoid penalty by not placing mass in certain data regions at all, which can encourage <strong>mode seeking</strong> behavior: focusing on a subset of modes that look very realistic while ignoring others.</p> <p>This helps explain common qualitative differences between generative objectives. Some objectives produce samples that are diverse but less sharp; others produce sharp samples but with reduced diversity. The divergence choice shapes gradients, and gradients shape what the model learns to prioritize. This sets the stage for GANs, where the discriminator/critic effectively defines a discrepancy and therefore strongly influences training dynamics and failure modes.</p> <hr/> <h2 id="7-from-intractable-divergences-to-adversarial-objectives">7. From intractable divergences to adversarial objectives</h2> <p>In the previous section, divergences such as $f$-divergences were defined using density ratios like $ (p(x)/q(x)) $. That definition is mathematically clean, but it creates a problem for <strong>implicit</strong> generative models (GAN-style generators): we can sample from $ (P_\theta) $, but we cannot evaluate its density $ (p_\theta(x)) $. So how can we still “minimize a divergence” if the divergence formula seems to require densities?</p> <p>The key trick is to rewrite divergences into a form that depends only on <strong>expectations over samples</strong>. Convex duality makes that possible. It turns “compute a function of a density ratio” into “optimize over a scoring function,” which is exactly the role played by a discriminator/critic network.</p> <hr/> <h3 id="71-convex-duality-in-one-useful-identity">7.1 Convex duality in one useful identity</h3> <p>Convex duality provides a way to represent a convex function as the best (largest) value among a family of linear functions. For a convex function $ (f) $, its <strong>Fenchel conjugate</strong> $ (f^\ast) $ is defined as:</p> \[f^\ast(t)=\sup_{u\ge 0}\left\{t\,u-f(u)\right\}, \qquad \text{and } \quad f(u)=\sup_{t}\left\{t\,u-f^\ast(t)\right\}.\] <p><strong>Symbol glossary (Fenchel conjugate).</strong></p> <ul> <li>$ (f(u)) $: a convex function defined for $ (u\ge 0) $.</li> <li>$ (u) $: a nonnegative scalar input to $ (f) $ (later it will become a density ratio like $ (p(x)/q(x)) $).</li> <li>$ (t) $: a free scalar variable used in the supremum (later it becomes a score produced by a neural network).</li> <li>$ (f^\ast(t)) $: the Fenchel conjugate of $ (f) $.</li> <li>$ (\sup) $: “supremum,” meaning the least upper bound (think “maximum,” but allowing that the maximum might not be achieved exactly).</li> </ul> <p>This identity says you can compute $ (f(u)) $ indirectly by searching over all $ (t) $ and picking the value that makes $ (t\,u-f^\ast(t)) $ as large as possible. The term $ (t\,u) $ is linear in $ (u) $, while $ (f^\ast(t)) $ acts like a penalty that prevents $ (t) $ from growing without bound. In generative modeling, the important idea is that we will set $ (u) $ to something like a density ratio. Even if we cannot compute that ratio directly, this dual form will let us replace “a function of the ratio” with “an optimization over a function of $x$,” which we <em>can</em> learn from samples.</p> <hr/> <h3 id="72-variational-divergence-minimization-the-sample-based-form">7.2 Variational divergence minimization (the sample-based form)</h3> <p>Start from the $f$-divergence definition for two distributions $ (P) $ and $ (Q) $ with densities (or PMFs) $ (p) $ and $ (q) $:</p> \[D_f(P\|Q) = \int q(x)\,f\!\left(\frac{p(x)}{q(x)}\right)\,dx.\] <p><strong>Symbol glossary (recap).</strong></p> <ul> <li>$ (P, Q) $: two distributions to compare.</li> <li>$ (p(x), q(x)) $: their densities/PMFs.</li> <li>$ (\frac{p(x)}{q(x)}) $: density ratio.</li> <li>$ (D_f(P|Q)) $: the $f$-divergence from $ (P) $ to $ (Q) $.</li> </ul> <p>Now plug the dual form $ (f(u)=\sup_{t}{t\,u-f^\ast(t)}) $ into the divergence (under standard regularity conditions), and the ratio gets absorbed into expectations. You obtain the variational form:</p> \[D_f(P\|Q) = \sup_{T} \left( \mathbb{E}_{x\sim P}[T(x)] - \mathbb{E}_{x\sim Q}\!\left[f^\ast(T(x))\right] \right).\] <p><strong>Symbol glossary (variational form).</strong></p> <ul> <li>$ (T:\mathcal{X}\to\mathbb{R}) $: a function over data space (a “scoring function”).</li> <li>$ (\sup_{T}) $: choose the best scoring function $ (T) $ to maximize the expression.</li> <li>$ (\mathbb{E}_{x\sim P}[\cdot]) $: expectation when $ (x) $ is sampled from $ (P) $.</li> <li>$ (\mathbb{E}_{x\sim Q}[\cdot]) $: expectation when $ (x) $ is sampled from $ (Q) $.</li> <li>$ (f^\ast(\cdot)) $: the conjugate of $ (f) $ from Section 7.1.</li> </ul> <p>This is the turning point: the density ratio is no longer visible. Everything is written as expectations under $ (P) $ and $ (Q) $, which can be approximated using mini-batches of samples. The function $ (T(x)) $ learns to assign high scores to samples typical under $ (P) $ and lower scores (after the $ (f^\ast) $ transformation) to samples typical under $ (Q) $. Because the expression contains a supremum over $ (T) $, learning naturally becomes a maximization problem over a function class. In practice, we choose a neural network $ (T_\psi) $ and maximize over its parameters $ (\psi) $.</p> <hr/> <h3 id="73-the-gan-connection-why-a-minmax-game-appears">7.3 The GAN connection (why a min–max game appears)</h3> <p>Now set $ (P=P_{\mathrm{data}}) $ (real data) and $ (Q=P_\theta) $ (the model distribution). For an implicit generator, $ (P_\theta) $ is defined by sampling $ (z\sim P_Z) $ and mapping it through a generator network:</p> \[z\sim P_Z, \qquad \widehat{x}=g_\theta(z), \qquad \widehat{x}\sim P_\theta.\] <p><strong>Symbol glossary (implicit generator).</strong></p> <ul> <li>$ (z) $: latent noise vector.</li> <li>$ (P_Z) $: base distribution for noise (often $ (\mathcal{N}(0,I)) $).</li> <li>$ (g_\theta) $: generator network mapping latent noise to data space.</li> <li>$ (\widehat{x}) $: a generated (“fake”) sample.</li> <li>$ (P_\theta) $: distribution induced by $ (\widehat{x}=g_\theta(z)) $.</li> </ul> <p>Using the variational form of the divergence and parameterizing the scoring function as a neural network $ (T_\psi) $, we get:</p> \[\min_{\theta}\; D_f\!\left(P_{\mathrm{data}}\|P_\theta\right) = \min_{\theta}\;\max_{\psi} \left( \mathbb{E}_{x\sim P_{\mathrm{data}}}\!\left[T_\psi(x)\right] - \mathbb{E}_{z\sim P_Z}\!\left[f^\ast\!\left(T_\psi(g_\theta(z))\right)\right] \right).\] <p><strong>Symbol glossary (adversarial objective).</strong></p> <ul> <li>$ (\min_{\theta}) $: update the generator parameters to reduce the divergence.</li> <li>$ (\max_{\psi}) $: update the critic/discriminator parameters to maximize separation.</li> <li>$ (T_\psi(x)) $: critic (discriminator-style) network that outputs a real-valued score for a sample $ (x) $.</li> <li>$ (f^\ast(\cdot)) $: conjugate function that determines the specific loss form.</li> <li>$ (x\sim P_{\mathrm{data}}) $: real samples from the dataset.</li> <li>$ (z\sim P_Z) $ and $ (g_\theta(z)) $: generated samples from the model.</li> </ul> <p>This equation is the general “GAN-like” pattern. The critic $ (T_\psi) $ is trained to score real samples and generated samples in a way that maximizes the gap implied by the chosen divergence. The generator $ (g_\theta) $ is trained to produce samples that make this maximized gap small. The crucial practical point is that the objective depends only on samples from $ (P_{\mathrm{data}}) $ and samples produced by $ (g_\theta) $; it never requires evaluating $ (p_\theta(x)) $.</p> <p>The min–max structure appears because the divergence becomes “best possible separation” over functions $ (T) $. That best-separation step is a maximization, and matching distributions becomes the minimization of that maximized value. This also explains training difficulty: the problem is a saddle point (a game), so the two players must remain balanced for gradients to be useful. When the critic becomes too strong too quickly, the generator can receive weak gradients; when the critic is too weak, the training signal becomes noisy and uninformative. So adversarial training is not an arbitrary trick—it is what you get when you turn a divergence that needs densities into a form you can optimize using samples.</p> <hr/> <h2 id="8-gans-as-distribution-matching">8. GANs as distribution matching</h2> <p>GANs (Generative Adversarial Networks) are a turning point in generative modeling because they learn to generate realistic samples <strong>without ever writing down an explicit likelihood</strong> $(p_\theta(x))$. Instead of comparing distributions through likelihoods, GANs compare them through a learned classifier-like network. This makes GANs especially appealing for complex data (images, audio, video) where designing a good likelihood model is hard, but judging realism from examples is easier.</p> <p>Throughout this section:</p> <ul> <li>$(P_{\mathrm{data}})$ is the (unknown) data distribution that produced the dataset.</li> <li>$(P_Z)$ is a simple noise distribution (often Gaussian) used as input to the generator.</li> <li>$(g_\theta)$ is the generator network that maps noise to samples.</li> <li>$(D_\psi)$ (or a critic $(f_\psi)$) is the network that compares real vs generated samples.</li> </ul> <hr/> <h3 id="81-the-original-minimax-game">8.1 The original minimax game</h3> <p>The original GAN formulation (Goodfellow et al., 2014) introduces a discriminator $(D_\psi(x)\in(0,1))$ that outputs the probability that a sample $(x)$ is real rather than generated.</p> \[\min_{\theta}\max_{\psi}\; \mathbb{E}_{x\sim P_{\mathrm{data}}}\!\left[\log D_\psi(x)\right] + \mathbb{E}_{z\sim P_Z}\!\left[\log\!\left(1-D_\psi(g_\theta(z))\right)\right].\] <p><strong>Symbol glossary (original GAN objective).</strong></p> <ul> <li>$(x\sim P_{\mathrm{data}})$: a real datapoint sampled from the dataset distribution.</li> <li>$(z\sim P_Z)$: a noise vector sampled from a simple prior (often $(P_Z=\mathcal{N}(0,I))$).</li> <li>$(g_\theta(z))$: a generated sample (a “fake” datapoint) produced by the generator network.</li> <li>$(\theta)$: generator parameters (weights of $(g_\theta)$).</li> <li>$(D_\psi(x))$: discriminator output, interpreted as “probability that $(x)$ is real.”</li> <li>$(\psi)$: discriminator parameters (weights of $(D_\psi)$).</li> <li>$(\mathbb{E}[\cdot])$: expectation, estimated in practice using mini-batches.</li> </ul> <p>This objective is a two-player game. The discriminator tries to assign high values to real samples ($(D_\psi(x)\approx 1)$) and low values to generated samples ($(D_\psi(g_\theta(z))\approx 0)$). That is exactly the standard binary classification goal: distinguish two classes, real vs fake. The generator tries to produce samples that make the discriminator predict “real,” so it pushes $(D_\psi(g_\theta(z)))$ upward.</p> <p>The key difference from likelihood-based learning is that there is no term like $(p_\theta(x))$ anywhere. The generator is trained only through the discriminator’s feedback signal. That is why GANs are called <strong>implicit</strong> generative models: they define a distribution by a sampling procedure, not by an explicit PDF/PMF that you can evaluate.</p> <hr/> <h3 id="82-optimal-discriminator-and-density-ratios">8.2 Optimal discriminator and density ratios</h3> <p>Even though we cannot compute $(p_\theta(x))$ in practice for an implicit generator, the classic GAN theory explains what happens in an idealized setting. Assume the discriminator has infinite capacity and is optimized perfectly while holding the generator fixed. Then the optimal discriminator has the following closed form:</p> \[D^\ast(x) = \frac{p_{\mathrm{data}}(x)}{p_{\mathrm{data}}(x)+p_\theta(x)}.\] <p><strong>Symbol glossary (optimal discriminator).</strong></p> <ul> <li>$(D^\ast(x))$: the best possible discriminator output at point $(x)$ for the current generator.</li> <li>$(p_{\mathrm{data}}(x))$: the data density/PMF at $(x)$ (conceptual; unknown in practice).</li> <li>$(p_\theta(x))$: the generator’s implied density/PMF at $(x)$ (also conceptual for GANs).</li> <li>The fraction $(\frac{p_{\mathrm{data}}(x)}{p_{\mathrm{data}}(x)+p_\theta(x)})$: the probability that $(x)$ is real under equal prior odds for real vs fake.</li> </ul> <p>This expression says the discriminator is computing a posterior probability: “how likely is $(x)$ to have come from the data distribution rather than the generator?” If the generator matches the data perfectly, then $(p_{\mathrm{data}}(x)=p_\theta(x))$ everywhere, so $(D^\ast(x)=1/2)$ everywhere. That means the discriminator cannot do better than random guessing, which is exactly what you want when the two distributions are indistinguishable.</p> <p>If the generator misses a region where the data has many samples, then $(p_{\mathrm{data}}(x)\gg p_\theta(x))$ there, and the discriminator outputs values near 1 in that region. This is why the discriminator becomes strong when the generator fails to cover the full data distribution. It also hints at a practical issue: if the discriminator becomes extremely accurate early on, it may not provide useful gradients for improving the generator.</p> <p>A closely related identity uses the <strong>logit</strong> (log-odds) of the optimal discriminator:</p> \[\log\frac{D^\ast(x)}{1-D^\ast(x)} = \log\frac{p_{\mathrm{data}}(x)}{p_\theta(x)}.\] <p><strong>Symbol glossary (logit and density ratio).</strong></p> <ul> <li>$(\log\frac{D^\ast(x)}{1-D^\ast(x)})$: log-odds that $(x)$ is real (the discriminator logit).</li> <li>$(\log\frac{p_{\mathrm{data}}(x)}{p_\theta(x)})$: log density ratio between data and model.</li> </ul> <p>This shows the discriminator is a <strong>density-ratio estimator</strong> in the idealized setting. It is not learning “pixel-level rules”; it is learning a function that identifies where the generator distribution differs from the data distribution. This perspective is useful because it connects GANs to divergence estimation and two-sample testing: if you can estimate density ratios, you can compare distributions without explicitly knowing either density.</p> <hr/> <h3 id="83-why-the-generator-loss-can-saturate-and-the-common-fix">8.3 Why the generator loss can saturate (and the common fix)</h3> <p>In the original minimax game, the generator tries to minimize:</p> \[\mathbb{E}_{z\sim P_Z}\!\left[\log\!\left(1-D_\psi(g_\theta(z))\right)\right].\] <p><strong>Symbol glossary (saturation issue).</strong></p> <ul> <li>$(D_\psi(g_\theta(z)))$: discriminator’s “realness” score for generated samples.</li> <li>$(\log(1-\cdot))$: becomes very flat when its input is near 0, which weakens gradients.</li> </ul> <p>If the discriminator is very strong early in training, then $(D_\psi(g_\theta(z))\approx 0)$ for generated samples. In that case, $(\log(1-D_\psi(\cdot)))$ changes slowly, so the generator receives weak gradients. This is often described as the generator loss <strong>saturating</strong>.</p> <p>A common fix is to change only the generator’s objective to the <strong>non-saturating</strong> version:</p> \[\min_{\theta}\; \mathbb{E}_{z\sim P_Z}\!\left[-\log D_\psi(g_\theta(z))\right].\] <p><strong>Symbol glossary (non-saturating generator loss).</strong></p> <ul> <li>$ (-\log D_\psi(g_\theta(z))) $: penalizes the generator when the discriminator assigns low “realness” to fake samples.</li> <li>The negative sign: turns “maximize $(\log D)$” into a minimization objective.</li> </ul> <p>This objective gives stronger gradients when $(D_\psi(g_\theta(z)))$ is small, which often makes early training much more effective. Under the ideal assumptions of the original analysis, both objectives share the same optimum (the distributions match), but they behave very differently during training. This is a practical lesson: in deep generative modeling, two losses can be “equivalent at the solution” yet have very different optimization dynamics.</p> <hr/> <h3 id="84-what-broke-and-what-improved-wasserstein-ideas">8.4 What broke and what improved (Wasserstein ideas)</h3> <p>One influential improvement is the Wasserstein GAN (WGAN). Instead of a discriminator that outputs probabilities in $(0,1)$, WGAN uses a real-valued <strong>critic</strong> $(f_\psi(x))$. The objective becomes:</p> \[\min_{\theta}\max_{\psi\in\mathcal{F}} \left( \mathbb{E}_{x\sim P_{\mathrm{data}}}\!\left[f_\psi(x)\right] - \mathbb{E}_{z\sim P_Z}\!\left[f_\psi(g_\theta(z))\right] \right), \quad \text{where } \mathcal{F} \text{ is the set of 1-Lipschitz functions.}\] <p><strong>Symbol glossary (WGAN objective).</strong></p> <ul> <li>$(f_\psi(x))$: critic score (not a probability), higher for real-like samples, lower for fake-like samples.</li> <li>$(\mathcal{F})$: a restricted function class; in WGAN it is the set of 1-Lipschitz functions.</li> <li>“1-Lipschitz”: a smoothness constraint meaning $ (\lvert f(x)-f(y)\rvert \le \lVert x-y\rVert ) $ for all $(x,y)$.</li> <li>The difference of expectations: measures how well the critic separates real vs generated samples.</li> </ul> <p>The motivation is geometric. In high-dimensional spaces, real data often concentrates near a thin set (informally, a low-dimensional manifold), while early generator samples might lie far away. Some divergences (like JS in the original GAN analysis) can become nearly constant when the supports are far apart, which leads to poor gradients. Wasserstein-inspired objectives can provide a more informative gradient signal even when distributions overlap very little, as long as the critic is constrained appropriately.</p> <p>In practice, enforcing the Lipschitz constraint is difficult, so implementations use approximations such as gradient penalties or spectral normalization. WGAN is not a universal cure, but it illustrates a deeper principle: the choice of discrepancy between distributions is not just a theoretical preference—it directly shapes the gradients the generator receives. That is why GAN research produced many variants: each variant is an attempt to keep the distribution-matching idea while improving optimization stability and sample diversity.</p> <hr/> <h2 id="9-common-confusions">9. Common confusions</h2> <p>This section collects the mistakes that show up again and again when people first learn generative modeling. They are “small” confusions that lead to big errors in notation, objectives, and interpretation.</p> <hr/> <h3 id="confusion-1-a-density-is-a-probability">Confusion 1: “A density is a probability.”</h3> <p>If $(X)$ is continuous (e.g., real-valued audio amplitudes, normalized pixel intensities treated as real numbers), then $(p(x))$ is a <strong>probability density function (PDF)</strong>. A density is not itself a probability.</p> <p>The correct statement is:</p> \[P(X\in A)=\int_A p(x)\,dx.\] <p>Here $(A)$ is a region (an event), and the integral over that region is a probability. For a single exact point $(x)$ in a continuous space, $(P(X=x)=0)$, even if $(p(x))$ is large. That is why a density can be greater than 1: its role is to tell you how probability accumulates locally, not to be a probability itself.</p> <p>A practical mental check: probabilities are unitless and lie in $[0,1]$, while a density has “units” that depend on the dimension of $(x)$. Only after integrating over a region do those units cancel and you get a probability.</p> <hr/> <h3 id="confusion-2-pdf-vs-pmf-and-why-images-are-tricky">Confusion 2: PDF vs PMF (and why images are tricky)</h3> <p>Whether you use a <strong>PMF</strong> or a <strong>PDF</strong> depends on whether the variable is modeled as discrete or continuous.</p> <ul> <li>If pixel values are treated as integers in $ ({0,\ldots,255}) $, then the likelihood is a <strong>probability mass function (PMF)</strong>, and probabilities are sums.</li> <li>If pixel values are treated as real numbers in $ ([0,1]) $, then the likelihood is a <strong>probability density function (PDF)</strong>, and probabilities are integrals.</li> </ul> <p>The two forms look similar in code (both produce a scalar loss), but they are mathematically different objects:</p> \[\text{Discrete: } P(X\in A)=\sum_{x\in A} p(x), \qquad \text{Continuous: } P(X\in A)=\int_A p(x)\,dx.\] <p>Images are “tricky” because the raw sensor data are quantized (discrete), but we often normalize and treat them as continuous for convenience. Both choices can be reasonable, but they lead to different likelihood models and different numerical behavior. For example, modeling pixels as categorical variables gives a PMF that can represent multi-peaked uncertainty at each pixel value. Modeling pixels as Gaussian noise around a real number gives a PDF that behaves more like a smooth regression model. The important rule is consistency: once you choose discrete vs continuous, the rest of the math (likelihood, gradients, and evaluation) must match that choice.</p> <hr/> <h3 id="confusion-3-likelihood-vs-probability">Confusion 3: Likelihood vs probability</h3> <p>The phrase “probability” usually refers to uncertainty about outcomes, like $(P(X=x))$ or $(P(X\in A))$. The phrase “likelihood” refers to how well parameters explain observed data.</p> <p>For i.i.d. data $ (\mathcal{D}={x^{(1)},\ldots,x^{(n)}}) $, the likelihood function is:</p> \[\mathcal{L}(\theta;\mathcal{D}) = p_\theta(\mathcal{D}) = \prod_{i=1}^{n} p_\theta\!\left(x^{(i)}\right).\] <p>Here $(x^{(i)})$ is fixed (observed), and $(\theta)$ is the variable you are scoring. So $ (\mathcal{L}(\theta;\mathcal{D})) $ is not “the probability of $\theta$.” It is a function that ranks parameter values by how well they explain the dataset.</p> <p>“Probability of parameters” only appears in Bayesian modeling, where you define a prior $(p(\theta))$ and compute a posterior:</p> \[p(\theta\mid \mathcal{D}) \propto p(\mathcal{D}\mid\theta)\,p(\theta).\] <p>This is why the same likelihood term can appear in both MLE and Bayesian inference, but the interpretation changes.</p> <hr/> <h3 id="confusion-4-the-direction-of-kl">Confusion 4: The direction of KL</h3> <p>KL divergence is not symmetric:</p> \[\mathrm{KL}(P\|Q)\neq \mathrm{KL}(Q\|P).\] <p>The direction changes what the model gets punished for. Minimizing $ (\mathrm{KL}(P_{\mathrm{data}}|P_\theta)) $ heavily penalizes the model if it assigns tiny probability to regions where data occur often. This pushes the model toward <strong>covering</strong> the data support.</p> <p>Minimizing $ (\mathrm{KL}(P_\theta|P_{\mathrm{data}})) $ mostly penalizes the model for putting mass where data do not occur. This can allow the model to focus on a subset of modes, which can look sharp but reduce diversity.</p> <p>This is one of the cleanest explanations for why different training objectives can lead to “diverse but blurry” vs “sharp but less diverse” samples.</p> <hr/> <h3 id="confusion-5-gans-do-not-have-a-density-so-how-can-we-talk-about-p_thetax">Confusion 5: “GANs do not have a density, so how can we talk about $(p_\theta(x))$?”</h3> <p>A GAN generator defines samples by:</p> \[z\sim P_Z, \qquad \widehat{x}=g_\theta(z), \qquad \widehat{x}\sim P_\theta.\] <p>Even if we cannot write down a closed-form density $(p_\theta(x))$, the sampling procedure still induces a well-defined distribution $(P_\theta)$ over outputs. Many theoretical results about GANs assume that $(P_\theta)$ admits a density $(p_\theta)$ (or is at least absolutely continuous in a suitable sense) so that density ratios and divergences can be written cleanly. Practical GAN training does not use $(p_\theta(x))$ directly—it relies only on samples and gradients through $(g_\theta)$ and the critic.</p> <p>A good way to phrase it: density-based notation is often used for analysis, while sample-based estimation is what makes GANs trainable.</p> <hr/> <h3 id="confusion-6-why-training-can-oscillate">Confusion 6: Why training can oscillate</h3> <p>Most deep learning training is a minimization problem: one set of parameters moves downhill. GAN training is a <strong>saddle-point</strong> problem:</p> \[\min_{\theta}\max_{\psi}\; V(\theta,\psi).\] <p>The generator parameters $(\theta)$ try to reduce the objective, while the discriminator/critic parameters $(\psi)$ try to increase it. This produces game-like dynamics: each side changes the landscape seen by the other. If the critic becomes too strong too quickly, gradients can become uninformative for the generator. If the generator changes too fast, the critic may never learn a stable signal. That is why practical GAN training often uses careful learning-rate choices, update ratios, regularization (gradient penalty, spectral normalization), and architectural constraints.</p> <hr/> <h2 id="10-summary">10. Summary</h2> <ul> <li>A dataset $ (\mathcal{D}) $ gives you samples, and we model them as $ (x^{(i)}\sim P_{\mathrm{data}}) $ so that averages over mini-batches approximate expectations (Monte Carlo learning).</li> <li>A PDF (continuous) and a PMF (discrete) are different objects; probabilities come from integrals or sums over <strong>events</strong>, not from density values at single points.</li> <li>Maximum likelihood estimation fits explicit models by maximizing $ (\sum_i \log p_\theta(x^{(i)})) $, and the solution depends entirely on the modeling assumptions inside $(p_\theta)$.</li> <li>KDE shows why “just estimate the density from samples” breaks in high dimensions: local neighborhoods become empty unless you have enormous data.</li> <li>Latent-variable models introduce hidden causes to represent multi-peaked structure, but learning then requires marginalization and inference (leading to EM-style ideas).</li> <li>Autoregressive models provide exact likelihood via the chain rule $ (p_\theta(x_{1:T})=\prod_t p_\theta(x_t\mid x_{&lt;t})) $, but sampling is sequential and can be slow.</li> <li>Normalizing flows keep exact likelihood by using invertible maps and the change-of-variables formula, trading architectural freedom for tractable Jacobians.</li> <li>VAEs combine neural decoders with latent variables and train using the ELBO, balancing reconstruction likelihood and a KL regularizer toward the prior.</li> <li>Divergences provide a common language for “distribution mismatch,” and different divergences create different gradient behavior and failure modes.</li> <li>Convex duality rewrites $f$-divergences into objectives over a critic function, which naturally produces the adversarial (min–max) structure.</li> <li>GANs match distributions through a learned discriminator/critic: the optimal discriminator estimates a density ratio, and training stability depends on the chosen discrepancy and constraints.</li> </ul> <hr/> <h2 id="references">References</h2> <ul> <li>Fisher, R. A. (1922). <em>On the Mathematical Foundations of Theoretical Statistics</em>. Philosophical Transactions of the Royal Society A.</li> <li>Parzen, E. (1962). <em>On Estimation of a Probability Density Function and Mode</em>. The Annals of Mathematical Statistics.</li> <li>Dempster, A. P., Laird, N. M., &amp; Rubin, D. B. (1977). <em>Maximum Likelihood from Incomplete Data via the EM Algorithm</em>. Journal of the Royal Statistical Society: Series B.</li> <li>Csiszár, I. (1967). <em>Information-type measures of difference of probability distributions and indirect observation</em>. Studia Scientiarum Mathematicarum Hungarica.</li> <li>Rockafellar, R. T. (1970). <em>Convex Analysis</em>. Princeton University Press.</li> <li>Bishop, C. M. (2006). <em>Pattern Recognition and Machine Learning</em>. Springer.</li> <li>Murphy, K. P. (2012). <em>Machine Learning: A Probabilistic Perspective</em>. MIT Press.</li> <li>Kingma, D. P., &amp; Welling, M. (2014). <em>Auto-Encoding Variational Bayes</em>. ICLR (arXiv:1312.6114).</li> <li>Rezende, D. J., Mohamed, S., &amp; Wierstra, D. (2014). <em>Stochastic Backpropagation and Approximate Inference in Deep Generative Models</em>. ICML (arXiv:1401.4082).</li> <li>van den Oord, A., Kalchbrenner, N., &amp; Kavukcuoglu, K. (2016). <em>Pixel Recurrent Neural Networks</em>. ICML (arXiv:1601.06759).</li> <li>Dinh, L., Sohl-Dickstein, J., &amp; Bengio, S. (2017). <em>Density Estimation using Real NVP</em>. ICLR (arXiv:1605.08803).</li> <li>Goodfellow, I., Pouget-Abadie, J., Mirza, M., et al. (2014). <em>Generative Adversarial Nets</em>. NeurIPS (arXiv:1406.2661).</li> <li>Nowozin, S., Cseke, B., &amp; Tomioka, R. (2016). <em>f-GAN: Training Generative Neural Samplers using Variational Divergence Minimization</em>. NeurIPS (arXiv:1606.00709).</li> <li>Arjovsky, M., Chintala, S., &amp; Bottou, L. (2017). <em>Wasserstein GAN</em>. ICML (arXiv:1701.07875).</li> <li>Gulrajani, I., Ahmed, F., Arjovsky, M., Dumoulin, V., &amp; Courville, A. (2017). <em>Improved Training of Wasserstein GANs</em>. NeurIPS (arXiv:1704.00028).</li> <li>Sugiyama, M., Suzuki, T., &amp; Kanamori, T. (2012). <em>Density Ratio Estimation in Machine Learning</em>. Cambridge University Press.</li> </ul> <hr/> <h3 id="stay-connected">Stay connected</h3> <p>Questions, corrections, or ideas? Drop a comment below — I read and respond.</p> <p><a href="https://www.linkedin.com/in/bhardwaj-suraj/">LinkedIn</a> • <a href="https://github.com/SurajBhar">GitHub</a> • <a href="https://surajbhar.github.io/">Website</a></p>]]></content><author><name>Suraj Bhardwaj</name></author><category term="ai-ml"/><summary type="html"><![CDATA[A beginner-friendly, math-grounded journey from probability basics and classical density estimation to deep generative models and GANs.]]></summary></entry><entry><title type="html">The Periodic Table of AI Systems: Predicting “Reactions” from Prompts to Autonomous Agents</title><link href="https://surajbhar.github.io/blog/2026/01/the-periodic-table-of-ai-systems-predicting-reactions-from-prompts-to-autonomous-agents/" rel="alternate" type="text/html" title="The Periodic Table of AI Systems: Predicting “Reactions” from Prompts to Autonomous Agents"/><published>2026-01-08T11:13:08+00:00</published><updated>2026-01-08T11:13:08+00:00</updated><id>https://surajbhar.github.io/blog/2026/01/the-periodic-table-of-ai-systems-predicting-reactions-from-prompts-to-autonomous-agents</id><content type="html" xml:base="https://surajbhar.github.io/blog/2026/01/the-periodic-table-of-ai-systems-predicting-reactions-from-prompts-to-autonomous-agents/"><![CDATA[<p>Most teams still talk about GenAI like it’s one ingredient: “Which model should we use?”</p> <p>But real-world AI products are <strong>systems</strong>. They’re made of components that interact. And once you start thinking in components, a simple idea becomes powerful:</p> <p><strong>A periodic table isn’t just for memorizing elements. It’s for predicting reactions.</strong></p> <p>Recently, Martin Keen from IBM Technology popularized a “periodic table” style sketch for AI systems. I like it because it gives people a shared language: prompts, embeddings, vector DBs, RAG, guardrails, agents, fine-tuning, small models, and more — organized in a way that hints at what combinations are stable, useful, or risky.</p> <p>In this post I’ll do three things:</p> <ul><li>Explain the table in plain English (row by row, family by family)</li><li>Fill the missing blocks in a way that matches the logic of the table</li><li>Give <strong>10 canonical “reactions”</strong> (reference architectures) you can reuse — <strong>each one explicitly using MCP servers</strong> to connect tools and data</li></ul> <h3>Why a “Periodic Table” for AI?</h3> <p>Because most product debates are framed incorrectly.</p> <p>People argue:</p> <ul><li>“RAG vs fine-tuning”</li><li>“Agents are hype”</li><li>“We just need a bigger model”</li><li>“Guardrails are enough”</li></ul> <p>Those arguments miss the point: <strong>systems are recipes</strong>. The right question is:</p> <blockquote><strong><em>What components do we need to combine — under our constraints — to produce a reliable behavior?</em></strong></blockquote> <p>A periodic table helps you:</p> <ul><li>reason about architectures like chemistry (components + interactions)</li><li>identify missing parts (what’s needed for reliability)</li><li>communicate design choices quickly (a shared vocabulary)</li></ul> <h3>The Table at a Glance</h3> <p>The table is organized along two axes:</p> <h3>Rows = maturity / capability layers</h3> <ul><li><strong>Row 1: Primitive</strong> — raw building blocks</li><li><strong>Row 2: Composition</strong> — you can assemble useful apps</li><li><strong>Row 3: Deployment</strong> — production-grade, scalable, safer</li><li><strong>Row 4: Emerging</strong> — fast-moving, not fully standardized yet</li></ul> <h3>Columns (Families) = what role the component plays</h3> <ul><li><strong>Group 1: Reactive</strong> — interaction &amp; action (prompts, function calling, agents)</li><li><strong>Group 2: Retrieval</strong> — memory outside the model (embeddings, vector DBs, synthetic data)</li><li><strong>Group 3: Orchestration</strong> — how components are coordinated (RAG, frameworks, protocols)</li><li><strong>Group 4: Validation</strong> — safety, correctness, testing (guardrails, red teaming, interpretability)</li><li><strong>Group 5: Models</strong> — the engines (LLMs, multimodal, small models, thinking models)</li></ul> <h3>The Filled Periodic Table (Elements)</h3> <p>Below is a clean, “complete” version. If you’re publishing this on Medium, place your table image right here.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*yTKz-0BjFiN0Ew34wKnVzA.png"/><figcaption>AI Systems Periodic Table</figcaption></figure> <h3>Row 1 — Primitive</h3> <p><strong>Pr (Prompts)</strong> — the simplest interface: language in, behavior out<br/><strong>Em (Embeddings)</strong> — numerical meaning representations<br/><strong>Ch (Prompt Chaining / Templates)</strong> — multi-step prompts, reusable templates, decomposition without tools<br/><strong>Sc (Schemas / Output Constraints)</strong> — structured outputs, format constraints, JSON schema, validation rules<br/><strong>Lg (LLM)</strong> — the general-purpose reasoning/generation engine</p> <h3>Row 2 — Composition</h3> <p><strong>Fc (Function Calling)</strong> — the model triggers tools/APIs<br/><strong>Vx (Vector Databases)</strong> — store/query embeddings at scale<br/><strong>Rg (RAG)</strong> — retrieve context and ground generation<br/><strong>Gr (Guardrails)</strong> — runtime checks and safety filters<br/><strong>Mm (Multimodal Models)</strong> — text + images + audio/video inputs</p> <h3>Row 3 — Deployment</h3> <p><strong>Ag (Agent)</strong> — plan → act → observe loop<br/><strong>Ft (Fine-tuning)</strong> — bake specialization into weights<br/><strong>Fw (Frameworks)</strong> — glue code + orchestration (LangChain/LangGraph/AutoGen patterns)<br/><strong>Rt (Red Teaming)</strong> — adversarial testing: jailbreaks, prompt injection, exfiltration simulation<br/><strong>Sm (Small Models)</strong> — fast, cheap, deployable (edge/on-device)</p> <h3>Row 4 — Emerging</h3> <p><strong>Ma (Multi-agent)</strong> — multiple agents collaborating<br/><strong>Sy (Synthetic Data)</strong> — generate training/eval data using AI<br/><strong>MCP (MCP Servers / Tool Protocol Layer)</strong> — standardized tool/data access for agents and apps<br/><strong>In (Interpretability)</strong> — explain why models do what they do<br/><strong>Th (Thinking Models)</strong> — explicit reasoning loops, test-time compute scaling</p> <p><strong>Why MCP belongs here:</strong> it’s becoming the “ports and adapters” layer for AI apps — standardizing how models talk to tools and context. It’s still evolving rapidly, so it fits best as an emerging orchestration element.</p> <h3>What This Table Gets Exactly Right</h3> <h3>1) Most AI products are “Row 2 systems”</h3> <p>If you’ve shipped a chatbot that feels useful, you almost certainly used Row2:</p> <ul><li><strong>RAG (Rg)</strong> for grounding</li><li><strong>Vector DB (Vx)</strong> for memory</li><li><strong>Guardrails (Gr)</strong> for safety</li><li><strong>Function calls (Fc)</strong> for real-time actions</li></ul> <p>Row 2 is where prototypes become products.</p> <h3>2) Autonomy grows along Group 1 (Reactive)</h3> <p>There’s a visible progression:</p> <ul><li><strong>Pr → Fc → Ag → Ma</strong><br/> That’s control → action → autonomy.</li></ul> <h3>3) Reliability grows along Group 4 (Validation)</h3> <p>Most failures in production are not “the model was dumb” but:</p> <ul><li>wrong retrieval</li><li>prompt injection</li><li>data leakage</li><li>tool misuse</li><li>brittle formatting</li></ul> <p>Validation elements are the difference between a demo and something you can trust.</p> <h3>How to “Predict Reactions”</h3> <p>When you combine elements, you get predictable product behaviors.</p> <p>A few examples:</p> <ul><li><strong>Rg + Vx + Em + Lg</strong> → grounded Q&amp;A over documents</li><li><strong>Ag + Fc + MCP</strong> → tool-using assistant that can actually do things</li><li><strong>Gr + Rt</strong> → safer deployment and fewer surprises</li><li><strong>Sm + Vx</strong> → fast, cheap assistants with external memory</li><li><strong>Sy + In</strong> → faster iteration and better understanding of failure modes</li></ul> <p>Now let’s make it concrete.</p> <h3>10 Canonical Reactions (Reference Architectures) — All Using MCP Servers</h3> <p>Each reaction below is written like a reusable recipe:</p> <ul><li><strong>Goal</strong></li><li><strong>Core elements</strong></li><li><strong>How it flows</strong></li><li><strong>Where it breaks</strong></li><li><strong>What to add when scaling</strong></li></ul> <h3>1) Secure Enterprise Documentation Chatbot (Production RAG)</h3> <p><strong>Goal:</strong> answer questions from internal docs without hallucinating or leaking sensitive content.</p> <p><strong>Elements:</strong> Pr + Em + Vx + Rg + Lg + Gr + Rt + <strong>MCP</strong></p> <p><strong>Flow:</strong></p> <ol><li>Ingest docs → chunk → <strong>Em</strong> → store in <strong>Vx</strong></li><li>User question (<strong>Pr</strong>) → embed → retrieve (<strong>Rg</strong>)</li><li>Compose context → answer with <strong>Lg</strong></li><li>Apply <strong>Gr</strong> (PII/policy checks, citation requirements)</li><li>Tools and doc stores accessed via <strong>MCP servers</strong></li><li>Validate using <strong>Rt</strong> (prompt injection + exfil attempts)</li></ol> <p><strong>Common failure:</strong> retrieval returns plausible but wrong chunks<br/><strong>Scale add-ons:</strong> eval sets, retrieval metrics, access controls by role</p> <h3>2) Customer Support Triage Agent (Tickets → Actions)</h3> <p><strong>Goal:</strong> classify tickets, draft replies, escalate, create tasks automatically.</p> <p><strong>Elements:</strong> Pr + Lg + Fc + Ag + Gr + <strong>MCP</strong></p> <p><strong>Flow:</strong></p> <ol><li>Ticket text arrives → agent (<strong>Ag</strong>) plans steps</li><li>Uses <strong>Fc</strong> via <strong>MCP</strong> to query CRM, order status, knowledge base</li><li>Draft response with <strong>Lg</strong></li><li><strong>Gr</strong> checks tone, policy, and PII rules</li><li>Optional human-in-the-loop approval</li></ol> <p><strong>Common failure:</strong> tool misuse (wrong customer record)<br/><strong>Scale add-ons:</strong> strict tool schemas (<strong>Sc</strong>), audit logs, sampling-based QA</p> <h3>3) Agentic Flight Booking Loop (Classic “Think–Act–Observe”)</h3> <p><strong>Goal:</strong> book a flight under constraints (budget, dates, preferences).</p> <p><strong>Elements:</strong> Pr + Ag + Fc + Gr + <strong>MCP</strong> (+ Rt recommended)</p> <p><strong>Flow:</strong></p> <ol><li>User gives constraints (<strong>Pr</strong>)</li><li>Agent decomposes: search → compare → confirm → book</li><li>Each tool call happens via <strong>MCP servers</strong> (flight API, calendar, payment sandbox)</li><li><strong>Gr</strong> enforces “ask before purchase” and safe payment handling</li><li>Red-team (<strong>Rt</strong>) to test injection (“ignore budget, buy premium”) and data exposure</li></ol> <p><strong>Common failure:</strong> agent loops or over-optimizes irrelevant criteria<br/><strong>Scale add-ons:</strong> planner constraints, budget ceilings hard-coded in tools</p> <h3>4) Codebase Copilot (Repo-Aware Coding Assistant)</h3> <p><strong>Goal:</strong> answer questions about your codebase and generate diffs responsibly.</p> <p><strong>Elements:</strong> Em + Vx + Rg + Lg + Sc + Gr + <strong>MCP</strong> (+ Ft optional)</p> <p><strong>Flow:</strong></p> <ol><li>Index repo docs + code → embeddings (<strong>Em</strong>) → <strong>Vx</strong></li><li>Ask “where is X implemented?” → <strong>Rg</strong> retrieves relevant files</li><li>Model answers + proposes patch</li><li><strong>Sc</strong> forces structured output (diff format, file paths)</li><li>Tools (repo search, CI results, ticket context) via <strong>MCP servers</strong></li><li><strong>Gr</strong> blocks secrets in output and risky commands</li></ol> <p><strong>Common failure:</strong> confident wrong refactors<br/><strong>Scale add-ons:</strong> unit-test execution tool, static analysis, gated merges</p> <h3>5) Multimodal Document Q&amp;A (PDFs with Tables + Images)</h3> <p><strong>Goal:</strong> answer questions over scanned PDFs, diagrams, and tables.</p> <p><strong>Elements:</strong> Mm + Em + Vx + Rg + Lg + Gr + <strong>MCP</strong></p> <p><strong>Flow:</strong></p> <ol><li>Extract text + images; multimodal embeddings (<strong>Mm/Em</strong>)</li><li>Store in <strong>Vx</strong> with page-level metadata</li><li><strong>Rg</strong> retrieves relevant passages + visuals</li><li><strong>Mm/Lg</strong> generates grounded answer</li><li>File store + OCR/vision tools accessed via <strong>MCP servers</strong></li><li><strong>Gr</strong> enforces citations and uncertainty when confidence is low</li></ol> <p><strong>Common failure:</strong> table misreads or citation mismatch<br/><strong>Scale add-ons:</strong> page screenshot citation, structured table extraction</p> <h3>6) Analytics Insight Bot (Natural Language → SQL → Explanation)</h3> <p><strong>Goal:</strong> business user asks questions; system runs SQL and explains results.</p> <p><strong>Elements:</strong> Pr + Ag + Fc + Sc + Gr + <strong>MCP</strong> (+ Rt recommended)</p> <p><strong>Flow:</strong></p> <ol><li>User asks: “Why did churn spike last month?”</li><li>Agent plans queries and checks assumptions</li><li>Uses <strong>Fc</strong> via <strong>MCP</strong> to run SQL on warehouse + fetch semantic layer metrics</li><li><strong>Sc</strong> forces output: SQL + result summary + caveats</li><li><strong>Gr</strong> prevents unsafe queries and enforces row limits / privacy rules</li></ol> <p><strong>Common failure:</strong> wrong joins, misleading causal language<br/> <strong>Scale add-ons:</strong> metric definitions store, query linting, counterfactual checks</p> <h3>7) SOC / Security Assistant (Alert Triage + Playbooks)</h3> <p><strong>Goal:</strong> reduce analyst load by summarizing alerts and suggesting next steps safely.</p> <p><strong>Elements:</strong> Rg + Vx + Lg + Ag + Fc + Rt + Gr + <strong>MCP</strong></p> <p><strong>Flow:</strong></p> <ol><li>Retrieve prior incidents/playbooks (<strong>Rg</strong>)</li><li>Agent suggests actions (isolate host, query logs)</li><li>Executes via <strong>MCP servers</strong> (SIEM, EDR, ticketing)</li><li><strong>Gr</strong> ensures safe actions (no destructive steps without approval)</li><li><strong>Rt</strong> stress-tests prompt injection from malicious logs</li></ol> <p><strong>Common failure:</strong> acting on attacker-controlled text<br/><strong>Scale add-ons:</strong> tool allow-lists, human approval gates, sandbox execution</p> <h3>8) On-Device Personal Assistant (Cheap + Private)</h3> <p><strong>Goal:</strong> fast assistant with privacy; uses external memory without cloud dependence.</p> <p><strong>Elements:</strong> Sm + Em + Vx + Rg + Gr + <strong>MCP</strong></p> <p><strong>Flow:</strong></p> <ol><li>Small model (<strong>Sm</strong>) handles interaction locally</li><li>Personal notes indexed with <strong>Em/Vx</strong></li><li><strong>Rg</strong> retrieves relevant context for responses</li><li>Device tools (calendar, reminders) via <strong>MCP servers</strong> (local adapters)</li><li><strong>Gr</strong> enforces privacy rules and prevents data export</li></ol> <p><strong>Common failure:</strong> limited reasoning depth<br/><strong>Scale add-ons:</strong> hybrid routing to larger model for complex tasks (with consent)</p> <h3>9) Synthetic Data Factory for Training + Evaluation</h3> <p><strong>Goal:</strong> generate diverse training/eval sets for hard edge cases.</p> <p><strong>Elements:</strong> Sy + Lg/Mm + In + Gr + <strong>MCP</strong></p> <p><strong>Flow:</strong></p> <ol><li>Define failure modes (e.g., ambiguous queries, OCR noise)</li><li>Generate synthetic examples (<strong>Sy</strong>) with constraints</li><li>Use <strong>In</strong>-style analysis to understand where models fail</li><li>Store datasets and pipelines via <strong>MCP servers</strong></li><li>Guardrails (<strong>Gr</strong>) ensure no sensitive data leaks into synthetic sets</li></ol> <p><strong>Common failure:</strong> synthetic data that’s too “clean”<br/><strong>Scale add-ons:</strong> adversarial generation, noise models, human review sampling</p> <h3>10) Production Reliability Loop (Eval Harness + Guardrails + Red Teaming)</h3> <p><strong>Goal:</strong> continuously improve quality, safety, and robustness.</p> <p><strong>Elements:</strong> Gr + Rt + In + Ch + Sc + <strong>MCP</strong> (+ Th emerging)</p> <p><strong>Flow:</strong></p> <ol><li>Define key behaviors and tests (prompt suites, injection tests)</li><li>Run regressions after prompt/model/retrieval changes</li><li>Use <strong>MCP servers</strong> to pull logs, traces, and evaluation datasets</li><li>Apply interpretability tools (<strong>In</strong>) to investigate failure clusters</li><li>Introduce systematic prompting (<strong>Ch</strong>) and structure (<strong>Sc</strong>) to reduce variance</li><li>Optionally use thinking models (<strong>Th</strong>) where deeper reasoning is worth the latency</li></ol> <p><strong>Common failure:</strong> “we shipped a change and broke everything silently”<br/><strong>Scale add-ons:</strong> automated gates, canary deploys, quality dashboards</p> <h3>How to Choose the Right Reaction</h3> <p>A quick rule of thumb:</p> <ul><li>If you need <strong>grounding</strong> → start with <strong>Rg + Vx + Em</strong></li><li>If you need <strong>actions</strong> → add <strong>Fc</strong>, then <strong>Ag</strong>, then <strong>MCP</strong></li><li>If you need <strong>safety</strong> → add <strong>Gr</strong>, then validate with <strong>Rt</strong></li><li>If you need <strong>speed/cost</strong> → consider <strong>Sm</strong> + retrieval instead of bigger models</li><li>If you need <strong>domain specialization</strong> → consider <strong>Ft</strong>, but keep retrieval anyway</li></ul> <h3>What’s Still Missing (and Why That’s Good)</h3> <p>Like any periodic table, this one will evolve. In practice, teams also need “elements” for:</p> <ul><li>observability (traces, eval metrics)</li><li>governance (access control, PII workflows)</li><li>data engineering (ETL quality, lineage)</li></ul> <p>Those may become future blocks — or they might live as “compounds” that span families.</p> <p>That’s the fun part: <strong>a periodic table is a living map of a field.</strong></p> <h3>Closing Thought</h3> <p>If you take one idea from this post, take this:</p> <blockquote><strong><em>Stop debating single ingredients. Start designing reactions.</em></strong></blockquote> <p>Once your team can point at components and predict outcomes, your architecture discussions become clearer, faster, and far more practical.</p> <p>Follow me on <a href="https://www.linkedin.com/in/bhardwaj-suraj/"><strong>LinkedIn</strong></a>, explore my work on <a href="https://github.com/SurajBhar"><strong>GitHub</strong></a>, and know more about me on my <a href="https://surajbhar.github.io/"><strong>portfolio</strong></a>.</p> <p>Watch the original video from IBM Technology by Martin Keen:</p> <p>“<a href="https://www.youtube.com/watch?v=ESBMgZHzfG0"><strong>AI Periodic Table Explained: Mapping LLMs, RAG &amp; AI Agent Frameworks</strong></a><strong>”</strong></p> <p><strong>References:</strong> <a href="https://www.youtube.com/watch?v=ESBMgZHzfG0">https://www.youtube.com/watch?v=ESBMgZHzfG0</a></p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=abd2cccadfe3" width="1" height="1" alt=""/></p>]]></content><author><name></name></author></entry><entry><title type="html">AI Systems Periodic Table</title><link href="https://surajbhar.github.io/blog/2026/01/ai-periodic-table/" rel="alternate" type="text/html" title="AI Systems Periodic Table"/><published>2026-01-08T09:00:00+00:00</published><updated>2026-01-08T09:00:00+00:00</updated><id>https://surajbhar.github.io/blog/2026/01/ai-periodic-table</id><content type="html" xml:base="https://surajbhar.github.io/blog/2026/01/ai-periodic-table/"><![CDATA[<p><strong>Connect:</strong> <a href="https://www.linkedin.com/in/bhardwaj-suraj/">LinkedIn</a> • <a href="https://github.com/SurajBhar">GitHub</a> • <a href="https://surajbhar.github.io/">Website</a></p> <p><strong>Inspiration / credit:</strong> This work is inspired by the IBM-style “AI Periodic Table” explained by Martin Keen (IBM). <a href="https://www.youtube.com/watch?v=ESBMgZHzfG0">Video:</a></p> <blockquote> <p><strong>TL;DR</strong><br/> Real-world AI products are not “just an LLM.” They are <strong>systems</strong> assembled from reusable components—like chemistry. A periodic table is valuable not for memorization, but for <strong>predicting reactions</strong>: which parts combine well, where failures happen, and what validation makes systems trustworthy.</p> </blockquote> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blogs/ai_periodic_table-480.webp 480w,/assets/img/blogs/ai_periodic_table-800.webp 800w,/assets/img/blogs/ai_periodic_table-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blogs/ai_periodic_table.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="AI Systems Periodic Table" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> AI Systems Periodic Table </div> <hr/> <h2 id="why-a-periodic-table-for-ai-systems">Why a “Periodic Table” for AI Systems?</h2> <p>AI discussions often reduce everything to a single ingredient: <em>the model</em>.<br/> But production systems need more than raw generation capability:</p> <ul> <li><strong>Grounding</strong> (so answers map to evidence)</li> <li><strong>Memory</strong> (so the system “knows” your domain)</li> <li><strong>Tool use</strong> (so it can fetch real-time data or take actions)</li> <li><strong>Safety and governance</strong> (so it doesn’t leak secrets or do unsafe actions)</li> <li><strong>Reliability</strong> (so you can ship, monitor, and improve)</li> </ul> <p>This periodic-table framing gives teams a shared language to design systems as <strong>compositions</strong> of components—with predictable interactions.</p> <hr/> <h2 id="how-to-read-the-table">How to Read the Table</h2> <h3 id="columns-families--groups">Columns (Families / Groups)</h3> <ul> <li><strong>Group 1 — Reactive:</strong> Components that translate intent into behavior (prompts → tools → agents → multi-agent).</li> <li><strong>Group 2 — Retrieval:</strong> Components that provide memory and knowledge access (embeddings, vector search, RAG, fine-tuning as weight-memory, synthetic data).</li> <li><strong>Group 3 — Orchestration:</strong> Components that coordinate steps and connect capabilities (chains, RAG pipelines, frameworks, interoperability like MCP).</li> <li><strong>Group 4 — Validation:</strong> Components that reduce risk and increase trust (schemas, guardrails, red teaming, interpretability).</li> <li><strong>Group 5 — Models:</strong> The engines (LLMs, multimodal models, small models, thinking models).</li> </ul> <h3 id="rows-maturity">Rows (Maturity)</h3> <ul> <li><strong>Row 1 — Primitive:</strong> Core building blocks (prompting, embeddings, chaining, constraints, base models).</li> <li><strong>Row 2 — Composition:</strong> Common “product recipes” (function calling, vector DB, RAG, guardrails, multimodal).</li> <li><strong>Row 3 — Deployment:</strong> What you need to ship and operate reliably (agents, fine-tuning, frameworks, red teaming, small models).</li> <li><strong>Row 4 — Emerging:</strong> Rapidly evolving capabilities (multi-agent, synthetic data at scale, MCP, interpretability, thinking models).</li> </ul> <h3 id="the-key-idea--predict-reactions">The Key Idea- Predict Reactions</h3> <p>A practical system is a <strong>reaction</strong> that combines elements across columns (often across several rows).<br/> The table helps you ask: <em>Which ingredients do we need to achieve a specific product outcome under constraints (latency, cost, security, compliance)?</em></p> <hr/> <h2 id="the-ai-systems-periodic-table">The AI Systems Periodic Table</h2> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blogs/ai_periodic_table-480.webp 480w,/assets/img/blogs/ai_periodic_table-800.webp 800w,/assets/img/blogs/ai_periodic_table-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/blogs/ai_periodic_table.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="AI Systems Periodic Table" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> AI Systems Periodic Table </div> <h3 id="element-map-symbols-used-in-reaction-formulas">Element Map (Symbols Used in “Reaction Formulas”)</h3> <table> <thead> <tr> <th>Row</th> <th>Group 1 (Reactive)</th> <th>Group 2 (Retrieval)</th> <th>Group 3 (Orchestration)</th> <th>Group 4 (Validation)</th> <th>Group 5 (Models)</th> </tr> </thead> <tbody> <tr> <td><strong>Row 1 — Primitive</strong></td> <td><strong>Pr</strong> Prompts</td> <td><strong>Em</strong> Embeddings</td> <td><strong>Ch</strong> Prompt Chaining</td> <td><strong>Sc</strong> Schemas &amp; Constraints</td> <td><strong>Lg</strong> LLMs</td> </tr> <tr> <td><strong>Row 2 — Composition</strong></td> <td><strong>Fc</strong> Function Calling</td> <td><strong>Vx</strong> Vector Databases</td> <td><strong>Rg</strong> RAG</td> <td><strong>Gr</strong> Guardrails</td> <td><strong>Mm</strong> Multimodal Models</td> </tr> <tr> <td><strong>Row 3 — Deployment</strong></td> <td><strong>Ag</strong> Agents</td> <td><strong>Ft</strong> Fine-tuning</td> <td><strong>Fw</strong> Frameworks</td> <td><strong>Rt</strong> Red Teaming</td> <td><strong>Sm</strong> Small Models</td> </tr> <tr> <td><strong>Row 4 — Emerging</strong></td> <td><strong>Ma</strong> Multi-agent Systems</td> <td><strong>Sy</strong> Synthetic Data</td> <td><strong>MCP</strong> MCP Servers &amp; Protocols</td> <td><strong>In</strong> Interpretability</td> <td><strong>Th</strong> Thinking Models</td> </tr> </tbody> </table> <p><strong>Filled blocks in this version:</strong></p> <ul> <li><strong>Ch (Prompt Chaining)</strong> — multi-step templates that reduce brittleness vs a single prompt</li> <li><strong>Sc (Schemas &amp; Constraints)</strong> — structured outputs and validation for reliability</li> <li><strong>MCP (MCP Servers &amp; Protocols)</strong> — a standard interface for tools and data sources</li> </ul> <hr/> <h1 id="detailed-explanation-element-catalog">Detailed Explanation (Element Catalog)</h1> <p>Each element includes: what it is, when to use it, typical risks, and practical notes.<br/> In real systems, elements are usually <strong>combined</strong> rather than used alone.</p> <h2 id="row-1--primitive">Row 1 — Primitive</h2> <h3 id="pr--prompts">Pr — Prompts</h3> <p><strong>What:</strong> Natural language instructions that steer model behavior.<br/> <strong>Use when:</strong> You need a fast prototype or a controllable behavior layer.<br/> <strong>Risks:</strong> Prompt injection, brittle phrasing, hidden assumptions.<br/> <strong>Practical notes:</strong> Treat prompts as <strong>versioned artifacts</strong> with review + regression tests.</p> <h3 id="em--embeddings">Em — Embeddings</h3> <p><strong>What:</strong> Vector representations capturing semantic similarity.<br/> <strong>Use when:</strong> You need search, clustering, deduplication, or retrieval.<br/> <strong>Risks:</strong> Domain mismatch, privacy leakage if embeddings are exposed.<br/> <strong>Practical notes:</strong> Choose embedding models that match your <strong>language + domain</strong>, and apply access control.</p> <h3 id="ch--prompt-chaining-templates">Ch — Prompt Chaining (Templates)</h3> <p><strong>What:</strong> Multi-step prompting (decompose → draft → critique → refine) and reusable templates.<br/> <strong>Use when:</strong> You want more reliable results than a single prompt.<br/> <strong>Risks:</strong> Latency, compounding errors, debugging complexity.<br/> <strong>Practical notes:</strong> Keep chains short, add checkpoints, and prefer <strong>structured outputs</strong> between steps.</p> <h3 id="sc--schemas--constraints">Sc — Schemas &amp; Constraints</h3> <p><strong>What:</strong> Enforcing output formats (e.g., JSON schema), allowed values, and validation checks.<br/> <strong>Use when:</strong> Downstream systems require reliable structure.<br/> <strong>Risks:</strong> Over-constraint can hurt answer quality; schema drift.<br/> <strong>Practical notes:</strong> Fail fast, return actionable validation errors, and log violations.</p> <h3 id="lg--llms">Lg — LLMs</h3> <p><strong>What:</strong> General-purpose language models for generation, reasoning, extraction.<br/> <strong>Use when:</strong> You need broad language capability.<br/> <strong>Risks:</strong> Hallucinations, bias, prompt sensitivity.<br/> <strong>Practical notes:</strong> For high-stakes use, pair with <strong>retrieval</strong>, <strong>constraints</strong>, and <strong>evaluation</strong>.</p> <hr/> <h2 id="row-2--composition">Row 2 — Composition</h2> <h3 id="fc--function-calling">Fc — Function Calling</h3> <p><strong>What:</strong> A controlled interface for models/agents to request tool execution.<br/> <strong>Use when:</strong> You need real-time data, actions, or computation.<br/> <strong>Risks:</strong> Tool misuse, insecure parameter passing, infinite tool loops.<br/> <strong>Practical notes:</strong> Validate arguments, sandbox tools, rate-limit calls, and log everything.</p> <h3 id="vx--vector-databases">Vx — Vector Databases</h3> <p><strong>What:</strong> Systems optimized to store/search embeddings at scale.<br/> <strong>Use when:</strong> You need fast semantic retrieval over large corpora.<br/> <strong>Risks:</strong> Stale indexes, poor chunking, access control mistakes.<br/> <strong>Practical notes:</strong> Use document-level ACLs, monitor retrieval hit-rate and relevance.</p> <h3 id="rg--rag-retrieval-augmented-generation">Rg — RAG (Retrieval-Augmented Generation)</h3> <p><strong>What:</strong> Retrieve relevant context, then generate grounded outputs.<br/> <strong>Use when:</strong> Knowledge changes often, or you need citations/traceability.<br/> <strong>Risks:</strong> Wrong retrieval → confident wrong answers; leakage of sensitive docs.<br/> <strong>Practical notes:</strong> Evaluate retrieval separately (recall/precision) and generation (faithfulness).</p> <h3 id="gr--guardrails">Gr — Guardrails</h3> <p><strong>What:</strong> Runtime policies and safety controls (filters, PII redaction, topic restrictions, refusal rules).<br/> <strong>Use when:</strong> You must enforce security, privacy, or compliance.<br/> <strong>Risks:</strong> Over-blocking harms UX; under-blocking increases risk.<br/> <strong>Practical notes:</strong> Combine with RBAC + audit logs; keep policy rules testable.</p> <h3 id="mm--multimodal-models">Mm — Multimodal Models</h3> <p><strong>What:</strong> Models that understand/generate across text + images/audio/video.<br/> <strong>Use when:</strong> Your task needs visual or audio context (docs, screenshots, videos).<br/> <strong>Risks:</strong> Sensitive content in media; higher cost/latency.<br/> <strong>Practical notes:</strong> Add modality-specific preprocessing and redaction.</p> <hr/> <h2 id="row-3--deployment">Row 3 — Deployment</h2> <h3 id="ag--agents">Ag — Agents</h3> <p><strong>What:</strong> Systems that plan, act via tools, and observe outcomes in a loop.<br/> <strong>Use when:</strong> Tasks require multi-step operations with external systems.<br/> <strong>Risks:</strong> Goal drift, infinite loops, unintended actions.<br/> <strong>Practical notes:</strong> Add budgets, step limits, approval gates, and strong logging.</p> <h3 id="ft--fine-tuning">Ft — Fine-tuning</h3> <p><strong>What:</strong> Adapting a base model using supervised or preference optimization.<br/> <strong>Use when:</strong> You need stable domain behavior, style, or specialized skill.<br/> <strong>Risks:</strong> Data leakage, governance complexity, catastrophic forgetting.<br/> <strong>Practical notes:</strong> Prefer adapters (e.g., LoRA), curate data, keep it auditable.</p> <h3 id="fw--frameworks">Fw — Frameworks</h3> <p><strong>What:</strong> Libraries to build chains/graphs/agents/integrations (e.g., orchestration frameworks).<br/> <strong>Use when:</strong> You want faster assembly and reusable patterns.<br/> <strong>Risks:</strong> Abstraction leaks, debugging complexity, lock-in.<br/> <strong>Practical notes:</strong> Keep core logic portable; test boundaries where tools connect.</p> <h3 id="rt--red-teaming">Rt — Red Teaming</h3> <p><strong>What:</strong> Adversarial testing (jailbreaks, prompt injection, data exfiltration simulation).<br/> <strong>Use when:</strong> You deploy to untrusted users or handle sensitive data.<br/> <strong>Risks:</strong> False confidence if test set is narrow.<br/> <strong>Practical notes:</strong> Maintain an attack library and rerun after every major change.</p> <h3 id="sm--small-models">Sm — Small Models</h3> <p><strong>What:</strong> Distilled/specialized models optimized for cost and latency.<br/> <strong>Use when:</strong> Edge/on-device or high-throughput settings.<br/> <strong>Risks:</strong> Capability gaps; may require better retrieval.<br/> <strong>Practical notes:</strong> Use a cascade: small model first, escalate to larger models if needed.</p> <hr/> <h2 id="row-4--emerging">Row 4 — Emerging</h2> <h3 id="ma--multi-agent-systems">Ma — Multi-agent Systems</h3> <p><strong>What:</strong> Multiple agents with roles (planner, executor, critic) collaborating.<br/> <strong>Use when:</strong> Decomposition + cross-checking improves outcomes.<br/> <strong>Risks:</strong> Coordination overhead; conflicting goals.<br/> <strong>Practical notes:</strong> Define roles clearly and enforce shared-memory boundaries.</p> <h3 id="sy--synthetic-data">Sy — Synthetic Data</h3> <p><strong>What:</strong> Generated data to expand training/eval coverage.<br/> <strong>Use when:</strong> Real data is scarce, expensive, or privacy-restricted.<br/> <strong>Risks:</strong> Distribution shift, bias reinforcement.<br/> <strong>Practical notes:</strong> Validate with human review and real-world holdout benchmarks.</p> <h3 id="mcp--mcp-servers--protocols">MCP — MCP Servers &amp; Protocols</h3> <p><strong>What:</strong> Model Context Protocol (MCP) servers expose tools and data sources through a standard interface.<br/> <strong>Use when:</strong> You want one integration pattern across many tools (files, databases, SaaS APIs).<br/> <strong>Risks:</strong> Over-permissioned tool access; missing audit trails.<br/> <strong>Practical notes:</strong> Treat MCP servers like production services: auth, RBAC, logging, rate limits.</p> <h3 id="in--interpretability">In — Interpretability</h3> <p><strong>What:</strong> Techniques to understand and explain model decisions.<br/> <strong>Use when:</strong> Safety, debugging, or compliance needs transparency.<br/> <strong>Risks:</strong> Explanations can mislead if not validated.<br/> <strong>Practical notes:</strong> Combine with counterfactual tests and targeted probes.</p> <h3 id="th--thinking-models">Th — Thinking Models</h3> <p><strong>What:</strong> Models that allocate extra compute to reasoning (deliberation) to improve correctness.<br/> <strong>Use when:</strong> Tasks are complex and errors are costly.<br/> <strong>Risks:</strong> Higher latency and cost.<br/> <strong>Practical notes:</strong> Use selective routing: invoke Th only when confidence is low.</p> <hr/> <h1 id="10-canonical-reactions-reference-architectures">10 Canonical Reactions (Reference Architectures)</h1> <p>A <strong>canonical reaction</strong> is a reusable architecture pattern written as a formula.<br/> Each pattern below includes <strong>MCP servers</strong> as a standard interface for tools and data sources.</p> <hr/> <h2 id="reaction-1--secure-documentation-chatbot-production-rag">Reaction 1 — Secure Documentation Chatbot (Production RAG)</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Em + Vx + Rg + Sc + Gr + MCP + Lg (+ Rt)</code></p> <p><strong>Use case:</strong> Answer questions using internal documentation with citations and access control.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Confluence/SharePoint MCP (read-only doc fetch)</li> <li>Jira MCP (tickets, runbooks, incidents)</li> <li>File storage MCP (PDFs, SOPs, policies)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Ingest docs → chunk → compute embeddings (<strong>Em</strong>) → store in <strong>Vx</strong> with document-level ACLs</li> <li>Query: validate user permissions; build structured requests (<strong>Pr + Sc</strong>)</li> <li>Retrieve top-k chunks (<strong>Rg</strong>) with citations + metadata</li> <li>Generate grounded answer (<strong>Lg</strong>) with structured response (answer + citations + confidence)</li> <li>Apply <strong>Gr</strong> (PII/secret redaction, refusal rules)</li> <li>Log everything for audit; run <strong>Rt</strong> regularly (injection + exfil tests)</li> </ol> <p><strong>Where it breaks:</strong> wrong retrieval causes confident wrong answers<br/> <strong>Scale note:</strong> evaluate retrieval and generation separately; monitor hit-rate and faithfulness</p> <hr/> <h2 id="reaction-2--agentic-travel-booking-thinkactobserve">Reaction 2 — Agentic Travel Booking (Think–Act–Observe)</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Fc + Ag + Fw + MCP + Sc + Gr + Lg (+ Th)</code></p> <p><strong>Use case:</strong> Book a flight under constraints (budget, dates) with approvals and safe tool execution.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Flights API MCP (search/pricing)</li> <li>Calendar MCP (availability)</li> <li>Email/Messaging MCP (send itinerary, approvals)</li> <li>Payments MCP (optional, behind explicit user approval gate)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Restate constraints and ask clarifying questions (templates via <strong>Pr</strong>)</li> <li>Plan steps (<strong>Ag</strong>) and call tools via <strong>Fc</strong> through <strong>MCP</strong></li> <li>Iterate: observe results, compare options; maintain structured state (<strong>Sc</strong>)</li> <li>Request explicit confirmation before booking</li> <li>Execute booking via MCP and send confirmation via email MCP</li> <li>Apply <strong>Gr</strong>: spending limits, safe actions, redact sensitive info</li> </ol> <p><strong>Where it breaks:</strong> loops / goal drift / unsafe actions without gating<br/> <strong>Scale note:</strong> budgets, step limits, timeouts, and approvals are non-negotiable</p> <hr/> <h2 id="reaction-3--sql-analytics-copilot-over-a-data-warehouse">Reaction 3 — SQL Analytics Copilot over a Data Warehouse</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Em + Vx + Rg + Fc + MCP + Sc + Gr + Lg</code></p> <p><strong>Use case:</strong> Generate safe SQL, run it, and summarize results with definitions + caveats.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Warehouse MCP (Postgres/Snowflake/BigQuery/etc.)</li> <li>Data catalog MCP (schemas, KPI glossary)</li> <li>BI tool MCP (optional: publish charts)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Retrieve schema + KPI definitions via <strong>Rg</strong></li> <li>Draft SQL with <strong>Sc</strong> constraints (read-only, row limits, no PII)</li> <li>Execute SQL via <strong>Fc</strong> through warehouse MCP; fetch results</li> <li>Summarize insights + assumptions; include SQL + preview</li> <li>Apply <strong>Gr</strong>: privacy rules, safe-query enforcement</li> </ol> <p><strong>Where it breaks:</strong> wrong joins, misleading causal language<br/> <strong>Scale note:</strong> add query linting, semantic layer, and golden-test questions</p> <hr/> <h2 id="reaction-4--codebase-assistant-with-github-pr-aware">Reaction 4 — Codebase Assistant with GitHub (PR-aware)</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Em + Vx + Rg + Fc + Ag + MCP + Sc + Gr (+ Ft)</code></p> <p><strong>Use case:</strong> Understand a repo, propose changes, run tests, open PRs safely.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>GitHub MCP (read/write repo, issues, PRs)</li> <li>CI logs MCP (build/test logs)</li> <li>Security scanner MCP (optional)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Index repo docs/code into <strong>Vx</strong></li> <li>Retrieve context via <strong>Rg</strong></li> <li>Produce change plan with <strong>Sc</strong> (files, edits, tests)</li> <li>Apply edits via MCP; run CI via MCP; summarize results</li> <li>Open PR with checklist; enforce <strong>Gr</strong> (no secrets, safe actions)</li> </ol> <p><strong>Where it breaks:</strong> prompt injection hidden in README/issues; unsafe tool actions<br/> <strong>Scale note:</strong> isolate tool instructions; enforce allowlists; scan diffs for secrets</p> <hr/> <h2 id="reaction-5--multimodal-image-generation-studio-text-to-image">Reaction 5 — Multimodal Image Generation Studio (Text-to-Image)</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Ch + Sc + Gr + Mm (+ Sm)</code></p> <p><strong>Use case:</strong> Generate/edit images from text while enforcing policy and consistent style.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Asset library MCP (brand assets, templates)</li> <li>Storage MCP (save versions, metadata)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Apply templates (<strong>Ch</strong>) for style + constraints</li> <li>Validate policy (<strong>Gr</strong>) and structure (<strong>Sc</strong>)</li> <li>Generate with <strong>Mm</strong>; optionally <strong>Sm</strong> for fast previews</li> <li>Store versions via MCP; export packages</li> </ol> <p><strong>Where it breaks:</strong> unsafe prompt requests; reproducibility issues<br/> <strong>Scale note:</strong> version prompts, seeds, and model versions</p> <hr/> <h2 id="reaction-6--document-understanding--extraction-invoices-forms">Reaction 6 — Document Understanding &amp; Extraction (Invoices, Forms)</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Mm + Sc + Fc + MCP + Gr + Lg</code></p> <p><strong>Use case:</strong> Extract structured fields from PDFs/images and write to downstream systems.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Document store MCP (Drive/S3/Blob)</li> <li>ERP/CRM MCP (SAP/Dynamics/etc.)</li> <li>Validation rules MCP (schemas, business checks)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Fetch doc via MCP; parse with <strong>Mm</strong></li> <li>Extract fields with strict <strong>Sc</strong> schema (with confidence)</li> <li>Validate business rules; route to human review if low confidence</li> <li>Write results via <strong>Fc</strong> through MCP</li> <li>Apply <strong>Gr</strong> for PII handling and safe logging</li> </ol> <p><strong>Where it breaks:</strong> adversarial PDFs, schema drift, low-confidence fields<br/> <strong>Scale note:</strong> keep labeled eval set per template + language</p> <hr/> <h2 id="reaction-7--customer-support-autopilot-triage--draft--crm-update">Reaction 7 — Customer Support Autopilot (Triage + Draft + CRM Update)</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Em + Vx + Rg + Fc + Ag + MCP + Sc + Gr + Lg</code></p> <p><strong>Use case:</strong> Classify tickets, retrieve knowledge, draft responses, update CRM safely.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Ticketing MCP (Zendesk/Freshdesk)</li> <li>CRM MCP (Salesforce/HubSpot)</li> <li>Knowledge base MCP (Confluence/SharePoint)</li> <li>Messaging MCP (Slack/Teams) for escalation</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Ingest knowledge base into <strong>Vx</strong></li> <li>Classify and retrieve context via <strong>Rg</strong></li> <li>Draft response with <strong>Sc</strong> (tone, required fields, disclaimers)</li> <li>Execute allowed CRM updates via MCP after policy checks</li> <li>Escalate edge cases via messaging MCP and learn from feedback</li> </ol> <p><strong>Where it breaks:</strong> policy violations and hallucinated promises<br/> <strong>Scale note:</strong> approvals-first rollout; strict allowed-action playbook</p> <hr/> <h2 id="reaction-8--security-operations-copilot-alert-investigation">Reaction 8 — Security Operations Copilot (Alert Investigation)</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Rg + Vx + Fc + Ag + MCP + Gr + Rt + Th</code></p> <p><strong>Use case:</strong> Investigate alerts, gather evidence, produce incident summary.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>SIEM MCP (Splunk/Sentinel)</li> <li>EDR MCP (Defender/CrowdStrike)</li> <li>Threat intel MCP (feeds/IOCs)</li> <li>Case management MCP (ServiceNow)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Retrieve runbooks and similar incidents via <strong>Rg</strong></li> <li>Agent queries SIEM/EDR via MCP and builds a structured case file (<strong>Sc</strong>)</li> <li>Use <strong>Th</strong> selectively for complex correlation</li> <li>Draft timeline + recommendations; update case via MCP</li> <li>Apply <strong>Gr</strong>: no destructive actions without approval; data minimization</li> <li>Continuous <strong>Rt</strong> for injection attempts through logs/runbooks</li> </ol> <p><strong>Where it breaks:</strong> attacker-controlled text attempting prompt injection<br/> <strong>Scale note:</strong> read-only by default; strict tool permissions; audit everything</p> <hr/> <h2 id="reaction-9--multi-agent-research--writing-system">Reaction 9 — Multi-agent Research &amp; Writing System</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Pr + Ma + Ag + Fc + Fw + MCP + Rg + Gr + Th</code></p> <p><strong>Use case:</strong> Agents gather sources, synthesize, and write a report with traceable citations.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Reference manager MCP (Zotero/Mendeley)</li> <li>Internal docs MCP (policies, prior reports)</li> <li>Project tracker MCP (Jira) for tasks/deadlines</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Planner agent decomposes work and assigns roles (<strong>Ma</strong>)</li> <li>Research agent retrieves sources via MCP and summarizes grounded via <strong>Rg</strong></li> <li>Critic agent checks claims and consistency</li> <li>Writer agent outputs structured report (<strong>Sc</strong>)</li> <li><strong>Gr:</strong> no fabricated citations; explicit uncertainty; traceable sources</li> <li>Use <strong>Th</strong> only where necessary to manage cost</li> </ol> <p><strong>Where it breaks:</strong> citation fabrication, inconsistent claims across agents<br/> <strong>Scale note:</strong> enforce “every claim must map to retrieved evidence” rules</p> <hr/> <h2 id="reaction-10--on-device-assistant-with-local-rag-sync">Reaction 10 — On-device Assistant with Local RAG Sync</h2> <p><strong>Formula:</strong> <code class="language-plaintext highlighter-rouge">Sm + Em + Vx + Rg + MCP + Sc + Gr (+ Ft)</code></p> <p><strong>Use case:</strong> Low-latency assistant on-device with local retrieval + optional cloud sync.</p> <p><strong>Typical MCP servers:</strong></p> <ul> <li>Local files MCP (device file system)</li> <li>Calendar/tasks MCP (OS integrations)</li> <li>Optional cloud sync MCP (enterprise knowledge snapshot)</li> </ul> <p><strong>Reference flow:</strong></p> <ol> <li>Build local embedding index (<strong>Em</strong>) in small <strong>Vx</strong></li> <li>Answer via <strong>Rg</strong>; keep responses structured (<strong>Sc</strong>)</li> <li>Use MCP tools to read/update notes and calendar (confirmations required)</li> <li>Apply <strong>Gr</strong>: privacy-by-default; no cloud sending unless opt-in</li> <li>Optional <strong>Ft</strong> for personalization (with auditable data)</li> </ol> <p><strong>Where it breaks:</strong> limited capability vs larger models<br/> <strong>Scale note:</strong> cascade routing—use Sm first, escalate only with explicit permission</p> <hr/> <h1 id="practical-tips-what-ive-found-works-in-real-builds">Practical Tips (What I’ve Found Works in Real Builds)</h1> <ul> <li><strong>Start from the reaction you need, not the component you like.</strong> Product goal determines the formula.</li> <li><strong>Evaluate retrieval and generation separately.</strong> Many failures come from wrong context, not model capability.</li> <li><strong>Treat tool access like production security.</strong> MCP servers should have auth, RBAC, logging, and rate limits.</li> <li><strong>Guardrails are not one filter.</strong> Combine <strong>Sc</strong> (constraints), <strong>Gr</strong> (policies), and <strong>Rt</strong> (adversarial testing).</li> <li><strong>Use small models where possible.</strong> Route to larger/thinking models only when needed.</li> <li><strong>Version everything.</strong> Prompts, schemas, indexes, model versions, and policy rules.</li> </ul> <hr/> <h2 id="references">References</h2> <ul> <li>Martin Keen (IBM), “What if AI had its own periodic table?” (YouTube): https://www.youtube.com/watch?v=ESBMgZHzfG0</li> </ul> <hr/> <h3 id="want-to-collaborate">Want to collaborate?</h3> <p>If you’re building GenAI systems (RAG, agents, multimodal, or evaluation tooling), feel free to reach out: <a href="https://www.linkedin.com/in/bhardwaj-suraj/">LinkedIn</a> • <a href="https://github.com/SurajBhar">GitHub</a> • <a href="https://surajbhar.github.io/">Website</a></p>]]></content><author><name>Suraj Bhardwaj</name></author><category term="ai-systems"/><category term="ai-ml"/><summary type="html"><![CDATA[A practical framework to think about modern AI like chemistry — reusable components (LLMs, RAG, agents, guardrails) that combine into predictable reference architectures, including MCP-based tool integration.]]></summary></entry><entry><title type="html">Understanding AI Agents and Agentic AI</title><link href="https://surajbhar.github.io/blog/2025/09/agentic-ai/" rel="alternate" type="text/html" title="Understanding AI Agents and Agentic AI"/><published>2025-09-28T05:00:00+00:00</published><updated>2025-09-28T05:00:00+00:00</updated><id>https://surajbhar.github.io/blog/2025/09/agentic-ai</id><content type="html" xml:base="https://surajbhar.github.io/blog/2025/09/agentic-ai/"><![CDATA[<h2 id="what-are-agents">What Are Agents?</h2> <p>In AI, <strong>agents</strong> are autonomous systems that can perceive their environment, reason about their goals, and take actions to achieve them. Unlike traditional software that follows fixed instructions, agents adapt to context, interact with external tools, and continuously refine their decisions. Think of them as “decision-making entities” powered by large language models (LLMs) and integrated with real-world data streams.</p> <h2 id="what-does-agentic-ai-mean">What Does Agentic AI Mean?</h2> <p><strong>Agentic AI</strong> refers to the paradigm where LLMs are not just passive text generators but active participants in a reasoning–action loop. In this setup, an LLM is augmented with memory, tool-use capabilities, and the ability to plan across multiple steps. Instead of answering a single prompt, Agentic AI systems dynamically decide what to do next: query knowledge, call an API, run code, or ask clarifying questions.</p> <h2 id="why-do-we-need-agents">Why Do We Need Agents?</h2> <p>LLMs alone are powerful but limited — they have no persistent memory, can hallucinate, and cannot directly act in digital ecosystems. Agents fill this gap by enabling <strong>autonomy, interactivity, and continuous improvement</strong>. They allow AI to move from “static Q&amp;A” toward <strong>problem-solving companions</strong> in domains like software engineering, research, operations, and personalized tutoring.</p> <h2 id="building-blocks-of-agents">Building Blocks of Agents</h2> <p>AI agents are constructed from several <strong>core components</strong>:</p> <ul> <li><strong>Perception/Input Layer</strong> (understanding prompts, parsing context).</li> <li><strong>Reasoning Engine</strong> (often an LLM that interprets and plans).</li> <li><strong>Memory Systems</strong> (short-term and long-term recall, often supported by vector databases).</li> <li><strong>Action Interfaces</strong> (APIs, tools, external systems).</li> <li><strong>Feedback Mechanisms</strong> (evaluating results, self-correction, or asking users for clarification).</li> </ul> <p>Together, these blocks allow agents to operate as <strong>closed-loop systems</strong>:</p> <ul> <li><strong>perceive → reason → act → learn</strong>.</li> </ul> <h2 id="role-of-vector-databases">Role of Vector Databases</h2> <p>Vector databases (like Pinecone, Weaviate, Milvus, FAISS) are essential for agent memory. Instead of storing data in rows and columns, they represent knowledge as <strong>high-dimensional embeddings</strong> that capture semantic meaning. This lets agents perform similarity search: retrieving contextually relevant documents, code snippets, or past conversations in milliseconds. Without vector stores, agents would lack the “contextual memory” needed for coherent reasoning and grounded responses.</p> <h2 id="role-of-mcp-servers">Role of MCP Servers</h2> <p><strong>MCP (Model Context Protocol) servers</strong> are an emerging standard that allow LLM-based agents to securely access tools, APIs, and organizational data. Think of them as a <strong>bridge between the LLM and external systems</strong>: they expose structured endpoints (search, database queries, custom tools) while enforcing security and governance. In Agentic AI, MCP servers solve the challenge of safe tool integration, ensuring agents can act beyond text generation while maintaining compliance and reliability.</p> <h2 id="mathematical-intuition-behind-agentic-ai">Mathematical Intuition Behind Agentic AI</h2> <p>At its core, Agentic AI can be framed mathematically as a <strong>sequential decision-making process</strong>. An agent observes state $(s_t)$, reasons about actions $(a_t)$, and updates its belief of the environment based on feedback $(r_t)$. This aligns with the framework of <strong>Markov Decision Processes (MDPs)</strong> and <strong>reinforcement learning</strong>, but enhanced by LLM priors. The LLM provides a probabilistic policy $( \pi(a|s))$ grounded in language understanding, while vector databases supply state memory and MCP servers expand the action space. In simple terms, Agentic AI = LLMs + memory + tools + feedback, operating like a stochastic planner over dynamic environments.</p> <h2 id="final-thoughts">Final Thoughts</h2> <p>AI agents mark a shift from <strong>static models</strong> to <strong>interactive problem solvers</strong>. With building blocks like vector databases for memory, MCP servers for tool access, and mathematical foundations in decision theory, Agentic AI systems can operate in increasingly complex environments. While still early, this direction holds promise for making AI more reliable, autonomous, and useful in real-world workflows.</p>]]></content><author><name>Suraj Bhardwaj</name></author><category term="ai-ml"/><category term="external-services"/><category term="agentic-ai"/><summary type="html"><![CDATA[A primer on AI agents, why they matter, and the mathematical and system-level intuition behind Agentic AI.]]></summary></entry><entry><title type="html">Understanding the GPT-Style Assistant Training Pipeline</title><link href="https://surajbhar.github.io/blog/2025/09/llm-training-pipeline/" rel="alternate" type="text/html" title="Understanding the GPT-Style Assistant Training Pipeline"/><published>2025-09-27T20:42:00+00:00</published><updated>2025-09-27T20:42:00+00:00</updated><id>https://surajbhar.github.io/blog/2025/09/llm-training-pipeline</id><content type="html" xml:base="https://surajbhar.github.io/blog/2025/09/llm-training-pipeline/"><![CDATA[<p>Large language models (LLMs) such as GPT-2, GPT-3, and ChatGPT are not trained in one step. Instead, they go through a <strong>multi-stage pipeline</strong> that gradually transforms a raw text predictor into a helpful, safe, and instruction-following assistant.</p> <h3 id="1-pretraining">1. Pretraining</h3> <p>The journey starts with <strong>pretraining</strong> — teaching a neural network general language understanding.<br/> A vast corpus of <strong>hundreds of billions to trillions of tokens</strong> is collected and carefully filtered from sources like web pages, books, encyclopedias, and open-source code. The model is trained with the <strong>causal language modeling</strong> objective: predicting the next token given the previous sequence.</p> <p>This process is extremely compute-intensive, requiring <strong>thousands of GPUs or TPUs</strong> for <strong>weeks to months</strong>. The result is a <strong>base model</strong> — strong at generating text but not yet fine-tuned for safe, user-friendly responses.</p> <h3 id="2-supervised-fine-tuning-sft">2. Supervised Fine-Tuning (SFT)</h3> <p>Next comes <strong>supervised fine-tuning</strong>, where human annotators create <strong>instruction–response pairs</strong> (from about 10k up to a few hundred thousand examples). The base model is fine-tuned on this smaller, high-quality dataset using the same next-token prediction loss but at a far lower compute cost (often <strong>tens of GPUs for a few days</strong>).<br/> This step yields an <strong>instruction-tuned model</strong> — better at following prompts and producing clear, task-oriented answers (e.g., InstructGPT, Vicuna).</p> <h3 id="3-reward-modeling-rm">3. Reward Modeling (RM)</h3> <p>To align with human judgment, developers train a <strong>reward model</strong>. Human raters compare multiple outputs for the same prompt and rank them by <strong>helpfulness and safety</strong> (typically <strong>50k–300k comparisons</strong>).<br/> The reward model learns to predict a <strong>preference score</strong> and is used later to guide optimization. This step requires moderate compute but is crucial for aligning outputs with what humans consider good.</p> <h3 id="4-reinforcement-learning-from-human-feedback-rlhf">4. Reinforcement Learning from Human Feedback (RLHF)</h3> <p>Finally, <strong>RLHF</strong> applies <strong>reinforcement learning</strong> — usually <strong>Proximal Policy Optimization (PPO)</strong> — to refine the assistant. The model is rewarded when its responses score highly on the reward model, while ensuring stability and avoiding catastrophic drift.<br/> This makes the assistant more <strong>truthful, harmless, and helpful</strong>, bridging the gap between raw text generation and safe deployment (key for ChatGPT-like behavior).</p> <h3 id="deployment--continuous-improvement">Deployment &amp; Continuous Improvement</h3> <p>After RLHF, the model can be deployed as an API or integrated into apps. User feedback — ratings, refusal cases, jailbreak attempts — is used to retrain or adjust fine-tuning and safety filters. This <strong>iterative improvement loop</strong> keeps the assistant aligned with evolving use cases and safety standards.</p> <h2 id="glossary-of-key-technical-terms">Glossary of Key Technical Terms</h2> <table> <thead> <tr> <th>Term</th> <th>Definition</th> </tr> </thead> <tbody> <tr> <td><strong>LLM (Large Language Model)</strong></td> <td>Neural network with hundreds of millions to trillions of parameters trained on large text corpora.</td> </tr> <tr> <td><strong>Token</strong></td> <td>Smallest unit of text (word, subword, or character) used during training and inference.</td> </tr> <tr> <td><strong>Base Model</strong></td> <td>LLM trained only on next-token prediction without special safety or task tuning.</td> </tr> <tr> <td><strong>Instruction–Response Pair</strong></td> <td>Prompt and ideal answer used for fine-tuning.</td> </tr> <tr> <td><strong>Supervised Fine-Tuning (SFT)</strong></td> <td>Training on curated instruction data to make the model assistant-like.</td> </tr> <tr> <td><strong>Reward Model (RM)</strong></td> <td>Model predicting preference scores from human ratings to guide RLHF.</td> </tr> <tr> <td><strong>Reinforcement Learning from Human Feedback (RLHF)</strong></td> <td>RL method optimizing the model using human-derived reward signals.</td> </tr> <tr> <td><strong>PPO (Proximal Policy Optimization)</strong></td> <td>Popular reinforcement learning algorithm for fine-tuning language models.</td> </tr> <tr> <td><strong>GPU/TPU</strong></td> <td>Specialized hardware (Graphics/ Tensor Processing Units) for large-scale neural network training.</td> </tr> <tr> <td><strong>Causal Language Modeling</strong></td> <td>Objective where the model predicts the next token from the sequence so far.</td> </tr> </tbody> </table> <p><em>If you’re exploring LLM development or safety research, understanding these stages helps demystify how cutting-edge assistants evolve from raw text predictors to human-aligned AI systems.</em></p>]]></content><author><name>Suraj Bhardwaj</name></author><category term="llms"/><summary type="html"><![CDATA[From raw text to aligned AI assistants — a clear overview of how models like ChatGPT are built]]></summary></entry><entry><title type="html">Vector Databases — Concepts, Terminologies, and Industry Players</title><link href="https://surajbhar.github.io/blog/2025/09/vector-databases/" rel="alternate" type="text/html" title="Vector Databases — Concepts, Terminologies, and Industry Players"/><published>2025-09-22T20:40:00+00:00</published><updated>2025-09-22T20:40:00+00:00</updated><id>https://surajbhar.github.io/blog/2025/09/vector-databases</id><content type="html" xml:base="https://surajbhar.github.io/blog/2025/09/vector-databases/"><![CDATA[<p>Vector Databases — Concepts, Terminologies, and Industry Players</p> <h2 id="what-are-vector-databases">What Are Vector Databases?</h2> <p>Traditional databases store structured data in rows and columns. <strong>Vector databases</strong>, by contrast, are designed to store and query <strong>embeddings</strong> — high-dimensional vectors produced by models that capture semantic meaning. For example, the sentence <em>“The cat sat on the mat”</em> can be transformed into a 1,536-dimensional vector (using OpenAI embeddings). Storing such vectors allows for <strong>similarity search</strong>, enabling systems to retrieve semantically relevant information instead of relying on keyword matching.</p> <h2 id="why-do-we-need-them">Why Do We Need Them?</h2> <p>As LLMs and multimodal systems become context-hungry, agents must recall knowledge, past interactions, and reference documents. A vector database provides:</p> <ul> <li><strong>Semantic search:</strong> retrieving relevant results by meaning, not just keywords.</li> <li><strong>Context injection:</strong> supplying the most relevant chunks to LLM prompts.</li> <li><strong>Scalability:</strong> storing millions or billions of embeddings while maintaining fast query speed.</li> <li><strong>Low latency:</strong> powering real-time applications like RAG (Retrieval-Augmented Generation), recommendation engines, and chatbots.</li> </ul> <p>Without vector stores, AI systems would remain <strong>memory-less</strong> and struggle to scale in production.</p> <h2 id="key-concepts-and-terminologies">Key Concepts and Terminologies</h2> <ul> <li><strong>Embeddings:</strong> numerical representations of text, images, audio, or code in high-dimensional space.</li> <li><strong>Dimensionality:</strong> the length of embedding vectors (e.g., 384, 768, 1536 dimensions).</li> <li><strong>Similarity Metrics:</strong> mathematical functions to compare vectors, such as cosine similarity, Euclidean distance, or dot product.</li> <li><strong>Approximate Nearest Neighbor (ANN) Search:</strong> algorithms that speed up similarity search by trading exactness for efficiency (e.g., HNSW, IVF, PQ).</li> <li><strong>Indexing:</strong> data structures that organize vectors for fast search, like inverted files or hierarchical graphs.</li> <li><strong>Recall vs. Latency Trade-off:</strong> higher recall ensures better accuracy but increases query time; ANN balances both.</li> <li><strong>Hybrid Search:</strong> combining semantic vector search with keyword/metadata filters.</li> <li><strong>Persistence &amp; Scaling:</strong> distributed storage, replication, and cloud-native scaling for production workloads.</li> </ul> <h2 id="vector-database-providers-in-the-market">Vector Database Providers in the Market</h2> <p>Several players provide vector databases or integrate vector search into broader data platforms:</p> <ul> <li><strong>Pinecone</strong> – A fully managed cloud-native vector database, widely used in RAG pipelines.</li> <li><strong>Weaviate</strong> – Open-source and cloud-hosted options, strong support for hybrid search and modular extensions.</li> <li><strong>Milvus</strong> – An open-source, high-performance vector database backed by Zilliz, optimized for billion-scale datasets.</li> <li><strong>FAISS</strong> (Facebook AI Similarity Search) – A library (not a full database) for efficient similarity search; often embedded in custom solutions.</li> <li><strong>Chroma</strong> – Lightweight, developer-friendly vector database popular in LangChain/LLM projects.</li> <li><strong>Qdrant</strong> – Open-source with strong focus on high-performance ANN search and production readiness.</li> <li><strong>Vespa</strong> – Large-scale serving engine for search and recommendation, integrates dense and sparse retrieval.</li> <li><strong>Elasticsearch / OpenSearch</strong> – Traditional search engines that now support dense vector search alongside keyword indexing.</li> <li><strong>Redis Vector</strong> – Redis modules with vector similarity search, blending cache + vector store.</li> <li><strong>Postgres Extensions (pgvector)</strong> – Add-on for PostgreSQL to handle embeddings directly in relational DB workflows.</li> </ul> <h2 id="final-thoughts">Final Thoughts</h2> <p>Vector databases are the <strong>memory layer of modern AI systems</strong>. They transform embeddings into actionable context, powering RAG, semantic search, and Agentic AI workflows. With diverse players — from fully managed SaaS (Pinecone) to open-source frameworks (Milvus, Weaviate, FAISS) — the space is evolving rapidly. As models scale and applications demand <strong>low-latency semantic recall</strong>, vector databases will remain a foundational pillar of AI infrastructure.</p>]]></content><author><name>Suraj Bhardwaj</name></author><category term="ai-ml"/><category term="external-services"/><category term="agentic-ai"/><summary type="html"><![CDATA[A deep dive into vector databases, their key concepts, and the major providers shaping this critical AI infrastructure.]]></summary></entry><entry><title type="html">Attention Mechanisms in Deep Learning</title><link href="https://surajbhar.github.io/blog/2025/09/attention/" rel="alternate" type="text/html" title="Attention Mechanisms in Deep Learning"/><published>2025-09-20T20:19:00+00:00</published><updated>2025-09-20T20:19:00+00:00</updated><id>https://surajbhar.github.io/blog/2025/09/attention</id><content type="html" xml:base="https://surajbhar.github.io/blog/2025/09/attention/"><![CDATA[<p>The <strong>attention mechanism</strong> has reshaped modern AI, becoming the foundation of models like Transformers, GPT, and Vision Transformers. It was originally proposed to overcome a weakness in <strong>sequence-to-sequence (seq2seq)</strong> models: the inability to remember long inputs when compressed into a single fixed-length vector.</p> <p>By letting models <em>“pay attention”</em> to different parts of the input dynamically, attention mechanisms provide a <strong>flexible, adaptive context</strong> for making predictions.</p> <h2 id="the-intuition-behind-attention">The Intuition Behind Attention</h2> <p>Attention is inspired by how humans <strong>focus selectively</strong>.</p> <ul> <li>In vision: when we look at a photo of a dog in a sweater, our eyes shift between the ears, eyes, and nose, assigning “high resolution” to the important parts and less focus elsewhere.</li> <li>In language: when reading <em>“She is eating …”</em>, we naturally expect the next word to be food-related, not random.</li> </ul> <p>Mathematically, attention is a <strong>weighted sum</strong> of input representations, where weights reflect <strong>relevance</strong> to the current decoding step or token.</p> <h2 id="why-attention-was-born">Why Attention Was Born</h2> <p>Classic seq2seq models used an <strong>encoder-decoder</strong> with a fixed context vector (Sutskever et al., 2014). This vector often “forgot” early parts of long sentences.</p> <p>Bahdanau et al. (2015) introduced <strong>additive attention</strong>, allowing the decoder to access <em>all</em> encoder hidden states, with learned weights determining which tokens matter most. This resolved long-sequence memory issues in <strong>machine translation</strong> and opened the door to broader adoption.</p> <h2 id="categories-of-attention">Categories of Attention</h2> <table> <thead> <tr> <th>#</th> <th>Category</th> <th>Variants / Examples</th> <th>Key Idea</th> </tr> </thead> <tbody> <tr> <td><strong>1</strong></td> <td><strong>Soft Attention (Deterministic)</strong></td> <td>Additive (Bahdanau), Multiplicative/Dot-Product (Luong), Scaled Dot-Product (Transformers)</td> <td>Continuous weights over all tokens; smooth, fully differentiable.</td> </tr> <tr> <td><strong>2</strong></td> <td><strong>Hard Attention (Stochastic)</strong></td> <td>REINFORCE-based Hard Attention, Glimpse Networks</td> <td>Selects discrete positions; cheaper at inference but not differentiable, needs sampling/RL</td> </tr> <tr> <td><strong>3</strong></td> <td><strong>Self-Attention</strong></td> <td>Vanilla Transformer Self-Attention, Multi-Head Attention, Sparse Attention (Longformer, BigBird), Linear Attention (Performer, Linformer), SAGAN (for vision)</td> <td>Each token attends to others in the <em>same</em> sequence.</td> </tr> <tr> <td><strong>4</strong></td> <td><strong>Cross-Attention</strong></td> <td>Encoder–Decoder Attention (seq2seq), Query–Key Attention, Fusion-in-Decoder (FiD), multimodal alignment (text–image, text–speech)</td> <td>One sequence queries another (e.g., decoder attends to encoder).</td> </tr> <tr> <td><strong>5</strong></td> <td><strong>Structured / Specialized Attention</strong></td> <td>Local vs Global, Axial Attention, Hierarchical Attention Networks, Graph Attention (GAT), Co-Attention (VQA), Memory-Augmented Attention (Neural Turing Machine)</td> <td>Attention patterns are biased to suit structure (graphs, long docs, multimodal input).</td> </tr> <tr> <td><strong>6</strong></td> <td><strong>Pointer-style Attention</strong></td> <td>Pointer Networks (Vinyals et al., 2015)</td> <td>Uses attention not to blend context but to <em>select</em> positions from input (e.g., TSP).</td> </tr> </tbody> </table> <h2 id="key-innovations-and-extensions">Key Innovations and Extensions</h2> <ul> <li><strong>Global vs Local Attention (Luong, 2015):</strong> Global attends to the whole sequence, local narrows focus to a window.</li> <li><strong>Neural Turing Machines (Graves, 2014):</strong> Used content-based + location-based attention to read/write from memory.</li> <li><strong>Transformers (Vaswani, 2017):</strong> Replaced recurrence entirely with <em>multi-head scaled dot-product self-attention</em>.</li> <li><strong>SNAIL (Mishra, 2017):</strong> Mixed self-attention with temporal convolutions to fix positional weaknesses.</li> <li><strong>Self-Attention GANs (Zhang, 2018):</strong> Used attention in vision tasks to capture global pixel dependencies.</li> </ul> <h2 id="advantages-of-attention">Advantages of Attention</h2> <ul> <li>Captures <strong>long-range dependencies</strong> better than RNNs.</li> <li><strong>Parallelizable</strong>, unlike sequential RNN computation.</li> <li>Adaptable across domains: text, images, graphs, multimodal.</li> <li>Offers <strong>interpretability</strong> via attention weights.</li> </ul> <h2 id="drawbacks-of-attention">Drawbacks of Attention</h2> <ul> <li><strong>Quadratic cost</strong> in vanilla self-attention: O(n²) with sequence length.</li> <li><strong>Ambiguity in interpretability</strong>: high weights ≠ true causal focus.</li> <li>Requires <strong>large data and compute</strong> for effective training.</li> <li>Memory-hungry in very large-scale models.</li> </ul> <h2 id="summary">Summary</h2> <p>From its origins in machine translation to powering <strong>Transformers and beyond</strong>, attention has proven to be one of the most <strong>general-purpose inductive biases</strong> in deep learning. Different forms — soft, hard, self, cross, structured — all share the goal of letting models <strong>focus adaptively</strong> on what matters most.</p> <p>The future direction lies in making attention <strong>cheaper, more scalable, and more robust</strong>, with innovations like <strong>sparse/linear attention</strong> and hybrid designs.</p> <p><em>Disclaimer: This post reflects my personal interpretations and opinions on AI. It simplifies some technical details for readability.</em></p>]]></content><author><name>Suraj Bhardwaj</name></author><category term="ai-ml"/><category term="llms"/><summary type="html"><![CDATA[A comprehensive guide to attention mechanisms — from their origins in machine translation to modern variants powering Transformers, GANs, and beyond.]]></summary></entry><entry><title type="html">Advance Git Commands</title><link href="https://surajbhar.github.io/blog/2025/09/advance-git/" rel="alternate" type="text/html" title="Advance Git Commands"/><published>2025-09-16T13:29:00+00:00</published><updated>2025-09-16T13:29:00+00:00</updated><id>https://surajbhar.github.io/blog/2025/09/advance-git</id><content type="html" xml:base="https://surajbhar.github.io/blog/2025/09/advance-git/"><![CDATA[<p>Git is more than <code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">commit</code>, and <code class="language-plaintext highlighter-rouge">push</code>. Once you start collaborating on real projects, you’ll often need to rewrite history, clean up commits, rebase branches, or even recover “lost” work. This guide explores advanced Git features with explanations and space for practical demonstrations.</p> <h2 id="1-commit-history-rewriting-in-git">1. Commit History Rewriting in Git</h2> <h3 id="11-amending-git-commits">1.1 Amending Git Commits</h3> <p>Amending in Git is like fixing or updating your most recent commit without creating a brand-new one. Instead of stacking another commit on top, you “rewrite” the last one.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span>
0832375 <span class="o">(</span>HEAD -&gt; main, origin/main<span class="o">)</span> HTML File
f86213d first commit


git commit <span class="nt">-am</span> <span class="s2">"main:modified the title - blog.html"</span>
<span class="o">[</span>main ab40261] main:modified the title - blog.html
 2 files changed, 9 insertions<span class="o">(</span>+<span class="o">)</span>, 1 deletion<span class="o">(</span>-<span class="o">)</span>


git log <span class="nt">--oneline</span>                                   
ab40261 <span class="o">(</span>HEAD -&gt; main<span class="o">)</span> main:modified the title - blog.html
0832375 <span class="o">(</span>origin/main<span class="o">)</span> HTML File
f86213d first commit
</code></pre></div></div> <ul> <li>Lets’s suppose you have a file named 404.html and you want to commit the changes made inside this file as well in the previous commit.</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Stage the changes</span>
git add 404.html

git status
On branch main
Your branch is ahead of <span class="s1">'origin/main'</span> by 1 commit.
  <span class="o">(</span>use <span class="s2">"git push"</span> to publish your <span class="nb">local </span>commits<span class="o">)</span>

Changes to be committed:
  <span class="o">(</span>use <span class="s2">"git restore --staged &lt;file&gt;..."</span> to unstage<span class="o">)</span>
        new file:   404.html

</code></pre></div></div> <p>In other words, now we want to rewrite the previous commit SHA “ab40261” and amend it to reflect the changes made in both blog.html and 404.html file.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Commit the changes with amend and no edit flag</span>
git commit <span class="nt">--amend</span> <span class="nt">--no-edit</span>

<span class="o">[</span>main 4bf9f69] main:modified the title - blog.html
 Date: Thu Sep 11 20:15:36 2025 +0200
 3 files changed, 60 insertions<span class="o">(</span>+<span class="o">)</span>, 1 deletion<span class="o">(</span>-<span class="o">)</span>
 create mode 100644 404.html

<span class="c"># The commit has been made with new SHA and the old commit shaw is deleted</span>
git log <span class="nt">--oneline</span>           
4bf9f69 <span class="o">(</span>HEAD -&gt; main<span class="o">)</span> main:modified the title - blog.html
0832375 <span class="o">(</span>origin/main<span class="o">)</span> HTML File
f86213d first commit

</code></pre></div></div> <ul> <li>”–no-edit” flag let’s you make the amendment to your commit without changing its commit message.</li> <li>Amending rewrites history. If you already pushed that commit to a shared branch, amending it means the hash changes, which can cause confusion for others. Safe rule of thumb: <ul> <li>Amend freely on local commits you haven’t shared.</li> <li>Avoid amending commits that are already pushed to remote unless you know how to handle a force-push.</li> </ul> </li> </ul> <h4 id="tomake-changes-to-the-message-of-the-commit">Tomake changes to the message of the commit:</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add config.yaml
git commit <span class="nt">--amend</span>

<span class="o">[</span>main 65cf379] main:modified the title - blog.html and added 404.html<span class="p">;</span> added config.yaml
 Date: Thu Sep 11 20:15:36 2025 +0200
 4 files changed, 80 insertions<span class="o">(</span>+<span class="o">)</span>, 1 deletion<span class="o">(</span>-<span class="o">)</span>
 create mode 100644 404.html
 create mode 100644 config.yaml

git log <span class="nt">--oneline</span>  
65cf379 <span class="o">(</span>HEAD -&gt; main<span class="o">)</span> main:modified the title - blog.html and added 404.html<span class="p">;</span> added config.yaml
0832375 <span class="o">(</span>origin/main<span class="o">)</span> HTML File
f86213d first commit

</code></pre></div></div> <p>If you only want to fix the <strong>commit message</strong> and not change the files, Git gives you a flag for that:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">--amend</span> <span class="nt">-m</span> <span class="s2">"New, corrected commit message"</span>
</code></pre></div></div> <p>That command replaces the old message with the new one while keeping the commit’s content exactly the same.</p> <p>Alternatively, if you want to open your editor to edit the message interactively:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">--amend</span>
</code></pre></div></div> <p>Just don’t stage any new files before running it—otherwise Git will also include those changes in the amended commit.</p> <h2 id="2-git-rebase">2. Git Rebase</h2> <p><strong>Rebase</strong> moves your commits to a new base commit. Instead of merging two branches (which creates a merge commit), rebase <strong>replays</strong> your branch’s commits <em>on top of</em> another branch, producing a <strong>linear history</strong>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new branch named feature-branch</span>
git checkout <span class="nt">-b</span> feature-branch 

<span class="c"># sample command for rebase</span>
git rebase &lt;BASE&gt;
<span class="c"># base can be: an ID, a branch name, a tag, or a relative referance to HEAD</span>
git rebase main
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">--graph</span> <span class="nt">--all</span>
<span class="k">*</span> 89293e4 <span class="o">(</span>HEAD -&gt; main, origin/main<span class="o">)</span> Rebasing
<span class="k">*</span> 86bcbc6 Updated Readme with future section
<span class="k">*</span> 65cf379 main:modified the title - blog.html and added 404.html<span class="p">;</span> added config.yaml
<span class="k">*</span> 0832375 HTML File
<span class="k">*</span> f86213d first commit

</code></pre></div></div> <h4 id="practical-example-with-conflict-simulation">Practical Example (with Conflict Simulation)</h4> <p>In this walkthrough, we will simulate a real scenario: creating a feature branch, making changes, modifying <code class="language-plaintext highlighter-rouge">main</code>, and then rebasing with conflicts.</p> <h4 id="step-1--create-a-new-feature-branch">Step 1- Create a New Feature Branch</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">-b</span> feature-branch
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Switched to a new branch 'feature-branch'
</code></pre></div></div> <p>At this point, both <code class="language-plaintext highlighter-rouge">main</code> and <code class="language-plaintext highlighter-rouge">feature-branch</code> point to the same commit.</p> <h4 id="step-2--add-commits-to-feature-branch">Step 2- Add Commits to Feature Branch</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-am</span> <span class="s2">"feature-branch: modified title - 404.html"</span>
<span class="o">[</span>feature-branch 82ff182] feature-branch: modified title - 404.html
 2 files changed, 21 insertions<span class="o">(</span>+<span class="o">)</span>, 1 deletion<span class="o">(</span>-<span class="o">)</span>

git commit <span class="nt">-am</span> <span class="s2">"feature-branch: modified Error Message - 404.html"</span>
<span class="o">[</span>feature-branch e7f8a2e] feature-branch: modified Error Message - 404.html
 2 files changed, 8 insertions<span class="o">(</span>+<span class="o">)</span>, 2 deletions<span class="o">(</span>-<span class="o">)</span>
</code></pre></div></div> <p>Now, <code class="language-plaintext highlighter-rouge">feature-branch</code> has two commits that are not present in <code class="language-plaintext highlighter-rouge">main</code>.</p> <h4 id="step-3--add-commits-to-main">Step 3- Add Commits to Main</h4> <p>Switch back to <code class="language-plaintext highlighter-rouge">main</code> and simulate independent changes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout main
git commit <span class="nt">-am</span> <span class="s2">"main: modified title again- 404.html"</span>
<span class="o">[</span>main 84900fe] main: modified title again- 404.html
 1 file changed, 2 insertions<span class="o">(</span>+<span class="o">)</span>, 2 deletions<span class="o">(</span>-<span class="o">)</span>

git commit <span class="nt">-am</span> <span class="s2">"main: modified Error Message again- 404.html"</span>
<span class="o">[</span>main 9cbea38] main: modified Error Message again- 404.html
 1 file changed, 2 insertions<span class="o">(</span>+<span class="o">)</span>, 2 deletions<span class="o">(</span>-<span class="o">)</span>
</code></pre></div></div> <p><strong>Visual Log (<code class="language-plaintext highlighter-rouge">git log --oneline --decorate --graph --all</code>):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>* 9cbea38 (HEAD -&gt; main) main: modified Error Message again- 404.html
* 84900fe main: modified title again- 404.html
| * e7f8a2e (feature-branch) feature-branch: modified Error Message - 404.html
| * 82ff182 feature-branch: modified title - 404.html
|/  
* 89293e4 (origin/main) Rebasing
* 86bcbc6 Updated Readme with future section
</code></pre></div></div> <p>Now both branches have diverged.</p> <h4 id="step-4--start-rebasing">Step 4- Start Rebasing</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout feature-branch
Switched to branch <span class="s1">'feature-branch'</span>

git rebase main
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Auto-merging 404.html
CONFLICT (content): Merge conflict in 404.html
error: could not apply 82ff182... feature-branch: modified title - 404.html
hint: Resolve all conflicts manually, mark them as resolved with
hint: "git add/rm &lt;conflicted_files&gt;", then run "git rebase --continue".
</code></pre></div></div> <p>Git stopped because of a conflict in <code class="language-plaintext highlighter-rouge">404.html</code>.</p> <h4 id="step-5--resolve-the-first-conflict">Step 5- Resolve the First Conflict</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git mergetool
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Merging:
404.html

Normal merge conflict for '404.html':
  {local}: modified file
  {remote}: modified file
</code></pre></div></div> <p>Open the file, resolve the conflict, then stage the changes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add 404.html
git rebase <span class="nt">--continue</span>
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[detached HEAD 352a722] feature-branch: Rebasing the feature branch - 404.html
 2 files changed, 21 insertions(+), 1 deletion(-)
Auto-merging 404.html
CONFLICT (content): Merge conflict in 404.html
error: could not apply e7f8a2e... feature-branch: modified Error Message - 404.html
</code></pre></div></div> <p>Another conflict occurred in the next commit.</p> <h4 id="step-6--resolve-the-second-conflict">Step 6- Resolve the Second Conflict</h4> <p>Fix the file again, then run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add 404.html
git rebase <span class="nt">--continue</span>
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[detached HEAD 24d3a81] feature-branch: rebase in progress - 404.html
 2 files changed, 8 insertions(+), 2 deletions(-)
Successfully rebased and updated refs/heads/feature-branch.
</code></pre></div></div> <p>All conflicts are resolved, and rebasing is complete.</p> <h4 id="step-7--verify-commit-history">Step 7- Verify Commit History</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">--graph</span> <span class="nt">--all</span>
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>* fca2f48 (HEAD -&gt; feature-branch) feature-branch: modified README.md File - README.md
* 24d3a81 feature-branch: rebase in progress - 404.html
* 352a722 feature-branch: Rebasing the feature branch - 404.html
* 9cbea38 (main) main: modified Error Message again- 404.html
* 84900fe main: modified title again- 404.html
* 89293e4 (origin/main) Rebasing
</code></pre></div></div> <p>Notice how <code class="language-plaintext highlighter-rouge">feature-branch</code> commits are now <strong>on top of <code class="language-plaintext highlighter-rouge">main</code></strong>, forming a linear history.</p> <h4 id="step-8--merge-feature-branch-into-main">Step 8- Merge Feature Branch into Main</h4> <p>Since <code class="language-plaintext highlighter-rouge">feature-branch</code> is rebased, merging results in a <strong>fast-forward</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout main
git merge feature-branch
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Updating 9cbea38..fca2f48
Fast-forward
 404.html  |  4 ++--
 README.md | 38 ++++++++++++++++++++++++++++++++++++++
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">main</code> now contains all rebased commits.</p> <h4 id="step-9--cleanup">Step 9- Cleanup</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-d</span> feature-branch
Deleted branch feature-branch <span class="o">(</span>was fca2f48<span class="o">)</span><span class="nb">.</span>
</code></pre></div></div> <h4 id="handy-rebase-commands">Handy Rebase Commands</h4> <ul> <li> <p><strong>Abort rebase and go back</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase <span class="nt">--abort</span>
</code></pre></div> </div> </li> <li> <p><strong>Skip a conflicting commit</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase <span class="nt">--skip</span>
</code></pre></div> </div> </li> <li> <p><strong>Continue after fixing conflicts</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase <span class="nt">--continue</span>
</code></pre></div> </div> </li> </ul> <h4 id="key-takeaways">Key Takeaways</h4> <ul> <li>Rebasing helps maintain a <strong>clean, linear history</strong>.</li> <li>Conflicts must be <strong>resolved commit by commit</strong> during rebase.</li> <li>Rebasing is typically done on <strong>feature branches</strong> before merging into <code class="language-plaintext highlighter-rouge">main</code> to ensure history clarity.</li> </ul> <p>This simulation demonstrated a real-world rebasing scenario with conflicts, including how to resolve them and preserve a clean history.</p> <h2 id="3-git-fetch">3. Git Fetch</h2> <p>When working with Git in collaborative projects, it’s important to keep your local repository <strong>aware of changes</strong> that have happened on the remote without immediately applying them. That’s where <strong><code class="language-plaintext highlighter-rouge">git fetch</code></strong> comes in.</p> <p>The command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin
</code></pre></div></div> <ul> <li><strong>Downloads</strong> objects and refs from the remote repository (<code class="language-plaintext highlighter-rouge">origin</code>).</li> <li><strong>Updates your remote-tracking branches</strong> (e.g., <code class="language-plaintext highlighter-rouge">origin/main</code>) to match the remote.</li> <li><strong>Does not modify your local branches</strong> (e.g., <code class="language-plaintext highlighter-rouge">main</code>).</li> </ul> <p>This makes <code class="language-plaintext highlighter-rouge">git fetch</code> a safe operation—you can inspect remote changes before deciding whether to merge or rebase them into your local branch.</p> <h2 id="step-1--checking-branches">Step 1- Checking Branches</h2> <p>List all local and remote branches:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-a</span>
</code></pre></div></div> <p>Output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>* main
  remotes/origin/main
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">main</code> → local branch</li> <li><code class="language-plaintext highlighter-rouge">remotes/origin/main</code> → remote-tracking branch (your local copy of the remote branch)</li> </ul> <p>Check only local branches:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-v</span>
<span class="k">*</span> main 453b2c9 Rebasing Done with Updated Readme
</code></pre></div></div> <p>Check only remote branches:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-r</span>
  origin/main
</code></pre></div></div> <p>Check remote-tracking with last commit:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-rv</span>
  origin/main 453b2c9 Rebasing Done with Updated Readme
</code></pre></div></div> <p>See everything:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch <span class="nt">-av</span>
<span class="k">*</span> main                453b2c9 Rebasing Done with Updated Readme
  remotes/origin/main 453b2c9 Rebasing Done with Updated Readme
</code></pre></div></div> <h3 id="explanation">Explanation</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">main</code></strong>: local branch, currently checked out (<code class="language-plaintext highlighter-rouge">*</code>).</li> <li><strong><code class="language-plaintext highlighter-rouge">remotes/origin/main</code></strong>: remote-tracking branch, representing the <code class="language-plaintext highlighter-rouge">main</code> branch on <code class="language-plaintext highlighter-rouge">origin</code>.</li> </ul> <p>Both point to the same commit <code class="language-plaintext highlighter-rouge">453b2c9</code>, meaning your local branch is <strong>in sync</strong> with remote.</p> <h2 id="step-2--detecting-divergence">Step 2- Detecting Divergence</h2> <p>Suppose new commits were added on the remote repository. Before fetching:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> main
453b2c9 Rebasing Done with Updated Readme
... <span class="o">(</span>older commits<span class="o">)</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> origin/main
453b2c9 Rebasing Done with Updated Readme
... <span class="o">(</span>older commits<span class="o">)</span>
</code></pre></div></div> <p>Both look the same.</p> <p>Now, after someone pushes <strong>two new commits</strong> (<code class="language-plaintext highlighter-rouge">Included Hyperparameter tuning</code>, <code class="language-plaintext highlighter-rouge">Created svm_classification.py</code>) to remote, your local copy is outdated.</p> <p>Run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin main
</code></pre></div></div> <p>Output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>From github.com:SurajBhar/advance_git
 * branch            main       -&gt; FETCH_HEAD
   453b2c9..e5c4de0  main       -&gt; origin/main
</code></pre></div></div> <p>Remote-tracking branch <code class="language-plaintext highlighter-rouge">origin/main</code> is now updated, but <strong>local <code class="language-plaintext highlighter-rouge">main</code> is untouched</strong>.</p> <h2 id="step-3--inspect-remote-changes">Step 3- Inspect Remote Changes</h2> <p>Check remote branch logs:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> origin/main
e5c4de0 <span class="o">(</span>origin/main<span class="o">)</span> Included Hyperparameter tuning
86f6a84 Created svm_classification.py
453b2c9 Rebasing Done with Updated Readme
... <span class="o">(</span>older commits<span class="o">)</span>
</code></pre></div></div> <p>Compare with local:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
On branch main
Your branch is behind <span class="s1">'origin/main'</span> by 2 commits, and can be fast-forwarded.
</code></pre></div></div> <p>At this point:</p> <ul> <li><code class="language-plaintext highlighter-rouge">origin/main</code> knows about the <strong>2 new commits</strong>.</li> <li><code class="language-plaintext highlighter-rouge">main</code> is <strong>2 commits behind</strong>.</li> </ul> <h2 id="step-4--merging-remote-changes">Step 4- Merging Remote Changes</h2> <p>To bring your local branch up to date:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git merge origin/main
</code></pre></div></div> <p>Output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Updating 453b2c9..e5c4de0
Fast-forward
 svm_classification.py | 125 +++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 125 insertions(+)
 create mode 100644 svm_classification.py
</code></pre></div></div> <p>Now:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">--graph</span> <span class="nt">--all</span>
<span class="k">*</span> e5c4de0 <span class="o">(</span>HEAD -&gt; main, origin/main<span class="o">)</span> Included Hyperparameter tuning
<span class="k">*</span> 86f6a84 Created svm_classification.py
<span class="k">*</span> 453b2c9 Rebasing Done with Updated Readme
...
</code></pre></div></div> <p>Your local <code class="language-plaintext highlighter-rouge">main</code> is synced with <code class="language-plaintext highlighter-rouge">origin/main</code>.</p> <h2 id="advantages-of-git-fetch">Advantages of <code class="language-plaintext highlighter-rouge">git fetch</code></h2> <ul> <li><strong>Safe</strong> – does not change your working branch.</li> <li><strong>Informative</strong> – lets you see remote changes before merging.</li> <li><strong>Collaboration-friendly</strong> – helps you stay aware of your teammates’ work.</li> <li><strong>Keeps remote-tracking branches up-to-date</strong>.</li> </ul> <h2 id="disadvantages--gotchas">Disadvantages / Gotchas</h2> <ul> <li><code class="language-plaintext highlighter-rouge">git fetch</code> alone does not update your local branch—you still need <code class="language-plaintext highlighter-rouge">merge</code> or <code class="language-plaintext highlighter-rouge">rebase</code>.</li> <li>Can cause confusion for beginners because <code class="language-plaintext highlighter-rouge">origin/main</code> is updated but <code class="language-plaintext highlighter-rouge">main</code> is not.</li> <li>Requires extra step compared to <code class="language-plaintext highlighter-rouge">git pull</code> (which does fetch + merge).</li> </ul> <h2 id="tips-for-users">Tips for Users</h2> <ul> <li><strong>Fetch daily</strong>: Run <code class="language-plaintext highlighter-rouge">git fetch</code> before starting your workday.</li> <li><strong>Fetch before pushing</strong>: Ensures you don’t accidentally push outdated work.</li> <li><strong>Inspect before merging</strong>: Use <code class="language-plaintext highlighter-rouge">git log origin/main</code> to review remote commits.</li> <li><strong>Combine with prune</strong>: <code class="language-plaintext highlighter-rouge">git fetch --prune</code> cleans up deleted remote branches.</li> </ul> <h3 id="key-takeaway">Key Takeaway</h3> <ul> <li><code class="language-plaintext highlighter-rouge">git fetch</code> updates your <strong>view of the remote repository</strong>.</li> <li>Your local branch remains unchanged until you explicitly merge/rebase.</li> <li>Think of it as “checking for mail” without opening the letters.</li> </ul> <h2 id="4-git-pull-and-pull-with-rebase">4. Git Pull and Pull with Rebase</h2> <p>When collaborating with others, keeping your local repository in sync with the remote repository is crucial. After all, you’re not the only one pushing changes!</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">git fetch</code></strong> brings your remote-tracking branches (like <code class="language-plaintext highlighter-rouge">origin/main</code>) up to date but does not touch your local working branch.</li> <li><strong><code class="language-plaintext highlighter-rouge">git pull</code></strong> goes further: it fetches changes <strong>and merges them into your local branch</strong>.</li> </ul> <p>In other words:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull = git fetch + git merge
</code></pre></div></div> <h2 id="step-1--basic-git-pull">Step 1- Basic Git Pull</h2> <p>Let’s say you are on <code class="language-plaintext highlighter-rouge">main</code> and new commits have appeared on the remote repository. Running:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull origin main
</code></pre></div></div> <ul> <li>Fetches the new commits from <code class="language-plaintext highlighter-rouge">origin/main</code>.</li> <li>Merges them into your local <code class="language-plaintext highlighter-rouge">main</code>.</li> <li>If there are no conflicts, you get a <strong>fast-forward merge</strong>.</li> <li> <p>If there are conflicts, Git will stop and ask you to resolve them.</p> </li> <li><strong>Advantage</strong>: quick and simple, your local branch matches remote.</li> <li><strong>Disadvantage</strong>: history may become cluttered with merge commits, especially when you pull frequently.</li> </ul> <h2 id="step-2--why-pull-with-rebase">Step 2- Why Pull with Rebase?</h2> <p>Sometimes merging is not the cleanest solution. If you are working on <strong>local changes</strong> but also want to integrate the latest updates from remote:</p> <ul> <li>A normal merge will produce a merge commit and mix histories.</li> <li>A rebase will <strong>reapply your local commits on top of the remote commits</strong>, producing a cleaner, linear history.</li> </ul> <p>The command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull <span class="nt">--rebase</span> origin main
</code></pre></div></div> <p>works like this:</p> <ol> <li><code class="language-plaintext highlighter-rouge">git fetch</code> → gets latest changes from <code class="language-plaintext highlighter-rouge">origin/main</code>.</li> <li><code class="language-plaintext highlighter-rouge">git rebase</code> → temporarily “removes” your local commits, applies the new commits from <code class="language-plaintext highlighter-rouge">origin/main</code>, then replays your local commits on top.</li> </ol> <h2 id="step-3--example-walkthrough">Step 3- Example Walkthrough</h2> <p>Before pulling, the commit history looks like this:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">--graph</span> <span class="nt">--all</span>
<span class="k">*</span> e5c4de0 <span class="o">(</span>HEAD -&gt; main, origin/main<span class="o">)</span> Included Hyperparameter tuning
<span class="k">*</span> 86f6a84 Created svm_classification.py
<span class="k">*</span> 453b2c9 Rebasing Done with Updated Readme
<span class="k">*</span> fca2f48 feature-branch:modified README.md File - README.md
<span class="k">*</span> 24d3a81 feature-branch: rebase <span class="k">in </span>progress - 404.html
<span class="k">*</span> 352a722 feature-branch: Rebasing the feature branch - 404.html
<span class="k">*</span> 9cbea38 main: modified Error Message again- 404.html
<span class="k">*</span> 84900fe main: modified title again- 404.html
...
</code></pre></div></div> <p>Two new commits (<code class="language-plaintext highlighter-rouge">Update 2 config.yaml</code>, <code class="language-plaintext highlighter-rouge">Updated 1 config.yaml</code>) exist on the remote but are not yet on local.</p> <p>Now run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull <span class="nt">--rebase</span> origin main
</code></pre></div></div> <p>Output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>From github.com:SurajBhar/advance_git
 * branch            main       -&gt; FETCH_HEAD
   e5c4de0..0535e79  main       -&gt; origin/main
Successfully rebased and updated refs/heads/main.
</code></pre></div></div> <h2 id="step-4--after-rebase">Step 4- After Rebase</h2> <p>Check the new commit history:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span> <span class="nt">--decorate</span> <span class="nt">--graph</span> <span class="nt">--all</span>
<span class="k">*</span> ebf7c5c <span class="o">(</span>HEAD -&gt; main<span class="o">)</span> main: modified readme - README.md
<span class="k">*</span> 0535e79 <span class="o">(</span>origin/main<span class="o">)</span> Update 2 config.yaml
<span class="k">*</span> d9b0764 Updated 1 config.yaml
<span class="k">*</span> e5c4de0 Included Hyperparameter tuning
<span class="k">*</span> 86f6a84 Created svm_classification.py
<span class="k">*</span> 453b2c9 Rebasing Done with Updated Readme
...
</code></pre></div></div> <p>Notice the difference:</p> <ul> <li>The remote commits (<code class="language-plaintext highlighter-rouge">d9b0764</code>, <code class="language-plaintext highlighter-rouge">0535e79</code>) were applied first.</li> <li>Your local commit (<code class="language-plaintext highlighter-rouge">ebf7c5c main: modified readme</code>) was <strong>rebased on top</strong>.</li> <li>The history is linear, without extra merge commits.</li> </ul> <h2 id="advantages-of-git-pull---rebase">Advantages of <code class="language-plaintext highlighter-rouge">git pull --rebase</code></h2> <ul> <li>Produces a <strong>clean, linear history</strong>.</li> <li>Easier to follow commit logs (<code class="language-plaintext highlighter-rouge">git log</code> looks tidy).</li> <li>Avoids unnecessary merge commits when working on small feature changes.</li> </ul> <h2 id="disadvantages--cautions">Disadvantages / Cautions</h2> <ul> <li>Can be risky on <strong>shared branches</strong> — rebasing rewrites history.</li> <li>Requires more caution when conflicts occur; you must resolve them commit by commit.</li> <li>For long-running feature branches, sometimes merge is the safer and more transparent option.</li> </ul> <h2 id="tips-for-using-pull-with-rebase">Tips for Using Pull with Rebase</h2> <ul> <li>Use <code class="language-plaintext highlighter-rouge">git pull --rebase</code> for personal feature branches or small sets of local commits.</li> <li>Avoid rebasing on shared/team branches unless everyone agrees to it.</li> <li>Set rebase as default for pulls:</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> pull.rebase <span class="nb">true</span>
</code></pre></div></div> <ul> <li>If conflicts occur, use:</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase <span class="nt">--continue</span>
git rebase <span class="nt">--abort</span>
</code></pre></div></div> <h3 id="key-takeaway-1">Key Takeaway</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">git pull</code></strong> = fetch + merge → quick but may clutter history.</li> <li><strong><code class="language-plaintext highlighter-rouge">git pull --rebase</code></strong> = fetch + rebase → cleaner history, local commits reapplied on top of remote.</li> <li>Choose <strong>merge</strong> for long-lived, collaborative branches and <strong>rebase</strong> for short-lived, personal branches where you want a tidy history.</li> </ul> <h2 id="5-git-reference-logs-reflog">5. Git Reference Logs (Reflog)</h2> <p>Git’s <strong>reference logs (reflog)</strong> act as a <strong>safety net</strong> for your repository. While <code class="language-plaintext highlighter-rouge">git log</code> shows the commits that are part of your branch’s history, <code class="language-plaintext highlighter-rouge">git reflog</code> records <strong>all changes to the tips of branches and other references</strong>—even if those commits don’t appear in the visible history anymore.</p> <p>Think of the reflog as a <strong>personal diary of your Git actions</strong>, maintained locally. Every time you:</p> <ul> <li>Checkout a branch</li> <li>Commit changes</li> <li>Reset or rebase</li> <li>Merge or pull</li> </ul> <p>…the <code class="language-plaintext highlighter-rouge">HEAD</code> reference is updated, and the reflog records it.</p> <h2 id="step-1--viewing-the-reflog">Step 1- Viewing the Reflog</h2> <p>Run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reflog
</code></pre></div></div> <p>Example output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ebf7c5c (HEAD -&gt; main, origin/main) HEAD@{0}: pull --rebase origin main (finish): returning to refs/heads/main
ebf7c5c (HEAD -&gt; main, origin/main) HEAD@{1}: pull --rebase origin main (pick): main: modified readme - README.md
0535e79 HEAD@{2}: pull --rebase origin main (start): checkout 0535e79...
e7d0f19 HEAD@{3}: commit: main: modified readme - README.md
e5c4de0 HEAD@{4}: merge origin/main: Fast-forward
453b2c9 HEAD@{5}: commit: Rebasing Done with Updated Readme
...
</code></pre></div></div> <p>Here’s what’s happening:</p> <ul> <li><code class="language-plaintext highlighter-rouge">HEAD@{0}</code> – The most recent action. In this case, completing a pull with rebase.</li> <li><code class="language-plaintext highlighter-rouge">HEAD@{3}</code> – A commit made earlier (<code class="language-plaintext highlighter-rouge">main: modified readme</code>).</li> <li><code class="language-plaintext highlighter-rouge">HEAD@{5}</code> – A commit you thought might be “lost” if you reset later.</li> </ul> <p>Unlike <code class="language-plaintext highlighter-rouge">git log</code>, these entries <strong>remain visible even after resets or rebases</strong>.</p> <h2 id="step-2--why-reflog-matters">Step 2- Why Reflog Matters</h2> <p>Let’s say you reset a branch or made changes you regret.</p> <ul> <li><code class="language-plaintext highlighter-rouge">git log</code> may no longer show those commits (they are detached from the branch).</li> <li>But reflog keeps them recorded, allowing you to <strong>recover lost commits</strong>.</li> </ul> <p>That’s why reflog is Git’s <strong>time machine</strong>.</p> <h2 id="step-3--inspecting-historical-states">Step 3- Inspecting Historical States</h2> <p>You can inspect any past state of the repository with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git show HEAD@<span class="o">{</span>n<span class="o">}</span>
</code></pre></div></div> <p>Example:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git show HEAD@<span class="o">{</span>29<span class="o">}</span>
</code></pre></div></div> <p>Output shows the very <strong>first commit</strong>:</p> <div class="language-diff highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">commit f86213de...
Author: Suraj Bhardwaj
Date:   Thu Sep 11 17:45:43 2025 +0200
</span><span class="err">
</span>    first commit
</code></pre></div></div> <p>You can also query reflog by <strong>time references</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git show main@<span class="o">{</span>1.hour.ago<span class="o">}</span>
git show main@<span class="o">{</span>yesterday<span class="o">}</span>
git show main@<span class="o">{</span>1.week.ago<span class="o">}</span>
</code></pre></div></div> <p>Example:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git show main@<span class="o">{</span>1.hour.ago<span class="o">}</span>
</code></pre></div></div> <p>Output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>commit e5c4de0...
Author: Suraj Bhardwaj
Date:   Fri Sep 12 10:29:35 2025 +0200

    Included Hyperparameter tuning
</code></pre></div></div> <h2 id="step-4--using-reflog-to-recover-from-mistakes">Step 4- Using Reflog to Recover from Mistakes</h2> <h3 id="scenario-hard-reset">Scenario: Hard Reset</h3> <p>Suppose you ran:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reset <span class="nt">--hard</span> bee45fc
</code></pre></div></div> <p>This moved your branch pointer back to an older commit. At first glance, it looks like you lost your newer commits. But reflog recorded them:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reflog
bee45fc <span class="o">(</span>HEAD -&gt; main<span class="o">)</span> HEAD@<span class="o">{</span>0<span class="o">}</span>: reset: moving to bee45fc
ebf7c5c <span class="o">(</span>origin/main<span class="o">)</span> HEAD@<span class="o">{</span>1<span class="o">}</span>: reset: moving to ebf7c5c
bee45fc <span class="o">(</span>HEAD -&gt; main<span class="o">)</span> HEAD@<span class="o">{</span>2<span class="o">}</span>: commit: main: deleted server info - config.yaml
...
</code></pre></div></div> <p>You can still recover the commits via their reflog entries.</p> <h3 id="scenario--restoring-lost-commits">Scenario- Restoring Lost Commits</h3> <p>You decide those commits weren’t mistakes after all. To bring them back:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Checkout an old reflog entry in detached HEAD state</span>
git checkout HEAD@<span class="o">{</span>2<span class="o">}</span>

<span class="c"># Create a new branch to save those commits</span>
git checkout <span class="nt">-b</span> restore-branch

<span class="c"># Merge back into main</span>
git checkout main
git merge restore-branch

<span class="c"># Delete the temporary branch</span>
git branch <span class="nt">-d</span> restore-branch
</code></pre></div></div> <p>Now your “lost” commits are restored into your main history.</p> <h2 id="step-5--where-reflog-data-lives">Step 5- Where Reflog Data Lives</h2> <ul> <li>Reflog entries are stored inside <code class="language-plaintext highlighter-rouge">.git/logs/</code></li> <li>They are <strong>local only</strong> – other collaborators cannot see your reflog.</li> <li>Git garbage collection (<code class="language-plaintext highlighter-rouge">git gc</code>) may eventually clean up very old reflog entries.</li> </ul> <h2 id="step-6--cheat-sheet">Step 6- Cheat Sheet</h2> <p>Here’s a quick summary of useful reflog commands:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Show reflog for HEAD (all actions)</span>
git reflog

<span class="c"># Show reflog for a specific branch</span>
git reflog show main

<span class="c"># Show commit at a specific reflog entry</span>
git show HEAD@<span class="o">{</span>5<span class="o">}</span>

<span class="c"># Checkout an old state</span>
git checkout HEAD@<span class="o">{</span>10<span class="o">}</span>

<span class="c"># Recover work by branching from a reflog entry</span>
git checkout <span class="nt">-b</span> restore-branch HEAD@<span class="o">{</span>8<span class="o">}</span>

<span class="c"># Show history by time references</span>
git show main@<span class="o">{</span>1.hour.ago<span class="o">}</span>
git show main@<span class="o">{</span>yesterday<span class="o">}</span>

<span class="c"># Diff between old state and current</span>
git diff @<span class="o">{</span>1.hour.ago<span class="o">}</span>

<span class="c"># Show reflog in log format</span>
git log <span class="nt">-g</span>
</code></pre></div></div> <h2 id="key-takeaways-1">Key Takeaways</h2> <ul> <li><strong>Reflog is your safety net</strong>: it tracks all branch tip movements.</li> <li>You can <strong>restore commits</strong> even after resets, rebases, and amends.</li> <li>It is <strong>local-only</strong> – don’t expect teammates to see your reflog.</li> <li>Always check reflog when you think “I lost my commits!”</li> </ul> <h2 id="final-thoughts">Final Thoughts</h2> <p>In real-world projects, Git is more than just a tool for saving code — it’s the foundation of teamwork and clean collaboration.</p> <p>The commands we covered — <strong>amending commits, rebasing, fetching, pulling with rebase, and using reflog</strong> — are the safety nets and precision tools that keep your history tidy and your team in sync. They also give you confidence that no mistake is truly permanent, since Git always keeps a trace.</p> <p>As an AI/ML Engineer, I’ve seen how messy histories and merge chaos can slow teams down. Mastering these advanced commands makes a big difference: your commits stay meaningful, your branches merge cleanly, and your teammates trust the process more.</p> <p>Next time you’re unsure about a Git situation, take a step back:</p> <ul> <li><strong>Amend</strong> to fix local mistakes.</li> <li><strong>Rebase</strong> to replay history cleanly.</li> <li><strong>Fetch</strong> to stay aware without changing your branch.</li> <li><strong>Pull with rebase</strong> for cleaner integration.</li> <li><strong>Reflog</strong> when you think all hope is lost.</li> </ul> <p>Keep practicing these on side projects — they’ll become second nature. And once they do, you’ll find that Git feels less like a source of stress and more like a true partner in your development workflow.</p> <h2 id="whats-next">What’s Next?</h2> <p>In the next post, I’ll explore <strong>Git Stash</strong> — a lifesaver for juggling multiple tasks in real-world AI/ML projects.</p> <p>You can also check out the <a href="https://git-scm.com/doc">official Git documentation</a> for deeper details.</p>]]></content><author><name>Suraj Bhardwaj</name></author><category term="version-control"/><summary type="html"><![CDATA[A practical guide to advanced Git workflows — rewriting history, rebasing, fetching, pulling with rebase, and using reflog to recover lost commits.]]></summary></entry><entry><title type="html">Git Stash, Practically: How to keep Teams Moving Without Losing Work</title><link href="https://surajbhar.github.io/blog/2025/09/git-stash-practically-how-to-keep-teams-moving-without-losing-work/" rel="alternate" type="text/html" title="Git Stash, Practically: How to keep Teams Moving Without Losing Work"/><published>2025-09-15T13:49:10+00:00</published><updated>2025-09-15T13:49:10+00:00</updated><id>https://surajbhar.github.io/blog/2025/09/git-stash-practically-how-to-keep-teams-moving-without-losing-work</id><content type="html" xml:base="https://surajbhar.github.io/blog/2025/09/git-stash-practically-how-to-keep-teams-moving-without-losing-work/"><![CDATA[<p>In real projects, the “urgent hotfix” arrives exactly when you’re mid-feature with half-written code and tests open. You need a clean branch immediately, but you don’t want to toss your work or make a noisy WIP commit. That’s where <strong>git stash</strong> shines: it takes a snapshot of your <strong>working tree</strong> and (optionally) your <strong>staging area (index)</strong>, parks it safely, and resets your tree to HEAD so you can switch context without risk.</p> <p>Below I guide you through the exact flows I use on team projects: cleanly pausing work, jumping to hotfixes, restoring the <strong>exact</strong> state (including what was staged), moving a stash into its own branch, and a few creative tricks that make stash feel like a superpower. I’ll show the classic commands (as they appeared in my terminal) and note the <strong>modern equivalents</strong> that read better and help teammates.</p> <blockquote><em>Reference while you read: the official docs are excellent and precise — keep them open: </em><a href="https://git-scm.com/docs/git-stash"><strong><em>git-scm.com/docs/git-stash</em></strong></a><em>.</em></blockquote> <h3>1) A Clear Mental Model</h3> <p>A <strong>stash</strong> is a pair of snapshots:</p> <ul><li>one for the <strong>index</strong> (what was staged),</li><li>one for the <strong>working directory</strong> (what was modified but not staged).</li></ul> <p>By default, stash captures <strong>tracked files</strong>. Add -u/--include-untracked to include new, untracked files (and --all if you also want ignored files). Stashes live in a local <strong>stack</strong>: newest is stash@{0}, then stash@{1}, and so on. They’re <strong>local</strong> — not pushed to remotes — and that’s intentional.</p> <blockquote><em>Modern tip: </em><em>git stash push -m &quot;message&quot; is the current, explicit form. Calling just </em><em>git stash still works (and is equivalent to </em><em>stash push), but I prefer </em><em>push -m for clarity in teams.</em></blockquote> <h3>2) Parking Work to Ship a Hotfix (The Everyday Case)</h3> <p><strong>Situation:</strong> On main, I’ve edited blog.html. Pager goes off. We need a hotfix.</p> <p><strong>What I did (classic):</strong></p> <pre>git status<br /># shows modified: blog.html<br />git stash<br /># Saved working directory and index state WIP on main: &lt;…&gt;<br />git stash list<br /># stash@{0}: WIP on main: &lt;…&gt;  (newest on top)</pre> <p><strong>Why:</strong> Stashing gives me a clean working tree so I can branch for the hotfix without dragging along my WIP.</p> <p><strong>Modern equivalent:</strong></p> <pre>git stash push -m &quot;WIP: blog.html edits on main&quot;</pre> <p>Now I branch, fix, and commit the hotfix:</p> <p><strong>Classic:</strong></p> <pre>git checkout -b hotfix<br />git commit -am &quot;hotfix: updated hello world - hello_world.py&quot;</pre> <p><strong>Modern:</strong></p> <pre>git switch -c hotfix<br /># …fix…<br />git commit -am &quot;hotfix: updated hello world - hello_world.py&quot;</pre> <p>Back on main, I had a quick tweak to 404.html. I stashed it <strong>with a message</strong> (this matters once you have multiple stashes):</p> <p><strong>Classic:</strong></p> <pre>git stash save &quot;modified the title - 404.html&quot;<br />git stash list<br /># stash@{0}: On main: modified the title - 404.html<br /># stash@{1}: WIP on main: &lt;…&gt;</pre> <p><strong>Modern:</strong></p> <pre>git stash push -m &quot;modified the title - 404.html&quot;</pre> <p><strong>Team reality:</strong> When multiple engineers are hopping branches, labeled stashes act like a <em>to-do stack</em> you can understand at a glance.</p> <h3>3) “What’s Inside This Stash?” (Choosing the Right One)</h3> <p>Sometimes I need to inspect an older, unlabeled stash to see what it changes:</p> <pre>git stash show stash@{2}<br /># blog.html | 2 +-<br /># 1 file changed, 1 insertion(+), 1 deletion(-)</pre> <p>Add -p to view the patch:</p> <pre>git stash show -p stash@{2}</pre> <p><strong>Why:</strong> I don’t want to apply/pop a random stash and create conflicts; I want the right one, first time.</p> <h3>4) Untracked Files: Why “No local changes to save” Happens</h3> <p>You’ll hit this sooner than you think. I added a brand-new newfile.txt, then:</p> <pre>git stash<br /># No local changes to save</pre> <p>Because it’s <strong>untracked</strong>, plain stash ignored it. The fix:</p> <p><strong>Classic:</strong></p> <pre>git stash -u</pre> <p><strong>Modern:</strong></p> <pre>git stash push -u -m &quot;Include untracked: newfile.txt&quot;<br /># or include ignored too:<br />git stash push --all -m &quot;Include untracked+ignored&quot;</pre> <p><strong>Team scenario:</strong> Spikes, throwaway scripts, notes — they’re often untracked. If you want to park them, remember -u (or --all).</p> <h3>5) Apply vs Pop vs Drop (Restoring Intentionally)</h3> <p>I typically <strong>apply</strong> when I might reuse the stash and <strong>pop</strong> when I’m done with it:</p> <pre>git stash apply           # restore, but keep in the stack<br />git stash pop stash@{1}   # restore and remove that entry if apply succeeds<br />git stash drop stash@{2}  # remove a specific entry unconditionally<br />git stash clear           # blow away all stashes (irreversible)</pre> <blockquote><em>Conflict note: If </em><em>apply/</em><em>pop hits conflicts, Git leaves the stash in place. Resolve files, stage (</em><em>git add), then drop or pop again as needed.</em></blockquote> <p><strong>Team scenario:</strong> After a hotfix merges, I apply the relevant stash back on main, finalize the change, and drop the stash so the stack doesn’t grow stale.</p> <h3>6) Exact Restoration: Don’t Lose Your Staging Plan</h3> <p>This is the most common gotcha. Say I had newfile.txt <strong>staged</strong> and newfile2.txt <strong>unstaged</strong>, then I stashed. A plain apply brings everything back <strong>unstaged</strong>, which is not what I want when I’m composing clean, small commits.</p> <p>Use <strong>--index</strong> to restore the index snapshot too:</p> <pre>git stash apply --index<br /># or<br />git stash pop --index</pre> <p><strong>Why:</strong> A stash stores two trees (index + working). --index restores both. In team reviews, this helps you keep tight commit boundaries you planned before the interruption.</p> <h3>7) Keep Staged, Stash Only the Rest (--keep-index)</h3> <p>Sometimes I want to <strong>keep</strong> my staged changes ready for an immediate commit and hide only the other, noisy edits:</p> <p><strong>Classic:</strong></p> <pre>git stash --keep-index</pre> <p><strong>Modern:</strong></p> <pre>git stash push --keep-index -m &quot;Stash only unstaged; keep staged intact&quot;</pre> <p><strong>Team scenario:</strong> You’re splitting work into multiple logical commits; stash the unfinished hunks and ship the one that’s ready.</p> <p><em>Bonus precision:</em></p> <pre>git stash push -- path/to/fileA dirB/          # stash only these paths<br />git stash push -p -m &quot;Interactive patch stash&quot; # pick specific hunks<br />git stash push --staged -m &quot;Only staged changes to stash</pre> <h3>8) When a Stash Deserves Its Own Branch</h3> <p>Large stashes are harder to reason about and review. I convert them into a branch:</p> <pre>git stash branch demobranch           # uses latest stash by default<br /># or explicitly:<br />git stash branch demobranch stash@{0}</pre> <p>This does four things for you:</p> <ol><li>creates the branch at the commit where the stash was made,</li><li>checks it out,</li><li>applies the stash (so you see your changes as edits),</li><li>drops that stash entry on success.</li></ol> <p>From there, I commit normally, open a PR, run CI, get reviews — in short, I make the work <strong>first-class history</strong> instead of a blob hidden in my stash stack.</p> <p><strong>Team scenario:</strong> If apply keeps conflicting on main because the base moved, stash branch applies on the <strong>original base</strong> — far fewer conflicts.</p> <h3>9) File Undo: Prefer restore Over Legacy checkout --</h3> <p>You’ll see old docs (and muscle memory) using git checkout -- &lt;path&gt; to discard a file. The intent is clearer with:</p> <pre>git restore &lt;path&gt;          # discard working changes to that file<br />git restore --staged &lt;path&gt; # unstage but keep edits</pre> <p>I’ve found teammates new to Git make fewer mistakes when we standardize on switch (for branches) and restore (for files).</p> <h3>10) Putting It Together: A Smooth, Team-Friendly Flow</h3> <p>Here’s a tidy day-in-the-life using modern forms:</p> <pre># On main, mid-feature:<br />git status<br />git stash push -m &quot;WIP: blog.html edits&quot;<br />git switch -c hotfix<br /># ... fix, run tests ...<br />git commit -am &quot;hotfix: correct 500 message&quot;<br />git switch main<br />git stash push -m &quot;WIP: 404 title tweak&quot;          # label each stash<br /># Include a new untracked scratch file<br />git stash push -u -m &quot;Spike: newfile.txt&quot;<br /># Later: restore EXACT staged/unstaged state<br />git stash apply --index<br /># Decide this belongs on its own branch:<br />git stash branch demobranch stash@{1}<br /># ... commit in small pieces, open PR, get review ...</pre> <p>This sequence plays nicely with CI, code review, and release trains — the things that truly matter on a team.</p> <h3>11) Why I Reach for Stash (and When I Don’t)</h3> <ul><li><strong>Hotfix interrupts:</strong> Park feature work instantly; fix prod; return with apply --index.</li><li><strong>Pre-rebase safety:</strong> Stash uncommitted edits before rebases or large pulls.</li><li><strong>Partial commits:</strong> Use --keep-index or -p to keep your commits small and meaningful.</li><li><strong>Spikes &amp; notes:</strong> Include untracked files with -u so you don’t lose context.</li><li><strong>Promotion to history:</strong> When a stash grows, convert with stash branch and finish via PR.</li></ul> <p>I avoid using stash as a long-term drawer. It’s local, easy to forget, and invisible to your teammates. If it’s important, promote it to a branch and make proper commits.</p> <h3>12) Quick Command Map (Classic → Modern)</h3> <ul><li>Create a labeled stash<br/> <strong>Classic:</strong> git stash save &quot;message&quot;<br/> <strong>Modern:</strong> git stash push -m &quot;message&quot;</li><li>Include untracked / ignored<br/> <strong>Classic:</strong> git stash -u / git stash --all<br/> <strong>Modern:</strong> git stash push -u -m &quot;…&quot;, git stash push --all -m &quot;…&quot;</li><li>Keep staged, stash only the rest<br/> <strong>Classic:</strong> git stash --keep-index<br/> <strong>Modern:</strong> git stash push --keep-index -m &quot;…&quot;</li><li>Restore exact index + working tree<br/> <strong>Classic/Modern:</strong> git stash apply --index / git stash pop --index</li><li>Inspect a stash<br/> <strong>Classic/Modern:</strong> git stash show [-p] stash@{n}</li><li>Make a branch from a stash<br/> <strong>Classic/Modern:</strong> git stash branch &lt;name&gt; [stash@{n}]</li><li>Safer branch and file ops<br/> <strong>Modern:</strong> git switch ([-c] &lt;name&gt;), git restore [--staged] &lt;path&gt;</li></ul> <h3>13) Final Notes &amp; Doc Link</h3> <p>git stash isn’t just a panic button. Used deliberately — with messages, --index, --keep-index, -u, and stash branch — it keeps your flow intact <strong>and</strong> your team’s history clean. Keep the official documentation close (it covers advanced switches like --patch, --staged, export/import, and configuration toggles): <strong>Official docs:</strong> <a href="https://git-scm.com/docs/git-stash"><strong>git-scm.com/docs/git-stash</strong></a><strong>.</strong></p> <p>If you adopt the modern forms (stash push, switch, restore) and the habits above, you’ll move between urgent tasks and deep work without losing a minute — or a single carefully staged hunk.</p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=affcbcd0053f" width="1" height="1" alt=""/></p>]]></content><author><name></name></author></entry><entry><title type="html">Mastering Advanced Git: Amending, Rebasing, Reflog, and Remote Workflows</title><link href="https://surajbhar.github.io/blog/2025/09/mastering-advanced-git-amending-rebasing-reflog-and-remote-workflows/" rel="alternate" type="text/html" title="Mastering Advanced Git: Amending, Rebasing, Reflog, and Remote Workflows"/><published>2025-09-12T13:57:45+00:00</published><updated>2025-09-12T13:57:45+00:00</updated><id>https://surajbhar.github.io/blog/2025/09/mastering-advanced-git-amending-rebasing-reflog-and-remote-workflows</id><content type="html" xml:base="https://surajbhar.github.io/blog/2025/09/mastering-advanced-git-amending-rebasing-reflog-and-remote-workflows/"><![CDATA[<p>Version control with Git starts simple — add, commit, push. But the deeper you go into real-world projects, the more complicated collaboration becomes. You make commits you regret, forget to include files, end up with messy histories, or lose work after a reset.</p> <p>At this point, basic Git isn’t enough. You need to know its advanced features — the ones that allow you to rewrite history, squash commits, recover lost work, and synchronize branches without cluttering your logs.</p> <p>This blog is divided into four detailed sections, each covering one advanced area of Git with <strong>step-by-step commands, real outputs, and practical tips</strong>:</p> <ol><li><strong>Commit History Rewriting (Amend &amp; Interactive Rebase)</strong></li><li><strong>Branch Rebase (Linearizing History)</strong></li><li><strong>Remote Workflows (Fetch, Pull, and Pull with Rebase)</strong></li><li><strong>Recovering Work with Reflog</strong></li></ol> <p>By the end, you’ll not only understand <em>how</em> these commands work but also <em>when</em> and <em>why</em> to use them in collaborative projects.</p> <h3>Section 1: Commit History Rewriting</h3> <p>One of the most powerful aspects of Git is the ability to <strong>rewrite commit history</strong>. While this can be dangerous if misused, it’s invaluable for cleaning up mistakes, improving readability, and polishing your repository before sharing.</p> <h3>1.1 Amending Commits</h3> <p>Amending allows you to <strong>update the last commit</strong> instead of creating a new one. This is useful if you forgot to add a file, want to tweak a commit message, or need to combine corrections into the most recent commit.</p> <p>The key point: amending <strong>rewrites history by generating a new commit SHA</strong>. That means you should amend only commits that haven’t been pushed yet.</p> <h4>Example: Adding a File to the Last Commit</h4> <p>Let’s say your log looks like this:</p> <pre>git log --oneline<br />0832375 (HEAD -&gt; main, origin/main) HTML File<br />f86213d first commit</pre> <p>You commit a change to blog.html:</p> <pre>git commit -am &quot;main: modified the title - blog.html&quot;<br />[main ab40261] main: modified the title - blog.html<br /> 2 files changed, 9 insertions(+), 1 deletion(-)<br /><br />git log --oneline<br />ab40261 (HEAD -&gt; main) main: modified the title - blog.html<br />0832375 (origin/main) HTML File<br />f86213d first commit</pre> <p>But now you realize that you also need to include 404.html in the same commit.</p> <pre>git add 404.html<br />git commit --amend --no-edit<br /><br />[main 4bf9f69] main: modified the title - blog.html<br /> Date: Thu Sep 11 20:15:36 2025 +0200<br /> 3 files changed, 60 insertions(+), 1 deletion(-)<br /> create mode 100644 404.html</pre> <p>Notice how the SHA changed from ab40261 to 4bf9f69. The old commit is gone; the new one includes both blog.html and 404.html.</p> <ul><li>--no-edit keeps the same commit message.</li><li>If the commit was already pushed, this change would require a force push.</li></ul> <h4>Example: Amending Only the Message</h4> <p>If you just want to fix the commit message:</p> <pre>git commit --amend -m &quot;main: updated blog.html and added 404.html&quot;</pre> <p>Or open your editor:</p> <pre>git commit --amend</pre> <p>This replaces the message while leaving the content intact.</p> <p><strong>Safe Rule of Thumb</strong>:</p> <ul><li>Amend local commits freely.</li><li>Avoid amending pushed commits unless you know how to force push safely.</li></ul> <h3>1.2 Interactive Rebase for Squashing</h3> <p>Sometimes you make multiple small commits that should really be one. For example:</p> <pre>git log --oneline<br />a1b2c3d Fix typo<br />d4e5f6g Update config<br />h7i8j9k Add feature</pre> <p>Instead of merging these as-is, you can squash them with interactive rebase:</p> <pre>git rebase -i HEAD~3</pre> <p>This opens a text editor:</p> <pre>pick h7i8j9k Add feature<br />pick d4e5f6g Update config<br />pick a1b2c3d Fix typo</pre> <p>Change the last two pick commands to squash:</p> <pre>pick h7i8j9k Add feature<br />squash d4e5f6g Update config<br />squash a1b2c3d Fix typo</pre> <p>After saving, Git combines them into one commit. The result is a cleaner log that highlights the feature rather than the noise of small corrections.</p> <p><strong>When to use</strong>: Always squash before merging a feature branch into main if your commits are too fragmented.</p> <h3>Section 1: Summary</h3> <ul><li><strong>Amend</strong>: Fix or extend the most recent commit.</li><li><strong>Interactive rebase</strong>: Squash multiple commits into one.</li><li>Both commands <strong>rewrite history</strong>, so they should be used with caution on shared branches.</li></ul> <h3>Section 2: Git Rebase — Keeping History Clean</h3> <p>If there’s one Git command that developers both love and fear, it’s rebase. At its core, rebasing means taking your commits and moving them onto a new base commit. Instead of merging, which creates a new “merge commit,” rebasing replays your commits as if they were made directly on top of the latest branch.</p> <p>The result? A <strong>linear history</strong> that is easier to read and follow.</p> <h3>2.1 Rebase vs Merge</h3> <p>Both merge and rebase solve the same problem: they integrate changes from one branch into another. But the way they do it creates very different histories.</p> <p><strong>Merge</strong>: Adds a new merge commit. History keeps all the branches and merges.</p> <ul><li>Pro: Preserves the full timeline of how work happened.</li><li>Con: History can get messy with lots of “Merge branch …” commits.</li></ul> <p><strong>Rebase</strong>: Moves your commits to the tip of another branch. Creates a straight line of commits.</p> <ul><li>Pro: History looks clean and linear.</li><li>Con: Rewrites history, which can be dangerous if not used carefully.</li></ul> <p>Think of merge as “recording exactly how the work happened” and rebase as “presenting the history as if it all happened in order.”</p> <h3>2.2 Performing a Basic Rebase</h3> <p>A simple rebase looks like this:</p> <pre>git checkout feature<br />git rebase main</pre> <p>This tells Git: “Take the commits from the feature branch and reapply them on top of the latest commit from main.”</p> <p>If there are no conflicts, the rebase finishes automatically.</p> <h3>2.3 Handling Rebase Conflicts (Step-by-Step Example)</h3> <p>Rebases often trigger conflicts when both branches have modified the same lines. Let’s simulate a real-world scenario.</p> <h4>Step 1: Create a feature branch</h4> <pre>git checkout -b feature-branch</pre> <p>Output:</p> <pre>Switched to a new branch &#39;feature-branch&#39;</pre> <p>Both main and feature-branch currently point to the same commit.</p> <h4>Step 2: Add commits to the feature branch</h4> <pre>git commit -am &quot;feature-branch: modified title - 404.html&quot;<br />[feature-branch 82ff182] feature-branch: modified title - 404.html<br /><br />git commit -am &quot;feature-branch: modified Error Message - 404.html&quot;<br />[feature-branch e7f8a2e] feature-branch: modified Error Message - 404.html</pre> <p>Now feature-branch has <strong>two unique commits</strong>.</p> <h4>Step 3: Add conflicting commits to main</h4> <pre>git checkout main<br />git commit -am &quot;main: modified title again - 404.html&quot;<br />[main 84900fe] main: modified title again - 404.html<br /><br />git commit -am &quot;main: modified Error Message again - 404.html&quot;<br />[main 9cbea38] main: modified Error Message again - 404.html</pre> <p>Visual log:</p> <pre>* 9cbea38 (main) main: modified Error Message again - 404.html<br />* 84900fe main: modified title again - 404.html<br />| * e7f8a2e (feature-branch) feature-branch: modified Error Message - 404.html<br />| * 82ff182 feature-branch: modified title - 404.html<br />|/</pre> <p>Both branches have diverged.</p> <h4>Step 4: Start the rebase</h4> <pre>git checkout feature-branch<br />git rebase main</pre> <p>Git attempts to replay the commits but hits a conflict:</p> <pre>Auto-merging 404.html<br />CONFLICT (content): Merge conflict in 404.html<br />error: could not apply 82ff182...</pre> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*JVnuaXqHUV53I3OrfbZjcg.png"/></figure> <h4>Step 5: Resolve the first conflict</h4> <pre>git mergetool</pre> <p>Output:</p> <pre>Merging:<br />404.html<br /><br />Normal merge conflict for &#39;404.html&#39;:<br />  {local}: modified file<br />  {remote}: modified file</pre> <p>Manually fix the conflict, then:</p> <pre>git add 404.html<br />git rebase --continue</pre> <h4>Step 6: Resolve the second conflict</h4> <p>Git applies the next commit, but another conflict occurs. Fix the file again, then:</p> <pre>git add 404.html<br />git rebase --continue</pre> <p>Now the rebase completes successfully.</p> <p>Output:</p> <pre>Successfully rebased and updated refs/heads/feature-branch.</pre> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*fchXhJhrBW3OcF6BfNGCBA.png"/></figure> <h4>Step 7: Verify history</h4> <pre>git log --oneline --decorate --graph --all</pre> <p>Output:</p> <pre>* 24d3a81 (HEAD -&gt; feature-branch) feature-branch changes<br />* 9cbea38 (main) main changes<br />* 84900fe main changes</pre> <p>The feature-branch commits are now sitting <strong>on top of main</strong>, giving a clean, linear history.</p> <h4>Step 8: Fast-forward merge</h4> <p>Since the branch is rebased, merging it into main is now a simple fast-forward:</p> <pre>git checkout main<br />git merge feature-branch</pre> <p>Output:</p> <pre>Updating 9cbea38..24d3a81<br />Fast-forward<br /> 404.html  |  4 ++--</pre> <h3>2.4 Handy Rebase Commands</h3> <ul><li>Abort a rebase:</li></ul> <pre>git rebase --abort</pre> <ul><li>Skip a commit:</li></ul> <pre>git rebase --skip</pre> <ul><li>Continue after fixing conflicts:</li></ul> <pre>git rebase --continue</pre> <h3>Section 2: Summary</h3> <ul><li><strong>Rebase</strong> replays commits on top of another branch, producing a linear history.</li><li>It’s best used on <strong>feature branches before merging into main</strong>.</li><li>Rebasing often requires resolving conflicts step by step.</li><li>Use merge when you need the full “record of history,” and rebase when you want a <strong>clean presentation of history</strong>.</li></ul> <h3>Section 3: Working with Remote Branches</h3> <p>In collaborative projects, your local repository is just one copy of the truth. The actual “source of truth” often lives in a remote repository such as GitHub, GitLab, or Bitbucket. While you are busy committing locally, teammates may push their own changes. Without synchronization, your main branch risks falling behind, which can lead to merge conflicts or overwritten work.</p> <p>Git provides three key commands to manage this synchronization: <strong>git fetch</strong>, <strong>git pull</strong>, and <strong>git pull --rebase</strong>. Although they may seem similar at first glance, they each serve distinct purposes and have different effects on history.</p> <h3>3.1 Git Fetch — Updating Remote Awareness</h3> <p>git fetch updates your local knowledge of the remote repository without touching your working branch.</p> <pre>git fetch origin</pre> <p>This downloads new commits and updates the pointer of origin/main, but your main branch remains unchanged. It’s like downloading the latest state of the remote into your inbox without applying the changes.</p> <h4>Practical Example: Detecting Divergence</h4> <p>Check all branches:</p> <pre>git branch -av</pre> <p>Output:</p> <pre>* main                453b2c9 Rebasing Done with Updated Readme<br />  remotes/origin/main 453b2c9 Rebasing Done with Updated Readme</pre> <p>At this point, both branches are in sync.</p> <p>Suppose teammates push two new commits (Included Hyperparameter tuning and Created svm_classification.py). Your local branch is now behind. Run:</p> <pre>git fetch origin main</pre> <p>Output:</p> <pre>From github.com:SurajBhar/advance_git<br /> * branch            main       -&gt; FETCH_HEAD<br />   453b2c9..e5c4de0  main       -&gt; origin/main</pre> <p>Now origin/main is updated, but main is still old.</p> <p>Compare logs:</p> <pre>git log --oneline main<br />453b2c9 Rebasing Done with Updated Readme</pre> <pre>git log --oneline origin/main<br />e5c4de0 (origin/main) Included Hyperparameter tuning<br />86f6a84 Created svm_classification.py<br />453b2c9 Rebasing Done with Updated Readme</pre> <p>Finally, fast-forward main to sync:</p> <pre>git merge origin/main</pre> <p>Output:</p> <pre>Updating 453b2c9..e5c4de0<br />Fast-forward<br /> svm_classification.py | 125 +++++++++++++++++++++++++++++++++++++++++++++<br /> 1 file changed, 125 insertions(+)<br /> create mode 100644 svm_classification.py</pre> <p>Now main and origin/main point to the same commit again.</p> <h3>3.2 Git Pull — Fetch + Merge</h3> <p>If you want Git to both fetch and integrate in one step, use:</p> <pre>git pull origin main</pre> <p>This updates origin/main and immediately merges those changes into your local branch.</p> <ul><li>If the local branch has no new commits, Git performs a <strong>fast-forward</strong>.</li><li>If both local and remote have diverged, Git creates a <strong>merge commit</strong>.</li></ul> <h4>Example: Simple Pull</h4> <pre>git pull origin main</pre> <p>Output (fast-forward case):</p> <pre>Updating e5c4de0..0535e79<br />Fast-forward<br /> config.yaml | 10 +++++-----</pre> <p>Now both main and origin/main share the same latest commit.</p> <p>The downside? If you pull often while also committing locally, you may accumulate many “merge commits” in history that don’t add value.</p> <h3>3.3 Git Pull with Rebase — Cleaner History</h3> <p>Sometimes you want the latest remote changes, but you also want to keep your history <strong>linear</strong> — as if your commits came after the remote commits.</p> <p>That’s where rebase comes in:</p> <pre>git pull --rebase origin main</pre> <p>This sequence happens:</p> <ol><li>Fetch updates from origin/main.</li><li>Temporarily remove your local commits.</li><li>Apply the new remote commits.</li><li>Replay your local commits on top.</li></ol> <h4>Example: Rebasing Local Commits on Top of Remote</h4> <p>Before pull:</p> <pre>git log --oneline --decorate --graph --all<br />* ebf7c5c (HEAD -&gt; main) main: modified readme - README.md<br />* e5c4de0 (origin/main) Included Hyperparameter tuning<br />* 86f6a84 Created svm_classification.py<br />* 453b2c9 Rebasing Done with Updated Readme</pre> <p>Now, teammates push two more commits:</p> <ul><li>Updated 1 config.yaml</li><li>Update 2 config.yaml</li></ul> <p>Run:</p> <pre>git pull --rebase origin main</pre> <p>Output:</p> <pre>From github.com:SurajBhar/advance_git<br /> * branch            main       -&gt; FETCH_HEAD<br />   e5c4de0..0535e79  main       -&gt; origin/main<br />Successfully rebased and updated refs/heads/main.</pre> <p>After rebase, the history becomes:</p> <pre>git log --oneline --decorate --graph --all<br />* ebf7c5c (HEAD -&gt; main) main: modified readme - README.md<br />* 0535e79 (origin/main) Update 2 config.yaml<br />* d9b0764 Updated 1 config.yaml<br />* e5c4de0 Included Hyperparameter tuning<br />* 86f6a84 Created svm_classification.py<br />* 453b2c9 Rebasing Done with Updated Readme</pre> <p>Notice that your commit (ebf7c5c) now appears <strong>on top</strong> of the remote commits, making history linear and easier to follow.</p> <h3>3.4 Comparative View</h3> <p>To consolidate the differences, here is a quick reference table:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*XyQDl9jTYcNDFLHsF9KjPQ.png"/></figure> <h3>Section 3: Summary</h3> <ul><li><strong>git fetch</strong> updates your awareness of the remote but does not affect your working branch. Think of it as “checking for mail.”</li><li><strong>git pull</strong> combines fetch + merge, synchronizing immediately but often introducing merge commits.</li><li><strong>git pull --rebase</strong> combines fetch + rebase, reapplying your local commits on top of remote commits for a clean, linear history.</li><li>In team environments, prefer fetch for safety, pull for shared long-lived branches, and pull --rebase for personal or short-lived branches.</li></ul> <h3>Section 4: Recovering Work with Git Reflog</h3> <p>One of the most intimidating experiences for a developer using Git is the moment you think you’ve “lost” your work. Maybe you ran a hard reset and wiped out a series of commits, or perhaps a rebase rewrote history in ways you didn’t expect. In most tools, that work would be gone forever. In Git, however, there is a safety net: <strong>the reflog</strong>.</p> <p>The reflog, short for <em>reference log</em>, is Git’s internal diary. Every time the tip of a branch changes — whether by commit, checkout, reset, merge, or rebase — Git writes an entry into the reflog. Unlike git log, which shows only the commits reachable in the current branch history, the reflog records <strong>all movements of HEAD</strong>, even those that have since been abandoned. This means that even if commits “disappear” from git log, they can often still be recovered through the reflog.</p> <h3>4.1 What is the Reflog?</h3> <p>At a high level, reflog is your personal undo history. It is <strong>local-only</strong> — each developer’s reflog exists in their .git/logs/ directory and is not shared with collaborators. It records every movement of the HEAD pointer:</p> <ul><li>checking out a new branch,</li><li>committing a change,</li><li>resetting to an earlier commit,</li><li>merging or pulling,</li><li>or even rebasing.</li></ul> <p>The crucial difference between git log and git reflog is that log shows the <em>official branch history</em>, while reflog shows the <em>unofficial local history</em> of how you got there.</p> <p>To see it, run:</p> <pre>git reflog</pre> <h3>4.2 Inspecting the Reflog</h3> <p>Let’s run a real example to illustrate.</p> <pre>git reflog</pre> <p>Output:</p> <pre>ebf7c5c (HEAD -&gt; main, origin/main) HEAD@{0}: pull --rebase origin main (finish): returning to refs/heads/main<br />ebf7c5c (HEAD -&gt; main, origin/main) HEAD@{1}: pull --rebase origin main (pick): main: modified readme - README.md<br />0535e79 HEAD@{2}: pull --rebase origin main (start): checkout 0535e79...<br />e7d0f19 HEAD@{3}: commit: main: modified readme - README.md<br />e5c4de0 HEAD@{4}: merge origin/main: Fast-forward<br />453b2c9 HEAD@{5}: commit: Rebasing Done with Updated Readme<br />...</pre> <p>Here’s how to interpret this output:</p> <ul><li>HEAD@{0} is the most recent action: completing a pull with rebase.</li><li>HEAD@{3} shows a commit you made earlier (main: modified readme).</li><li>HEAD@{5} is a commit that might not appear in your git log anymore if you rewrote history — but it’s still accessible via the reflog.</li></ul> <p>This is why reflog is often described as Git’s <em>time machine</em>: it allows you to travel back to states that no longer belong to the visible branch history.</p> <h3>4.3 Why Reflog Matters</h3> <p>Imagine you accidentally ran a destructive command:</p> <pre>git reset --hard bee45fc</pre> <p>Suddenly, several commits vanish from git log. At first glance, it appears they’re gone forever. But because the reflog logs every pointer movement, those commits still exist in the object database. They are simply no longer referenced by the branch.</p> <pre>git reflog<br />bee45fc (HEAD -&gt; main) HEAD@{0}: reset: moving to bee45fc<br />ebf7c5c (origin/main) HEAD@{1}: reset: moving to ebf7c5c<br />bee45fc (HEAD -&gt; main) HEAD@{2}: commit: main: deleted server info - config.yaml</pre> <p>Even though HEAD now points to bee45fc, you can still recover the abandoned commits by referencing their reflog entries.</p> <h3>4.4 Inspecting Past States</h3> <p>The reflog allows you to examine any past state using either numerical indices (HEAD@{n}) or time-based queries.</p> <p>Numerical access:</p> <pre>git show HEAD@{29}</pre> <p>Output:</p> <pre>commit f86213de...<br />Author: Suraj Bhardwaj<br />Date:   Thu Sep 11 17:45:43 2025 +0200<br /><br />first commit</pre> <p>Time-based access:</p> <pre>git show main@{1.hour.ago}<br />git show main@{yesterday}<br />git show main@{1.week.ago}</pre> <p>Example:</p> <pre>git show main@{1.hour.ago}</pre> <p>Output:</p> <pre>commit e5c4de0...<br />Author: Suraj Bhardwaj<br />Date:   Fri Sep 12 10:29:35 2025 +0200<br /><br />Included Hyperparameter tuning</pre> <p>This ability to query the reflog by both sequence and time makes it extremely flexible for investigating past states.</p> <h3>4.5 Recovering Work with Reflog</h3> <p>The most common use case for reflog is recovering commits after an accidental reset, rebase, or amend.</p> <h4>Scenario 1: Hard Reset Gone Wrong</h4> <p>Suppose you reset backwards, thinking you no longer needed your latest commits:</p> <pre>git reset --hard bee45fc</pre> <p>After this, git log shows no trace of the lost commits. But the reflog does:</p> <pre>git reflog<br />bee45fc (HEAD -&gt; main) HEAD@{0}: reset: moving to bee45fc<br />ebf7c5c (origin/main) HEAD@{1}: reset: moving to ebf7c5c</pre> <p>You can restore those commits by checking out an earlier state and branching from it:</p> <pre>git checkout HEAD@{1}<br />git checkout -b restore-branch<br />git checkout main<br />git merge restore-branch<br />git branch -d restore-branch</pre> <p>Your “lost” work is safely reintegrated into main.</p> <h4>Scenario 2: Rebase Misstep</h4> <p>Rebasing often requires resolving conflicts commit by commit. If you make a mistake during rebase and abort or reset incorrectly, commits can appear to vanish. Reflog preserves each step of the rebase (pick, squash, continue) so you can re-locate the original commits and restore them.</p> <p>For example:</p> <pre>HEAD@{0}: rebase finished: returning to refs/heads/feature-branch<br />HEAD@{1}: rebase: commit message adjusted<br />HEAD@{2}: rebase: conflict resolved in 404.html</pre> <p>Even if the branch tip changes, each commit can be recovered via its reflog reference.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*30S0FVPB1FLN_wTfrLyrAg.png"/></figure> <h3>4.6 Where Reflog Data Lives</h3> <p>Technically, reflog entries are stored in .git/logs/. Every branch has its own log file, and there’s also a log for HEAD. Over time, very old entries may be cleaned up by Git’s garbage collection (git gc). This means reflog is not infinite/ the reflog is not permanent; it is a short- to medium-term recovery tool, not a permanent archive.</p> <h3>4.7 Reflog Cheat Sheet</h3> <p>Some essential commands to remember:</p> <pre># Show full reflog of HEAD<br />git reflog<br /># Show reflog of a specific branch<br />git reflog show main<br /># Show commit at specific reflog index<br />git show HEAD@{5}<br /># Checkout an old state (detached)<br />git checkout HEAD@{10}<br /># Recover work by branching from reflog<br />git checkout -b restore-branch HEAD@{8}<br /># Query reflog by time<br />git show main@{yesterday}<br />git show main@{2.weeks.ago}<br /># Diff between states<br />git diff @{1.hour.ago}<br /># Show reflog in log format<br />git log -g</pre> <h3>Section 4: Summary</h3> <p>The reflog is one of Git’s most powerful yet underutilized features. While git log shows the curated history of your project, the reflog records every pointer movement, allowing you to step back through time and recover work that appears lost.</p> <ul><li>It acts as a <strong>safety net</strong>, logging commits even if they are no longer referenced.</li><li>It allows you to <strong>restore commits after destructive commands</strong> like reset --hard or failed rebases.</li><li>It is <strong>local-only</strong>, invisible to collaborators, and subject to garbage collection.</li><li>In practice, reflog transforms Git from a potentially destructive tool into a recoverable system, empowering developers to experiment confidently.</li></ul> <p>Whenever you feel panic because “my commits are gone,” the reflog should be the very first place you look.</p> <h3>Conclusion</h3> <p>Mastering advanced Git commands is more than just memorizing syntax; it is about understanding how history, collaboration, and recovery mechanisms work together in a distributed version control system.</p> <ul><li>With <strong>commit rewriting</strong> (amend and interactive rebase), you can polish and clarify your project history before sharing it.</li><li>With <strong>branch rebasing</strong>, you can maintain a linear timeline that makes it easier to follow changes across teams.</li><li>With <strong>remote workflows</strong> (fetch, pull, pull --rebase), you gain control over how external changes integrate into your local branch.</li><li>And with the <strong>reflog</strong>, you have a safety net that allows you to recover work even after mistakes that seem irreversible.</li></ul> <p>Together, these commands transform Git from a basic tool into a powerful system that gives you confidence to experiment, collaborate, and maintain clean project histories.</p> <p>If you found this guide useful and want to follow more of my deep dives into <strong>software engineering, Git workflows, and AI/ML development practices</strong>, feel free to connect with me on LinkedIn: <a href="https://www.linkedin.com/in/bhardwaj-suraj/">LinkedIn</a>.</p> <p>Do check out my GitHub repository for the more information on this guide: [<a href="https://github.com/SurajBhar/advance_git">GitHub Repository</a>].</p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=fd1a16f82350" width="1" height="1" alt=""/></p>]]></content><author><name></name></author></entry></feed>